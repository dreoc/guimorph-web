% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template_kmeans.R
\name{.template_kmeans_centers}
\alias{.template_kmeans_centers}
\title{Spatially even k-means sample of a point cloud or mesh}
\usage{
.template_kmeans_centers(
  x,
  k,
  iter.max = 100,
  project = TRUE,
  threads = 0,
  seed = GUIMORPHWEB_TEMPLATE_SEED,
  max.restarts = 10L
)
}
\arguments{
\item{x}{numeric matrix of coordinates (1-3 columns), or a \code{mesh3d}.}

\item{k}{number of clusters. Must be smaller than the number of points.}

\item{iter.max}{maximum Lloyd iterations per attempt.}

\item{project}{logical. If \code{x} is a triangular mesh, project the final
centres back onto its surface.}

\item{threads}{integer passed to Rvcg. 0 lets Rvcg choose.}

\item{seed}{integer seed. Defaults to \code{GUIMORPHWEB_TEMPLATE_SEED}.}

\item{max.restarts}{attempts allowed when an initialisation strands an empty
cluster.}
}
\value{
numeric matrix of cluster centres, \code{k} rows by \code{ncol(x)}.
}
\description{
Drop-in replacement for the \code{$centers} component of
\code{Morpho::fastKmeans}. All three call sites in this package use only that
component, so \code{selected} and \code{class} are not computed.
}
\keyword{internal}
