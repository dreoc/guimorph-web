---
phase: 12-in-gui-feedback-status
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r
autonomous: true
requirements: [UX-FB-03, UX-FB-01]
must_haves:
  truths:
    - "The 9 navigation-gate tkmessageBox modals in onNext/onPrevious are gone; gates now show inline status + disable nav buttons (UX-FB-03, D-03)"
    - "Nav gating is proactive — updateDotNum/updateAnchorNum disable/enable Next/Previous live as markers are placed, without a click (UX-FB-03, D-03)"
    - "Specimen display state is surfaced to the status bar (Specimen {id} — {n} of {N}) from showPicture (UX-FB-01, D-02)"
  artifacts:
    - path: "3dDigitize.main.r"
      provides: "onNext/onPrevious gates converted to setStatus + tkconfigure(state); showPicture console-parity"
      contains: "setStatus(e,"
    - path: "3dDigitize.digitize.r"
      provides: "proactive nav-gate hooks at tail of updateDotNum/updateAnchorNum"
      contains: "e$nextBtn"
  key_links:
    - from: "updateDotNum/updateAnchorNum"
      to: "e$nextBtn / e$prevBtn"
      via: "tkconfigure state disabled/normal on count check"
      pattern: "tkconfigure\\(e\\$nextBtn, state"
    - from: "showPicture"
      to: "e$statusLabel"
      via: "setStatus neutral specimen counter"
      pattern: "setStatus\\(e,"
---

<objective>
Convert all navigation flow-control modals to the non-blocking pattern and make the gate proactive.
Replace the 9 `tkmessageBox(...); return()` blocks in `onNext` (main.r:897/913/926/942/958) and
`onPrevious` (main.r:1073/1085/1098/1109) with `setStatus(..., "warning")` + nav-button disable;
add proactive disable/enable hooks at the tail of `updateDotNum`/`updateAnchorNum` (digitize.r);
and surface specimen-display state to the status bar from `showPicture` (main.r:746).

Purpose: UX-FB-03 (modal → inline + disabled controls) and the UX-FB-01 "specimen displayed"
console-parity, per D-02/D-03. The gate predicate is preserved; only the feedback changes.
Output: converted call sites + proactive hooks (no new symbols beyond Plan 12-01's).
</objective>

<artifacts_this_phase_produces>
No new R symbols — this plan consumes Plan 12-01's helpers (`setStatus`) and stored widgets
(`e$nextBtn`, `e$prevBtn`, `e$statusLabel`). It converts existing call sites in `onNext`,
`onPrevious`, `showPicture`, `updateDotNum`, and `updateAnchorNum`.
</artifacts_this_phase_produces>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md
@.planning/phases/12-in-gui-feedback-status/12-PATTERNS.md
@.planning/phases/12-in-gui-feedback-status/12-RESEARCH.md
@.planning/phases/12-in-gui-feedback-status/12-CONTEXT.md
@.planning/phases/12-in-gui-feedback-status/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 9 onNext/onPrevious nav modals with inline status + nav-button disable</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (onNext block ~895-965 with tkmessageBox at 897/913/926/942/958; onPrevious block ~1071-1116 with tkmessageBox at 1073/1085/1098/1109; existing disable analog `tkconfigure(e$bt, state = "disabled")` at 973)
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (§Non-blocking validation table — exact replacement copy per call site)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"onNext — nav-gate modal replacement"; §"onPrevious — nav-gate modal replacement")
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (Pitfall 5: keep return(), remove only tkmessageBox; Open Question #2: tab!=0 gate is hint-only, do NOT hard-block)
  </read_first>
  <action>
    In `onNext`, remove each `tkmessageBox(...)` call (lines 897/913/926/942/958) and keep the surrounding `return()` (Pitfall 5 — return() correctly aborts nav). Replace each with the exact UI-SPEC copy via `setStatus(e, ..., "warning")`: last-specimen gate → `setStatus(e, paste0("Already at the last specimen (", e$currImgId, " of ", length(e$activeDataList), ")."), "warning")` plus `tkconfigure(e$nextBtn, state = "disabled")`; landmark-count gate → `setStatus(e, paste0("Place all ", e$landmarkNum, " landmarks before continuing \u2014 ", <current>, " of ", e$landmarkNum, " placed."), "warning")` plus `tkconfigure(e$nextBtn, state = "disabled")` and `tkconfigure(e$prevBtn, state = "disabled")`; anchor-count gates (926/942) → same shape with "anchors" and `e$anchorNum`; "open digitizing tab" gate (958, where `e$tab != 0`) → `setStatus(e, "Switch to the 3D Digitizing tab to change specimen.", "warning")` and keep `return()` but do NOT disable nav buttons (Open Question #2 — hint-only). In `onPrevious`, mirror exactly: first-specimen gate (1073) → `setStatus(e, paste0("Already at the first specimen (1 of ", length(e$activeDataList), ")."), "warning")` plus `tkconfigure(e$prevBtn, state = "disabled")`; landmark/anchor gates (1085/1098) → identical warning copy as onNext; tab gate (1109) → identical hint-only copy. Use `\u2014` (em-dash) literals so copy matches the UI-SPEC exactly. Do NOT alter any gate predicate.
  </action>
  <acceptance_criteria>
    - `grep -c 'tkmessageBox' R/3dDigitize.main.r` decreases by exactly 9 versus the pre-task count (the 9 nav-gate sites removed); the surviving tkmessageBox sites are the non-flow-control ones (e.g. lines ~1581/1637/2581, out of scope).
    - 3dDigitize.main.r contains the exact strings `Already at the last specimen (` and `Already at the first specimen (1 of ` and `Place all ` ... ` before continuing \u2014 ` and `Switch to the 3D Digitizing tab to change specimen.`
    - Each converted gate still ends in `return()` (Pitfall 5); the tab!=0 gate calls setStatus but contains no `tkconfigure(e$nextBtn` / `e$prevBtn` disable (hint-only, Open Question #2).
    - Last/first gates call `tkconfigure(e$nextBtn, state = "disabled")` / `tkconfigure(e$prevBtn, state = "disabled")` respectively.
    - Behavior (manual UAT): triggering each former modal shows an amber (#b35900) status message and a disabled nav button — no pop-up.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'Already at the last specimen (' R/3dDigitize.main.r && grep -nF 'Already at the first specimen (1 of ' R/3dDigitize.main.r && grep -nF 'Switch to the 3D Digitizing tab to change specimen.' R/3dDigitize.main.r</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && test "$(grep -c 'tkmessageBox' R/3dDigitize.main.r)" -le 4 && echo "nav modals removed"</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>All 9 nav modals replaced by inline warning status + nav disable (tab gate hint-only); return() preserved; package loads clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add proactive nav-gate hooks to updateDotNum and updateAnchorNum</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r (`updateDotNum <-` at 954, tab-enable block 967-981; `updateAnchorNum <-` at 985, tab-enable block ~998-1005; `e$placeAnchorsVar` usage)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"updateDotNum — proactive nav-gate hook"; §"updateAnchorNum — proactive nav-gate hook" — includes the !is.null guard)
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (§Code Examples → "Proactive nav-gate disable (tail of updateDotNum)")
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (warning copy "Place all {target} ... before continuing — {current} of {target} placed.")
  </read_first>
  <action>
    At the tail of `updateDotNum` (after the existing tab-enable block ending ~line 981, still inside the function), append a proactive gate guarded by `if (!is.null(e$nextBtn) && !is.null(e$prevBtn))` (the guard prevents firing before ui.main builds the bar — e.g. during .dgt reload at startup). With `nTarget <- as.integer(e$landmarkNum)`: when `nDots < nTarget`, `setStatus(e, paste0("Place all ", nTarget, " landmarks before continuing \u2014 ", nDots, " of ", nTarget, " placed."), "warning")` and `tkconfigure(e$nextBtn, state = "disabled")` + `tkconfigure(e$prevBtn, state = "disabled")`; else `setStatus(e, paste0("Specimen ", e$currImgId, " \u2014 ", e$currImgId, " of ", length(e$activeDataList)), "neutral")` and re-enable both buttons with `state = "normal"`. Mirror the same pattern at the tail of `updateAnchorNum` using `nTarget <- as.integer(e$anchorNum)`, `nAnchors`, "anchors" copy, and additionally guard the whole block with `tclvalue(e$placeAnchorsVar) == "1"` (only gate on anchors when anchor placement is active). Use `\u2014` literals.
  </action>
  <acceptance_criteria>
    - 3dDigitize.digitize.r updateDotNum tail contains `if (!is.null(e$nextBtn) && !is.null(e$prevBtn))` and both `tkconfigure(e$nextBtn, state = "disabled")` and `tkconfigure(e$nextBtn, state = "normal")` branches.
    - updateDotNum contains the exact copy fragment `landmarks before continuing \u2014 ` and the neutral re-set copy `Specimen ` ... ` \u2014 `.
    - updateAnchorNum tail contains the anchor warning `anchors before continuing \u2014 ` guarded by `tclvalue(e$placeAnchorsVar) == "1"`.
    - Behavior (manual UAT): on the Digitize tab, Next ▸ and ◂ Previous are disabled while the landmark count is incomplete and re-enable automatically (no click) when the count is met; amber "{k} of {target} placed" warning shows during the incomplete state.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'landmarks before continuing \u2014 ' R/3dDigitize.digitize.r && grep -nF 'anchors before continuing \u2014 ' R/3dDigitize.digitize.r && grep -nF 'if (!is.null(e$nextBtn) && !is.null(e$prevBtn))' R/3dDigitize.digitize.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>Both count handlers proactively disable/enable nav buttons with the exact warning copy, guarded against pre-build calls; package loads clean.</done>
</task>

<task type="auto">
  <name>Task 3: Surface specimen-display state to the status bar from showPicture</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (`print(paste("showPicture : calling set 'specimen, id' : ", e$currImgId))` at 746)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"showPicture — console-parity status")
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (status copy "Specimen {id} — {n} of {N}", neutral)
    - .planning/phases/12-in-gui-feedback-status/12-CONTEXT.md (D-02: keep print() console-only output AND add GUI parity for meaningful states; do not remove the print)
  </read_first>
  <action>
    Immediately after the existing `print(paste("showPicture : ...", e$currImgId))` at main.r:746 (keep the print — D-02 says console output may remain), add `setStatus(e, paste0("Specimen ", e$currImgId, " \u2014 ", e$currImgId, " of ", length(e$activeDataList)), "neutral")`. Guard with `if (!is.null(e$statusLabel))` so a showPicture call during early startup (before the status bar is built) does not error. Use the `\u2014` em-dash literal to match the UI-SPEC "Specimen {id} — {n} of {N}" copy. Do not change any rendering/`set("specimen",...)` logic (CON-01).
  </action>
  <acceptance_criteria>
    - 3dDigitize.main.r still contains the original `print(paste("showPicture :` line (D-02 console output preserved).
    - 3dDigitize.main.r contains `setStatus(e, paste0("Specimen ", e$currImgId, " \u2014 ", e$currImgId, " of ", length(e$activeDataList)), "neutral")` guarded by `if (!is.null(e$statusLabel))`.
    - No `set("specimen", ...)` / renderer call was added, removed, or reordered around line 746 (CON-01).
    - Behavior (manual UAT): displaying a specimen updates the status bar to "Specimen {id} — {n} of {N}" in neutral color.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'setStatus(e, paste0("Specimen ", e$currImgId, " \u2014 ", e$currImgId, " of ", length(e$activeDataList)), "neutral")' R/3dDigitize.main.r && grep -nF 'print(paste("showPicture :' R/3dDigitize.main.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>showPicture emits the specimen counter to the status bar (guarded) while keeping the console print; renderer untouched; package loads clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local desktop GUI. Converted gates operate on `e$currImgId`/`e$landmarkNum`/`e$anchorNum` (R integers) and program-generated warning strings. No network, no new input parsing, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering/Injection | warning strings via `paste0` in onNext/onPrevious/updateDotNum | accept | Only `paste0` over R scalars; no `eval`/`parse`; gate predicates unchanged (RESEARCH §Security Domain). |
| T-12-02-DoS | Denial of Service | re-entrant nav from event flush | mitigate | No `tcl("update")` introduced in this plan; nav remains synchronous. Busy-path repaint is handled by busyStart (Plan 12-01) using `update idletasks`. |

No `high` threats. No package installs; renderer/`.dgt` paths untouched (CON-01/02).
</threat_model>

<verification>
- `grep -c 'tkmessageBox' R/3dDigitize.main.r` is ≤ 4 after Task 1 (9 nav modals removed; only out-of-scope modals remain).
- `R -q -e "devtools::load_all('...')"` loads clean.
- Manual UAT: nav gates show inline amber warnings + disabled buttons (no modal); buttons re-enable proactively; specimen counter shows on display.
</verification>

<success_criteria>
- 9 nav modals converted to inline status + disabled controls (tab gate hint-only).
- Proactive enable/disable wired into updateDotNum/updateAnchorNum with guards.
- showPicture surfaces the specimen counter while preserving the console print.
</success_criteria>

<output>
Create `.planning/phases/12-in-gui-feedback-status/12-02-SUMMARY.md` when done.
</output>
