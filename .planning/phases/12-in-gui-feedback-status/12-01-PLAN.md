---
phase: 12-in-gui-feedback-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
autonomous: true
requirements: [UX-FB-01, UX-FB-02, UX-FB-03]
must_haves:
  truths:
    - "A persistent status bar renders at the bottom of e$wnd showing program-generated text (UX-FB-01)"
    - "A shared ttkprogressbar is docked at the right of the status bar, ready for busy/determinate use (UX-FB-02, D-01)"
    - "Nav buttons (e$nextBtn/e$prevBtn) and GPA action buttons (e$computeBtn/e$saveBtn/e$plotBtn) are stored on e so helpers can disable them (UX-FB-03)"
    - "An inline GPA validation label (e$gpaStatusLabel) exists near the Compute control (UX-FB-03)"
  artifacts:
    - path: "3dDigitize.main.r"
      provides: ".STATUS_FG vector + setStatus/busyStart/busyStop helpers + status bar widgets (e$statusLabel, e$progressBar, e$statusFrame)"
      contains: "setStatus <- function(e, text, state"
    - path: "3dDigitize.main.r"
      provides: "Nav button env storage in createNavFrame"
      contains: "e$nextBtn <- nextBtn"
    - path: "3dDigitize.geomorph.r"
      provides: "GPA action button env storage + inline label in ui.geomorph"
      contains: "e$gpaStatusLabel <-"
  key_links:
    - from: "setStatus"
      to: "e$statusLabel"
      via: "tkconfigure foreground from .STATUS_FG"
      pattern: "tkconfigure\\(e\\$statusLabel"
    - from: "busyStart/busyStop"
      to: "e$progressBar / e$wnd cursor"
      via: "tcl progressbar start/stop + watch cursor"
      pattern: "tcl\\(e\\$progressBar"
---

<objective>
Build the foundational status/feedback infrastructure that every other Phase 12 plan depends on:
the `.STATUS_FG` state→foreground map, the `setStatus`/`busyStart`/`busyStop` helper trio, the
bottom status bar (`e$statusLabel` + shared `e$progressBar` + `e$statusFrame`), and the two
env-storage gaps that let helpers disable controls (`createNavFrame` nav buttons; `ui.geomorph`
GPA action buttons + inline `e$gpaStatusLabel`).

Purpose: No call-site conversion can disable a button or repaint a status message until these
widgets are stored on `e` and the helpers exist. This is the Wave 1 foundation (RESEARCH.md
"critical sequencing": env-storage gaps + status API + widgets land before conversions).
Output: New R symbols on `e` and at file top — see "Artifacts this phase produces".
</objective>

<artifacts_this_phase_produces>
R symbols created across the whole phase (this plan creates the ones marked ★):
- ★ `.STATUS_FG` — named character vector (neutral/info/success/warning/error → hex)
- ★ `setStatus(e, text, state = "neutral")` — helper (3dDigitize.main.r)
- ★ `busyStart(e, text, mode = "indeterminate")` — helper (3dDigitize.main.r)
- ★ `busyStop(e, text = NULL, state = "neutral")` — helper (3dDigitize.main.r)
- ★ `e$statusLabel` — bottom status bar ttklabel
- ★ `e$progressBar` — shared ttkprogressbar (status bar, right-docked)
- ★ `e$statusFrame` — status bar ttkframe
- ★ `e$nextBtn`, `e$prevBtn` — nav button handles (from createNavFrame)
- ★ `e$computeBtn`, `e$saveBtn`, `e$plotBtn` — GPA action button handles (from ui.geomorph)
- ★ `e$gpaStatusLabel` — inline GPA validation ttklabel
(Subsequent plans consume these; no new files are created this phase — all in-place edits.)
</artifacts_this_phase_produces>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md
@.planning/phases/12-in-gui-feedback-status/12-PATTERNS.md
@.planning/phases/12-in-gui-feedback-status/12-RESEARCH.md
@.planning/phases/12-in-gui-feedback-status/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .STATUS_FG + setStatus/busyStart/busyStop helpers and the bottom status bar in ui.main</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (the `e$imgPath <- ttklabel(...)` anchor at 489-491; `createNavFrame` call at 502; `tkpack(centerFrame, side = "left", ...)` at 529; top-of-file before init.main for helper placement)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§".STATUS_FG / setStatus / busyStart / busyStop helpers" verbatim block; §"ui.main — status bar widget construction")
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (Color → State foreground tokens; Spacing → status bar bindings; empty-state copy)
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (§Recommended Helper Signatures; Pitfall 1 idletasks; Pitfall 4 pack order)
  </read_first>
  <action>
    Near the top of 3dDigitize.main.r (after the version-stamp helpers, before init.main, per PATTERNS placement note) define `.STATUS_FG` as a named character vector with exactly: neutral="#000000", info="#1a5fb4", success="#2e7d32", warning="#b35900", error="#c01c28" (D-04 amber token). Then define `setStatus(e, text, state = "neutral")` → `tkconfigure(e$statusLabel, text = text, foreground = .STATUS_FG[[state]])`; `busyStart(e, text, mode = "indeterminate")` → call setStatus(e,text,"info"), `tkconfigure(e$wnd, cursor = "watch")`, and when mode=="indeterminate" `tkconfigure(e$progressBar, mode = "indeterminate")` then `tcl(e$progressBar, "start", 20)`, finishing with `tcl("update", "idletasks")` (NOT `tcl("update")` — Pitfall 1, avoids re-entrant compute); `busyStop(e, text = NULL, state = "neutral")` → `tcl(e$progressBar, "stop")`, `tkconfigure(e$progressBar, mode = "determinate", value = 0)`, `tkconfigure(e$wnd, cursor = "")`, and if text non-NULL call setStatus. Copy the helper bodies from PATTERNS.md verbatim (no codebase analog exists). Then in `ui.main`, immediately BEFORE the `tkpack(centerFrame, side = "left", ...)` at line 529, construct the status bar mirroring the `e$imgPath` ttklabel pattern (490-491): `statusFrame <- ttkframe(e$wnd)`, `sepStatus <- ttkseparator(e$wnd, orient = "horizontal")`, `e$statusLabel <- ttklabel(statusFrame, text = "Ready \u2014 open a PLY file to begin.", foreground = "#000000")`, `e$progressBar <- ttkprogressbar(statusFrame, mode = "determinate", length = 160, value = 0)`, `e$statusFrame <- statusFrame`; pack `e$progressBar` side="right" padx=8 pady=4, `e$statusLabel` side="left" padx=8 pady=4 anchor="w", then `sepStatus` side="bottom" fill="x" and `statusFrame` side="bottom" fill="x" — all BEFORE centerFrame/tn are packed (Pitfall 4). Use the exact em-dash escape `\u2014` in the Ready string so the copy matches the UI-SPEC empty-state body.
  </action>
  <acceptance_criteria>
    - 3dDigitize.main.r contains `setStatus <- function(e, text, state` and `busyStart <- function(e, text, mode` and `busyStop <- function(e, text` (grep: all three present).
    - 3dDigitize.main.r contains `.STATUS_FG` with the literal `warning = "#b35900"` token (D-04).
    - 3dDigitize.main.r contains `e$statusLabel <- ttklabel(` and `e$progressBar <- ttkprogressbar(` and `e$statusFrame <- statusFrame`.
    - The Ready string is exactly `Ready \u2014 open a PLY file to begin.` (UI-SPEC empty-state body, em-dash form).
    - `busyStart` body contains `tcl("update", "idletasks")` and NOT a bare `tcl("update")` call (Pitfall 1).
    - The `tkpack(statusFrame, side = "bottom"` and `tkpack(sepStatus, side = "bottom"` calls both appear BEFORE the `tkpack(centerFrame, side = "left"` call in source order (Pitfall 4).
    - Behavior (manual UAT): launching the GUI shows a full-width status bar at the window bottom, below the viewport, with a separator above it, displaying "Ready — open a PLY file to begin."
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nE 'setStatus <- function|busyStart <- function|busyStop <- function|e\$statusLabel <- ttklabel|e\$progressBar <- ttkprogressbar|warning = "#b35900"' R/3dDigitize.main.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>Helpers and status bar exist; package parses/loads clean; status bar renders at bottom with the exact Ready copy.</done>
</task>

<task type="auto">
  <name>Task 2: Close env-storage gaps — store nav buttons (createNavFrame) and GPA action buttons + inline label (ui.geomorph)</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (`createNavFrame` at 690; `prevBtn` def at 692, `nextBtn` def at 699, `sapply(list(prevBtn, nextBtn), ...)` at 713 — store before return)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (`ui.geomorph` at 25; `cmputBtn` at 54 and `assign("bt2", cmputBtn, ...)` at 55; `saveBtn` at 58; `plotspecsBtn` at 57; `tkgrid(plotspecsBtn)` at 74; `return(gpagenCtlFrame)` at 75)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"createNavFrame — nav-button env storage"; §"ui.geomorph — env button + inline label storage")
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (Pitfall 3: e$bt is the Place Anchors checkbutton — do NOT repurpose it)
  </read_first>
  <action>
    In `createNavFrame` (3dDigitize.main.r), after `prevBtn` and `nextBtn` are defined (around line 699-705) and before the function returns its frame, add direct env assignments `e$prevBtn <- prevBtn` and `e$nextBtn <- nextBtn` (use direct `e$xxx <-`, NOT `assign(...)` — the newer pattern; do NOT touch `e$bt`, which is the Place Anchors checkbutton per Pitfall 3). In `ui.geomorph` (3dDigitize.geomorph.r), after `tkgrid(plotspecsBtn)` at line 74 and before `return(gpagenCtlFrame)` at line 75, add `e$computeBtn <- cmputBtn`, `e$saveBtn <- saveBtn`, `e$plotBtn <- plotspecsBtn` (leave the existing `assign("bt2", cmputBtn, envir = e)` at line 55 intact — `e$computeBtn` is a semantic alias to the same handle), then create the inline GPA validation label `e$gpaStatusLabel <- ttklabel(gpagenCtlFrame, text = "", foreground = .STATUS_FG[["warning"]])` and place it with `tkgrid(e$gpaStatusLabel, sticky = "w", padx = 8)` so it sits near the Compute/Save/Plot controls (D-03 inline compute-gate surface).
  </action>
  <acceptance_criteria>
    - 3dDigitize.main.r contains `e$nextBtn <- nextBtn` and `e$prevBtn <- prevBtn` inside createNavFrame; no new reference to `e$bt` was added (Pitfall 3 preserved).
    - 3dDigitize.geomorph.r contains `e$computeBtn <- cmputBtn`, `e$saveBtn <- saveBtn`, `e$plotBtn <- plotspecsBtn`.
    - 3dDigitize.geomorph.r contains `e$gpaStatusLabel <- ttklabel(gpagenCtlFrame, text = "", foreground = .STATUS_FG[["warning"]])` followed by a `tkgrid(e$gpaStatusLabel` placement.
    - The original `assign("bt2", cmputBtn, envir = e)` (geomorph.r:55) is still present (not removed).
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nE 'e\$nextBtn <- nextBtn|e\$prevBtn <- prevBtn' R/3dDigitize.main.r && grep -nE 'e\$computeBtn <- cmputBtn|e\$saveBtn <- saveBtn|e\$plotBtn <- plotspecsBtn|e\$gpaStatusLabel <- ttklabel' R/3dDigitize.geomorph.r && grep -nF 'assign("bt2", cmputBtn, envir = e)' R/3dDigitize.geomorph.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>Nav and GPA action button handles plus the inline GPA label are stored on e; package loads clean; e$bt and e$bt2 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local single-user desktop R/Tk GUI. No network, no IPC, no auth, no new file parsing. Status strings are program-generated (R integers + filenames from the trusted Tk file picker), not user-supplied free text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering/Injection | status label text built via `paste0` | accept | No `eval`/`parse`; only `paste0`/`sprintf` over R scalars + picker-sourced paths (RESEARCH §Security Domain V5). No injection surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — all widgets are built-in Tk 8.6 / ttk (RESEARCH §Package Legitimacy Audit). |

No `high` threats. Phase touches only widget state and program-generated strings on a local desktop GUI (CON-01/02/03 unchanged).
</threat_model>

<verification>
- `R -q -e "devtools::load_all('...GUImorphDevelopment')"` parses and loads with no error (VALIDATION.md quick run).
- Source assertions in each task's `<verify>` pass.
- Manual UAT: status bar renders at bottom full-width with separator and the exact Ready copy.
</verification>

<success_criteria>
- `.STATUS_FG`, `setStatus`, `busyStart`, `busyStop` exist with the specified signatures.
- `e$statusLabel`, `e$progressBar`, `e$statusFrame`, `e$nextBtn`, `e$prevBtn`, `e$computeBtn`, `e$saveBtn`, `e$plotBtn`, `e$gpaStatusLabel` are all stored on `e`.
- Package loads clean; `e$bt`/`e$bt2` preserved.
</success_criteria>

<output>
Create `.planning/phases/12-in-gui-feedback-status/12-01-SUMMARY.md` when done.
</output>
