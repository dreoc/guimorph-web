---
phase: 12-in-gui-feedback-status
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
autonomous: true
requirements: [UX-FB-02, UX-FB-01]
must_haves:
  truths:
    - "Multi-file PLY load shows a determinate progressbar stepping per file with '{i} of {N}' status (UX-FB-02, D-01)"
    - "PLY load surfaces a load-complete summary with specimen count to the status bar (UX-FB-01, D-02)"
    - "A failed PLY load shows an inline error status instead of a frozen/silent UI (UX-FB-02)"
  artifacts:
    - path: "3dDigitize.main.r"
      provides: "loadPly determinate per-file add('specimen',...) loop with progress + completion/error status"
      contains: "mode = \"determinate\""
  key_links:
    - from: "loadPly"
      to: "e$progressBar"
      via: "determinate maximum=N, step per file"
      pattern: "tkconfigure\\(e\\$progressBar, value ="
    - from: "loadPly loop"
      to: "add(\"specimen\", ...)"
      via: "per-file load with status update before each add"
      pattern: "add\\(\"specimen\""
---

<objective>
Instrument the PLY load path with determinate per-file progress. Refactor `loadPly` (main.r:1273-1274)
so the single `add("specimen", e$activeDataList[[1]][[1]], e$currImgId)` becomes a loop over all N
specimens (the C engine already pre-allocates via `set("specimen","allocate",N)`), stepping the
shared `e$progressBar` and updating the status with the UI-SPEC "Loading {file} ({i} of {N})…" copy,
finishing with a load-complete summary, and surfacing PLY load failures inline.

Purpose: UX-FB-02 (determinate per-file progress, D-01) and UX-FB-01 load-complete parity (D-02).
This is Wave 3 because it edits `3dDigitize.main.r`, which Plans 12-01 and 12-02 also edit
(file-ownership serialization).
Output: refactored loadPly loop (no new symbols beyond Plan 12-01's).
</objective>

<artifacts_this_phase_produces>
No new R symbols — consumes Plan 12-01's `setStatus`/`busyStop`, `e$progressBar`, and `.STATUS_FG`.
Refactors `loadPly` in 3dDigitize.main.r.
</artifacts_this_phase_produces>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md
@.planning/phases/12-in-gui-feedback-status/12-PATTERNS.md
@.planning/phases/12-in-gui-feedback-status/12-RESEARCH.md
@.planning/phases/12-in-gui-feedback-status/12-CONTEXT.md
@.planning/phases/12-in-gui-feedback-status/12-01-SUMMARY.md
@.planning/phases/12-in-gui-feedback-status/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify add('specimen',...) idempotency, then refactor loadPly into a determinate per-file loop</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (`set("specimen", "allocate", length(e$activeDataList))` at 1273; `add("specimen", e$activeDataList[[1]][[1]], e$currImgId)` at 1274; surrounding loadPly body)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R (the `add` shim — confirm whether re-loading a slot is harmless; RESEARCH Open Question #1 / Assumption A1)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"loadPly — determinate PLY loop refactor" core loop pattern)
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (§Busy/progress — PLY load: "Loading {file} ({i} of {N})…" / "Loaded {N} specimen{s}." / determinate i/N)
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (Pitfall 2 lazy-load; A1 idempotency; Pitfall 1 idletasks)
  </read_first>
  <action>
    First resolve Assumption A1: grep `rtkogl.R` and the nav handlers for `add("specimen"` (e.g. in `onNext`/`onPrevious`) to determine whether pre-loading all N slots conflicts with the lazy per-nav `add`. If `add("specimen", file, i)` is idempotent / already-loaded-safe, proceed with the full pre-load loop; if NOT, leave the existing nav-handler `add` calls in place (do not remove) and still pre-load — record the finding in the SUMMARY. Then replace lines 1273-1274 with: `nSpecimens <- length(e$activeDataList)`, keep `set("specimen", "allocate", nSpecimens)`, `tkconfigure(e$progressBar, mode = "determinate", maximum = as.numeric(nSpecimens), value = 0)`, then a `for (i in seq_len(nSpecimens))` loop that, per iteration, sets `fname <- e$activeDataList[[i]][[1]]`, calls `setStatus(e, paste0("Loading ", basename(fname), " (", i, " of ", nSpecimens, ")\u2026"), "info")`, sets `tkconfigure(e$wnd, cursor = "watch")`, calls `tcl("update", "idletasks")` (Pitfall 1, NOT `tcl("update")`), then `add("specimen", fname, i)`, then `tkconfigure(e$progressBar, value = as.numeric(i))`. After the loop, `tkconfigure(e$wnd, cursor = "")`, `s <- if (nSpecimens == 1) "" else "s"`, `setStatus(e, paste0("Loaded ", nSpecimens, " specimen", s, "."), "success")`, and reset `tkconfigure(e$progressBar, mode = "determinate", value = 0)`. Use `\u2026` literal so copy matches the UI-SPEC. Do NOT alter `set("specimen","allocate",...)` semantics or the R↔Tcl protocol (CON-01).
  </action>
  <acceptance_criteria>
    - 3dDigitize.main.r `loadPly` contains a `for (i in seq_len(nSpecimens))` loop calling `add("specimen", fname, i)` inside it (replacing the single-slot `add(... e$activeDataList[[1]][[1]], e$currImgId)`).
    - loadPly contains `tkconfigure(e$progressBar, mode = "determinate", maximum = as.numeric(nSpecimens), value = 0)` and a per-iteration `tkconfigure(e$progressBar, value = as.numeric(i))`.
    - The per-iteration status copy is exactly `Loading ` + basename + ` (` + i + ` of ` + nSpecimens + `)\u2026` and the completion copy is `Loaded ` + nSpecimens + ` specimen` + s + `.` (UI-SPEC PLY rows).
    - loadPly uses `tcl("update", "idletasks")` inside the loop and contains no bare `tcl("update")` added by this task (Pitfall 1).
    - `set("specimen", "allocate", nSpecimens)` is preserved (allocation semantics unchanged, CON-01).
    - SUMMARY records the A1 idempotency finding and whether nav-handler `add` calls were left in place.
    - Behavior (manual UAT): loading a multi-file PLY set steps the determinate bar per file with "Loading {file} ({i} of {N})…", then shows green "Loaded {N} specimens.".
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'for (i in seq_len(nSpecimens))' R/3dDigitize.main.r && grep -nF 'maximum = as.numeric(nSpecimens)' R/3dDigitize.main.r && grep -nF ')\u2026' R/3dDigitize.main.r && grep -nF 'specimen", s, "."' R/3dDigitize.main.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>loadPly loads all N specimens with a determinate per-file bar, "{i} of {N}" status, and a load-complete summary; allocation/protocol unchanged; package loads clean; A1 finding recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline failure handling to the PLY load loop</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (the loadPly loop from Task 1; surrounding error context)
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (§Busy/progress — PLY failure copy "Could not load {file} — check the file and try again."; Error state body adds "See R console for details.")
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (A2 watch-cursor restore; Pitfall 1)
  </read_first>
  <action>
    Wrap the per-file `add("specimen", fname, i)` call inside the Task 1 loop in `tryCatch`. On error, restore the cursor with `tkconfigure(e$wnd, cursor = "")`, reset the bar with `tkconfigure(e$progressBar, mode = "determinate", value = 0)`, and call `setStatus(e, paste0("Could not load ", basename(fname), " \u2014 check the file and try again."), "error")`, then `return()` (or `break`/`stop` as fits the existing loadPly control flow) so a bad file does not freeze the loop. Keep any existing console diagnostics (D-02). Use the `\u2014` em-dash literal so the failure copy matches the UI-SPEC. Ensure the cursor is always restored even on the error path (A2).
  </action>
  <acceptance_criteria>
    - 3dDigitize.main.r loadPly contains a `tryCatch` around `add("specimen", fname, i)` with an `error =` handler.
    - The error handler contains the exact copy fragment `Could not load ` ... ` \u2014 check the file and try again.` passed to `setStatus(e, ..., "error")`.
    - The error handler restores the cursor (`tkconfigure(e$wnd, cursor = "")`) and resets the progressbar value to 0.
    - Behavior (manual UAT): attempting to load a corrupt/missing PLY shows the red "Could not load {file} — check the file and try again." status instead of a frozen UI, and the watch cursor reverts.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'Could not load ' R/3dDigitize.main.r && grep -nF 'check the file and try again.' R/3dDigitize.main.r && grep -nE 'tryCatch' R/3dDigitize.main.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>A failed PLY load surfaces an inline red error status, restores the cursor, and resets the bar; package loads clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file → loadPly | User selects `.ply` files via the trusted Tk file picker. This phase does NOT change PLY parsing (C-side, CON-01); it only loops the existing `add("specimen",...)` call and reports progress. Filenames pass through `basename()`/`paste0` into status text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering/Injection | filename interpolated into status label | accept | `basename(fname)` + `paste0` only; no `eval`/`parse`; path comes from the system file dialog (RESEARCH §Security Domain V5). |
| T-12-04-DoS | Denial of Service | malformed PLY freezing the load loop | mitigate | Task 2 `tryCatch` surfaces an inline error and restores cursor/bar so one bad file does not hang the UI. |
| T-12-04-RE | Denial of Service | re-entrant load via event flush | mitigate | Loop uses `tcl("update","idletasks")` (redraws only, not user events — Pitfall 1). |

No `high` threats. No package installs; PLY parsing and the R↔Tcl protocol unchanged (CON-01/02).
</threat_model>

<verification>
- `R -q -e "devtools::load_all('...')"` loads clean.
- Source assertions confirm the determinate loop, progress copy, completion copy, and error handling.
- Manual UAT: multi-file PLY load steps per file with "{i} of {N}" and a "Loaded {N} specimens." summary; bad file → inline error, cursor restored.
</verification>

<success_criteria>
- loadPly pre-loads all N specimens with a determinate per-file bar and the exact UI-SPEC progress/completion copy.
- PLY load failures surface inline (red) with cursor/bar restored.
- Allocation semantics, PLY parsing, and the R↔Tcl protocol unchanged; A1 finding recorded.
</success_criteria>

<output>
Create `.planning/phases/12-in-gui-feedback-status/12-04-SUMMARY.md` when done.
</output>
