---
phase: 12-in-gui-feedback-status
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
autonomous: true
requirements: [UX-FB-03, UX-FB-02, UX-FB-01]
must_haves:
  truths:
    - "The 3 geomorph flow-control modals (.gm_results_or_warn save/plot guard 144; compute landmark gate 167; compute anchor gate 195) are gone; gates show inline e$gpaStatusLabel + disable Save/Plot (UX-FB-03, D-03)"
    - "GPA Compute shows an indeterminate progressbar + watch cursor that paints before gpagen blocks the event loop (UX-FB-02, D-01)"
    - "GPA complete/failed states are surfaced to the GUI (UX-FB-01, D-02)"
  artifacts:
    - path: "3dDigitize.geomorph.r"
      provides: "inline-label compute gates + busyStart/busyStop wrapping of gpagen"
      contains: "busyStart(e,"
  key_links:
    - from: "compute"
      to: "geomorph::gpagen"
      via: "busyStart before / busyStop after"
      pattern: "busyStart\\(e,"
    - from: "compute gates"
      to: "e$gpaStatusLabel / e$saveBtn / e$plotBtn"
      via: "tkconfigure text+foreground / state disabled"
      pattern: "tkconfigure\\(e\\$gpaStatusLabel"
---

<objective>
Convert the GPA compute/save/plot flow-control modals to inline feedback and wrap the long
`gpagen` call with busy/progress feedback. Replace `tkmessageBox` at geomorph.r:144
(`.gm_results_or_warn` "Run Compute first"), :167 (compute landmark gate), and :195 (compute
anchor gate) with `e$gpaStatusLabel` warnings + Save/Plot disable; wrap `geomorph::gpagen` (239)
in `busyStart`/`busyStop` with the UI-SPEC GPA copy and an error path.

Purpose: UX-FB-03 (compute modal → inline + disabled controls, D-03), UX-FB-02 (GPA busy
feedback, D-01), and UX-FB-01 GPA complete/failed parity (D-02). `return()` in the compute gates
is preserved — aborting compute is correct (Pitfall 5).
Output: converted geomorph call sites (no new symbols beyond Plan 12-01's).
</objective>

<artifacts_this_phase_produces>
No new R symbols — consumes Plan 12-01's `busyStart`/`busyStop`/`setStatus`, `e$gpaStatusLabel`,
`e$saveBtn`, `e$plotBtn`, `e$progressBar`, and `.STATUS_FG`. Converts `.gm_results_or_warn` and
`compute` in 3dDigitize.geomorph.r.
</artifacts_this_phase_produces>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md
@.planning/phases/12-in-gui-feedback-status/12-PATTERNS.md
@.planning/phases/12-in-gui-feedback-status/12-RESEARCH.md
@.planning/phases/12-in-gui-feedback-status/12-CONTEXT.md
@.planning/phases/12-in-gui-feedback-status/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 3 geomorph compute/save/plot modals with inline e$gpaStatusLabel + Save/Plot disable</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (`.gm_results_or_warn <-` at 136 with tkmessageBox at 142; `compute <-` at 154; landmark gate tkmessageBox at 167; anchor gate tkmessageBox at 195)
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (§Non-blocking validation — geomorph.r:144/167/195 exact replacement copy)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§".gm_results_or_warn — modal replacement"; §"compute — landmark/anchor gate modal replacement")
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (Pitfall 5: keep return(); §Call-Site Inventory — geomorph gates)
  </read_first>
  <action>
    In `.gm_results_or_warn` (geomorph.r ~136-151), remove the `tkmessageBox(...)` (142) in the `is.null(gm.res)` branch and replace with `tkconfigure(e$gpaStatusLabel, text = "Run Compute before saving or plotting results.", foreground = .STATUS_FG[["warning"]])`, `tkconfigure(e$saveBtn, state = "disabled")`, `tkconfigure(e$plotBtn, state = "disabled")`, keeping the existing `return(NULL)`. In `compute`, replace the landmark-gate `tkmessageBox` (167): build `msg <- paste0("Specimen ", i, " needs ", as.numeric(e$landmarkNum), " landmarks \u2014 ", if (is.null(landmarks)) 0 else nrow(landmarks), " placed. Complete it before Compute.")`, then `tkconfigure(e$gpaStatusLabel, text = msg, foreground = .STATUS_FG[["warning"]])`, disable `e$saveBtn`/`e$plotBtn`, keep `return()`. Replace the anchor-gate `tkmessageBox` (195) with `msg <- paste0("Specimen ", i, " needs ", as.numeric(e$anchorNum), " anchors \u2014 ", nrow(anchors), " placed. Complete it before Compute.")` + same `tkconfigure(e$gpaStatusLabel, ...)` + Save/Plot disable + keep `return()`. Use `\u2014` literals. Leave the rgl/plot error dialog `tkmessageBox` at ~300 unchanged (out of scope — genuine error, not flow control).
  </action>
  <acceptance_criteria>
    - `grep -c 'tkmessageBox' R/3dDigitize.geomorph.r` returns exactly 1 (only the rgl/plot error dialog at ~300 remains); the 144/167/195 sites are removed.
    - 3dDigitize.geomorph.r contains the exact copy `Run Compute before saving or plotting results.` and the fragments `needs ` ... ` landmarks \u2014 ` and `needs ` ... ` anchors \u2014 ` and ` placed. Complete it before Compute.`
    - Each converted compute gate retains its `return()` (Pitfall 5) and calls `tkconfigure(e$saveBtn, state = "disabled")` + `tkconfigure(e$plotBtn, state = "disabled")`.
    - The rgl/plot error `tkmessageBox` near line 300 is still present (unchanged).
    - Behavior (manual UAT): with no GPA results, Save/Plot show inline "Run Compute before saving or plotting results." and are disabled; a wrong-count Compute shows an inline (non-modal) specimen warning.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && test "$(grep -c 'tkmessageBox' R/3dDigitize.geomorph.r)" -eq 1 && echo "only rgl error modal remains"</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'Run Compute before saving or plotting results.' R/3dDigitize.geomorph.r && grep -nF 'placed. Complete it before Compute.' R/3dDigitize.geomorph.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>All 3 geomorph flow-control modals are inline-label + disabled-control gates; return() preserved; rgl error modal untouched; package loads clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap gpagen with busyStart/busyStop indeterminate feedback and GPA complete/failed status</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (`print("before gpagen")` at 238; `e$gm.results <- geomorph::gpagen(...)` at 239; surrounding compute success path)
    - .planning/phases/12-in-gui-feedback-status/12-UI-SPEC.md (§Busy/progress — GPA Compute start/completion/failure copy; bar mode = indeterminate)
    - .planning/phases/12-in-gui-feedback-status/12-PATTERNS.md (§"compute — ... busy wrapping pattern")
    - .planning/phases/12-in-gui-feedback-status/12-RESEARCH.md (D-01 indeterminate mode; Pitfall 1 idletasks; A2 watch-cursor note)
  </read_first>
  <action>
    Immediately before the `geomorph::gpagen(...)` call at geomorph.r:239, call `busyStart(e, paste0("Computing GPA alignment for ", <N>, " specimens\u2026"), mode = "indeterminate")` where `<N>` is the specimen count already in scope in compute (e.g. `length(e$activeDataList)` or the existing loop bound — use the variable compute already uses). Wrap the `gpagen` call in `tryCatch`: on success, after gpagen returns, call `busyStop(e, paste0("GPA complete \u2014 ", <N>, " specimens aligned."), "success")`, then `tkconfigure(e$saveBtn, state = "normal")`, `tkconfigure(e$plotBtn, state = "normal")`, and `tkconfigure(e$gpaStatusLabel, text = "")` to clear the gate; on error (`error = function(err)`) call `busyStop(e, paste0("GPA failed \u2014 ", conditionMessage(err), ". See R console for details."), "error")` and re-raise or print the error to the console (keep existing console diagnostics — D-02). Use `\u2014` and `\u2026` literals so copy matches the UI-SPEC GPA rows. Do not change the gpagen arguments or the geomorph result handling (CON-01 — analysis logic unchanged; only feedback added).
  </action>
  <acceptance_criteria>
    - 3dDigitize.geomorph.r contains `busyStart(e, ` with the start copy fragment `Computing GPA alignment for ` ... ` specimens\u2026`.
    - 3dDigitize.geomorph.r contains the success copy `GPA complete \u2014 ` ... ` specimens aligned.` inside a `busyStop(e, ..., "success")` call, followed by `tkconfigure(e$saveBtn, state = "normal")` and `tkconfigure(e$plotBtn, state = "normal")` and `tkconfigure(e$gpaStatusLabel, text = "")`.
    - 3dDigitize.geomorph.r contains the failure copy fragment `GPA failed \u2014 ` inside a `busyStop(e, ..., "error")` call within a tryCatch error handler.
    - The `geomorph::gpagen(` call arguments are unchanged versus pre-task (CON-01).
    - Behavior (manual UAT): Compute on a multi-specimen set paints the indeterminate bar + watch cursor + blue "Computing GPA alignment for {N} specimens…" before the freeze, then shows green "GPA complete — {N} specimens aligned." and re-enables Save/Plot.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && grep -nF 'Computing GPA alignment for ' R/3dDigitize.geomorph.r && grep -nF 'GPA complete \u2014 ' R/3dDigitize.geomorph.r && grep -nF 'GPA failed \u2014 ' R/3dDigitize.geomorph.r && grep -nF 'busyStart(e,' R/3dDigitize.geomorph.r</automated>
    <automated>R -q -e "devtools::load_all('integrated-guimorph-development_EOC/Project/GUImorphDevelopment')" 2>&1 | grep -iE 'error|could not' && exit 1 || exit 0</automated>
  </verify>
  <done>gpagen is wrapped in busyStart/busyStop with indeterminate feedback and GPA complete/failed status; Save/Plot re-enabled on success; gpagen args unchanged; package loads clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local desktop GUI. Compute gates and busy wrapping operate on geomorph results and program-generated strings; `conditionMessage(err)` is an R error message (internal), not network/untrusted input. No auth, no IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering/Injection | gpaStatusLabel + status text via `paste0` | accept | `paste0`/`conditionMessage` only; no `eval`/`parse`; copy is program-generated (RESEARCH §Security Domain). |
| T-12-03-RE | Denial of Service | double-click Compute → re-entrant gpagen | mitigate | `busyStart` uses `tcl("update","idletasks")` (Plan 12-01), processing redraws only — not user events — preventing a second compute during repaint (Pitfall 1). |

No `high` threats. No package installs; gpagen analysis logic unchanged (CON-01).
</threat_model>

<verification>
- `grep -c 'tkmessageBox' R/3dDigitize.geomorph.r` equals 1 (only rgl error dialog).
- `R -q -e "devtools::load_all('...')"` loads clean.
- Manual UAT: indeterminate bar + watch cursor paint before gpagen freeze; complete/failed status shows; Save/Plot toggled correctly.
</verification>

<success_criteria>
- 3 geomorph flow-control modals converted to inline label + disabled Save/Plot.
- gpagen wrapped with indeterminate busy feedback and complete/failed parity copy.
- gpagen arguments and result handling unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-in-gui-feedback-status/12-03-SUMMARY.md` when done.
</output>
