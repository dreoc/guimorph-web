---
phase: 05-full-digitizing-and-data-parity
plan: 06
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-dgt-cross-platform.R
autonomous: true
requirements: [DAT-01, DAT-02, CMP-01]
must_haves:
  truths:
    - "The browser Save path serializes the session record through the SAME saveToDgt block sequence and the SAME .dgt_write_matrix_block writer — no second serializer exists"
    - "Given identical in-memory arrays, the browser save and the canonical writer produce byte-identical .dgt files (raw md5), proving DAT-01 as one-writer-vs-itself at the R level"
    - "The DAT-02 bidirectional -rewrite byte gate stays a documented skip until the Windows re-save fixtures land; the reader accepts both dialects"
    - "The native tkogl2 oracle load check (.gmw_engine$ok) is re-asserted and skips cleanly when the engine is absent; the rtkogl.R load path is never touched (CMP-01)"
  artifacts:
    - "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r (.gmw_save_session_dgt session-serialize path reusing the writer)"
    - "extended tests/testthat/test-dgt-cross-platform.R (DAT-01 write-vs-write byte identity + CMP-01 skip-if-absent load re-assertion)"
  key_links:
    - "/save route (05-02) -> .gmw_save_session_dgt(token, file) -> same .dgt_write_matrix_block / .dgt_writeln calls as saveToDgt"
    - "DAT-01 proof: identical arrays -> .gmw_save_session_dgt == canonical writer bytes (.byte_signature)"
    - "CMP-01: isNamespaceLoaded('GUImorphWeb') -> .gmw_engine$ok logical scalar, else skip (retina idiom); rtkogl.R untouched"
---

<objective>
Route the browser "Save" through the identical deterministic `.dgt` writer (05-01) reading
the session model (05-02), and prove DAT-01 byte-identity as a one-writer-vs-itself R-level
test. Keep the DAT-02 bidirectional gate honest: the reader accepts GUImorph- and
GUImorphWeb-authored files, and the `-rewrite` byte gate stays a documented skip until the
external Windows re-save fixtures land. Re-assert CMP-01 (native oracle still loadable),
which is retired only AFTER this phase.

Purpose: DAT-01/DAT-02/CMP-01. Byte-identity is a one-writer comparison because R keeps the
`.dgt` writer (RESEARCH Pattern 6 / Assumption A1); the browser adds only a trigger that
serializes the same in-memory arrays through the same block sequence. No second serializer
(RESEARCH Anti-Patterns).
Output: `.gmw_save_session_dgt` in `3dDigitize.main.r` + the DAT-01 byte test + CMP-01 gate.

STATED LIMITATIONS (do not claim closed — RESEARCH Environment Availability / Assumption A6):
- The `-rewrite` fixtures require a Windows re-save; until then `test-dgt-cross-platform.R`'s
  bidirectional gate remains a `skip()`, not a pass (tracked in `dat-parity-gate-is-a-skip.md`).
- Under the pinned-CRLF writer (05-01), re-saving the LF `mac-authored-roundtrip.dgt` emits CRLF
  and will not byte-round-trip — the documented finding, not a regression.
- DAT-02's GUImorph macOS→Windows leg is an open UPSTREAM dependency; state the half-proven
  contract rather than claiming full bidirectionality.
- The live native-GUI vs browser dual-path DAT-01 run needs a Windows `tkogl2` host and is
  recorded as manual UAT; the R-level write-vs-write byte test is the automated gate.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-digitizing-and-data-parity/05-RESEARCH.md
@.planning/phases/05-full-digitizing-and-data-parity/05-PATTERNS.md
@.planning/todos/pending/dat-parity-gate-is-a-skip.md

# Working directory for all verify commands: the R package root
#   integrated-guimorph-development_EOC/Project/GUImorphDevelopment/
# Run R with --no-init-file to avoid the renv activate hang (STATE.md Open Items).
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-dgt-cross-platform.R
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-picking-transport.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DAT tests — write-vs-write byte identity + CMP-01 gate</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-dgt-cross-platform.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-dgt-cross-platform.R  (`.byte_signature` raw-md5 7-13; the `.dgt_write_matrix_block` canonical block sequence used in "dgt writer output matches parity fixture" 46-72; the DAT-03 `-rewrite` skip gate 75-91)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-picking-transport.R  (the CMP-01 skip-if-absent block 134-156 — `isNamespaceLoaded` guard + `.gmw_engine$ok` logical-scalar check)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r  (`saveToDgt` block order 1931-2026; the deterministic writer from 05-01)
  </read_first>
  <action>
    Add a DAT-01 `test_that("browser save is byte-identical to the canonical writer for identical arrays")`
    block: define one in-memory array set (curves as a 3-index row, landmarks, anchors, surface, template,
    id). Seed a session token via the 05-02 session API with those arrays, write file A with
    `.gmw_save_session_dgt(token, fileA)`, and write file B by calling the SAME canonical block sequence
    directly (`.dgt_write_matrix_block("Curve=", ...)`, `TemplateNumber=NULL`, per-specimen
    `LM3=`/`AC3=`/`ID=`/`Template=`/`Surface=`) over the identical arrays. Assert
    `expect_identical(.byte_signature(fileA), .byte_signature(fileB))` — one writer vs itself. Add a
    comment noting the live native-vs-browser dual run is manual UAT. Add a CMP-01
    `test_that("native oracle stays loadable or skips cleanly")` block copied from the
    test-picking-transport.R CMP-01 idiom (source-scan that `3dDigitize.main.r`'s save path does not
    assign the engine env; then the `isNamespaceLoaded('GUImorphWeb')`-guarded `.gmw_engine$ok` logical
    check that skips when the engine is absent). Leave the existing DAT-03 `-rewrite` skip gate as-is.
    The DAT-01 block fails until Task 2.
  </action>
  <acceptance_criteria>
    - `test-dgt-cross-platform.R` contains a DAT-01 write-vs-write `.byte_signature` equality block and a CMP-01 skip-if-absent block; the existing DAT-03 `-rewrite` gate is unchanged (still a documented skip).
    - The DAT-01 block fails pre-Task-2 (the session-save function does not exist): `rtk Rscript --no-init-file -e 'testthat::test_file("tests/testthat/test-dgt-cross-platform.R")'`
    - The CMP-01 block passes or skips (never errors) on the headless host.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --no-init-file -e 'r <- as.data.frame(testthat::test_file("tests/testthat/test-dgt-cross-platform.R", reporter="silent")); stopifnot(sum(r$failed) + sum(r$error) >= 1)'</automated>
  </verify>
  <done>DAT-01 byte-identity and CMP-01 gate are in the test; DAT-01 fails against the not-yet-built save path; DAT-03 stays a skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: .gmw_save_session_dgt — serialize the session through the canonical writer</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r  (`saveToDgt` full block order 1931-2026 — `Curve=`, `TemplateNumber=NULL`, per-specimen `LM3=`/`AC3=`/`ID=`/`Template=`/`Surface=`; the deterministic `.dgt_write_matrix_block`/`.dgt_writeln`/`.dgt_format_num` from 05-01)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R  (the `/save` forward-call branch added in 05-02 that invokes `.gmw_save_session_dgt(token, file)`)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-dgt-cross-platform.R
  </read_first>
  <behavior>
    - `.gmw_save_session_dgt(token, file)` reads the session record (curves, per-specimen land/anchor/id/template/surfaces) and emits the .dgt using the EXACT same block sequence and helper calls as `saveToDgt`, in the same order, via `.dgt_write_matrix_block`/`.dgt_writeln` — no new formatting or serializer.
    - For identical in-memory arrays it produces bytes identical to the canonical block sequence.
    - It never assigns or reads the native oracle engine env (CMP-01).
  </behavior>
  <action>
    Add `.gmw_save_session_dgt(token, file)` to `3dDigitize.main.r`. Read arrays from
    `.gmw_session[[token]]` (session curves; per-specimen land/anchor/id/template/surfaces) and walk them
    through the identical block sequence `saveToDgt` uses — `Curve=`, then `TemplateNumber=NULL`, then per
    specimen `LM3=`/`AC3=`/`ID=`/`Template=`/`Surface=` — calling the SAME `.dgt_write_matrix_block` and
    `.dgt_writeln` helpers (already deterministic from 05-01). Do NOT introduce any alternate number
    formatting, terminator, or block layout; if practical, factor the shared block-emission out of
    `saveToDgt` so both entry points call ONE routine (preferred, guarantees identity), otherwise mirror
    it call-for-call. Keep it `@noRd`; `dbg()` logging; `stop(call.=FALSE)` on hard failure. Do not touch
    the `rtkogl.R` engine-load path (CMP-01).
  </action>
  <acceptance_criteria>
    - Source assertion: `rtk grep -n "gmw_save_session_dgt" integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r` shows the new path, and it calls `.dgt_write_matrix_block` (no new formatter).
    - The DAT-01 write-vs-write `.byte_signature` equality block passes; the folsom3d format test and DAT-03 skip gate are unaffected.
    - The CMP-01 block still passes/skips cleanly; no reference to the engine env in the save path.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --no-init-file -e 'testthat::test_file("tests/testthat/test-dgt-cross-platform.R"); testthat::test_file("tests/testthat/test-dgt-determinism.R")'</automated>
    <human-check>On a Windows `tkogl2` host (unavailable in the headless sandbox): save the same session through the native GUI path and the browser path and confirm byte-identical `.dgt`; then produce the two `-rewrite` fixtures per `dat-parity-gate-is-a-skip.md` to un-skip the DAT-03 gate. Record in 05-VALIDATION.md Manual-Only.</human-check>
  </verify>
  <done>Browser Save serializes the session through the one canonical deterministic writer; DAT-01 byte-identity proven at the R level; DAT-02 -rewrite gate and the native dual-path run recorded as owed external/manual evidence; CMP-01 re-asserted.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-06)

New functions:
- `.gmw_save_session_dgt(token, file)` — internal `@noRd` session serializer reusing the canonical `saveToDgt` block sequence + deterministic `.dgt_write_matrix_block`/`.dgt_writeln` (`3dDigitize.main.r`)
- (Optional) an extracted shared block-emission routine both `saveToDgt` and `.gmw_save_session_dgt` call (`3dDigitize.main.r`)

Extended files:
- `tests/testthat/test-dgt-cross-platform.R` (DAT-01 write-vs-write byte identity + CMP-01 skip-if-absent load re-assertion)

Owed external evidence (NOT produced here; documented):
- `tests/fixtures/parity/windows-authored-roundtrip-rewrite.dgt`, `mac-authored-roundtrip-rewrite.dgt` (Windows re-save)
- Native `tkogl2` Windows host dual-path DAT-01 run + CMP-01 runtime `.gmw_engine$ok` confirmation

No new routes (the `/save` branch is registered by 05-02).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /save route → filesystem | The route triggers a save to an R-side-chosen path; the request carries no path |
| session arrays → .dgt bytes | Serialization must not diverge from the canonical writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-15 | Tampering | second .dgt serializer divergence | high | mitigate | Browser save reuses the identical `.dgt_write_matrix_block` block sequence (ideally one shared routine); DAT-01 byte test proves no divergence. |
| T-5-16 | Tampering | save path injection | high | mitigate | `/save` carries no path; the save target is chosen R-side (existing file-dialog/known dir), never from the request body. |
| T-5-17 | Tampering | CMP-01 oracle load path | high | mitigate | save path never reads/assigns the native engine env; source-scan + skip-if-absent load check assert (rtkogl.R untouched). |
| T-5-SC | Tampering | package installs | low | accept | No installs this phase; nothing to gate. |
</threat_model>

<verification>
- `test-dgt-cross-platform.R` green for DAT-01 write-vs-write byte identity; DAT-03 `-rewrite` gate remains a documented skip.
- `test-dgt-determinism.R` still green (05-01 writer unregressed).
- CMP-01 block passes/skips cleanly; no engine-env reference in the save path.
- Limitations (`-rewrite` fixtures, mac LF round-trip, macOS→Windows upstream leg, native dual-path run) stated, not claimed closed.
</verification>

<success_criteria>
A `.dgt` written through the browser path is byte-identical to one written through the canonical
deterministic writer from the same in-memory session (DAT-01, one-writer-vs-itself, proven at the R
level); the reader accepts both authored dialects and the bidirectional byte gate is honestly gated on
the owed Windows `-rewrite` fixtures (DAT-02); the native oracle remains loadable/skip-safe (CMP-01).
</success_criteria>

<output>
Create `.planning/phases/05-full-digitizing-and-data-parity/05-06-SUMMARY.md` when done.
</output>
