---
phase: 05-full-digitizing-and-data-parity
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-flatten.R
autonomous: true
requirements: [DGT-02]
must_haves:
  truths:
    - "Surface semilandmarks are computed in R by the existing downSample TPS warp — the browser never computes geometry, only triggers and displays"
    - "The /downsample route runs the warp headlessly (no Tk, no C add()), stores the s x 3 surface array in the session record, and returns it flattened row-major"
    - "The display flatten is as.vector(t(surfaces[,,id])) — the transpose is covered by a regression test that fails on the column-major mistake"
  artifacts:
    - "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r (.gmw_downsample_session headless entry)"
    - "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-flatten.R"
  key_links:
    - "/downsample route (05-02) -> .gmw_downsample_session(token) -> reuse downSample TPS warp -> store session surfaces -> return .gmw_flat row-major"
    - "row-major flatten == as.vector(t(surfaces[,,id])), NOT as.vector(surfaces[,,id])"
---

<objective>
Add the headless surface-semilandmark entry the `/downsample` route (05-02) calls, reusing
the existing validated `downSample` TPS warp verbatim, and cover the mandatory
`as.vector(t(surfaces[,,id]))` transpose with a regression test — the ROADMAP note demands
a test, not care. The browser does not compute surfaces; R computes them and returns a
row-major point cloud for display (05-03).

Purpose: DGT-02 surface display. Omitting the transpose column-orders the flat vector and
silently scrambles point order, breaking GPA correspondence (RESEARCH Pitfall 1). The C
`add("downsample", ...)` path must NOT be used from the browser route — that re-couples
acquisition to `tkogl2`, which Phase 6 deletes (RESEARCH Anti-Patterns).
Output: `.gmw_downsample_session` in `3dDigitize.surface.r` + the flatten regression test.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-digitizing-and-data-parity/05-RESEARCH.md
@.planning/phases/05-full-digitizing-and-data-parity/05-PATTERNS.md

# Working directory for all verify commands: the R package root
#   integrated-guimorph-development_EOC/Project/GUImorphDevelopment/
# Run R with --no-init-file to avoid the renv activate hang (STATE.md Open Items).
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/view3d.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 scaffold — surface flatten transpose regression</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-flatten.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/view3d.R  (`.gmw_flat` 23-28 — already `as.vector(t(m))`, the reusable row-major flatten)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r  (the in-place transpose precedent `vertToDownsample <- as.vector(t(selected.out))` at 548)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-picking-transport.R  (header/skip idiom)
  </read_first>
  <behavior>
    - For a known p x 3 matrix, the surface flatten yields row-major `[x1,y1,z1,x2,y2,z2,...]` = `as.vector(t(m))`.
    - The column-major mistake `as.vector(m)` produces a DIFFERENT ordering and must make the test fail — the test pins the exact expected vector so a dropped transpose is caught.
  </behavior>
  <action>
    Create `test-surface-flatten.R` with the standard `pkg_root`/`skip_if_no_pkg_source()` header and
    `source()` the flatten source (`view3d.R` for `.gmw_flat`, and `3dDigitize.surface.r` for
    `.gmw_downsample_session` once it exists). Assert that flattening a fixed matrix such as
    `matrix(c(11,12,13, 21,22,23, 31,32,33), ncol=3, byrow=TRUE)` produces the row-major sequence
    `11,12,13,21,22,23,31,32,33` (i.e. `as.vector(t(m))`), and explicitly assert it does NOT equal the
    column-major `as.vector(m)` ordering. Add a second block that, given a small synthetic session
    surface array, `.gmw_downsample_session` returns the row-major flatten of `surfaces[,,id]` with point
    order preserved. Leave the second block red until Task 2 (the entry does not exist yet).
  </action>
  <acceptance_criteria>
    - File `tests/testthat/test-surface-flatten.R` exists and pins the exact `as.vector(t(m))` expected vector plus a negative assertion against `as.vector(m)`.
    - The flatten (`.gmw_flat`) block passes immediately (that helper already transposes); the `.gmw_downsample_session` block fails pre-Task-2.
    - `rtk Rscript --no-init-file -e 'testthat::test_file("tests/testthat/test-surface-flatten.R")'` shows a mix of pass + at least one failure.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --no-init-file -e 'r <- as.data.frame(testthat::test_file("tests/testthat/test-surface-flatten.R", reporter="silent")); stopifnot(sum(r$passed) >= 1, sum(r$failed) + sum(r$error) >= 1)'</automated>
  </verify>
  <done>The transpose regression test exists, pins row-major order, and fails on the not-yet-built downsample entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headless .gmw_downsample_session entry (reuse downSample TPS warp)</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r  (`downSample` 400-567 — template build, `tps2d3d` TPS warp, nearest-neighbour onto specimen producing `sliders`; the transpose + `add("downsample", vertToDownsample, ...)` + `activeDataList[[..]][[8]] <- sliders` at 548-559)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R  (the `/downsample` forward-call branch added in 05-02 that invokes `.gmw_downsample_session(token)`)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-flatten.R
  </read_first>
  <behavior>
    - `.gmw_downsample_session(token)` reads the current specimen's landmarks (+ anchors) and template from the session record, runs the SAME TPS warp / nearest-neighbour logic `downSample` uses (via the existing `gm_utils` helpers `tps2d3d`/`rotate.mat`/`cSize`), and produces the s x 3 `sliders` surface array.
    - It stores that array in the session record's `surfaces` slot for the current specimen (no `activeDataList`/Tk/C `add()` mutation from the browser path).
    - It returns the row-major flatten `as.vector(t(surfaces))` (via `.gmw_flat`) for the browser cloud.
  </behavior>
  <action>
    Add an internal `.gmw_downsample_session(token)` to `3dDigitize.surface.r`. Factor out or directly
    reuse the pure-geometry core of `downSample` (400-567) — the TPS template warp and nearest-neighbour
    onto the specimen — WITHOUT the Tk status-bar calls and WITHOUT `add("downsample", ...)` (that is the
    C-engine coupling to avoid). Source the landmarks/anchors/template from `.gmw_session[[token]]`'s
    current specimen record instead of `activeDataList`/`getLandmark`. Store the resulting `sliders`
    (s x 3) into the session record's `surfaces` slot. Return the browser payload as `.gmw_flat(surfaces)`
    (which already applies the mandatory `as.vector(t(.))`), keeping the exact row-major order the display
    expects. Use `dbg()` for logging; `stop(call.=FALSE)` for hard failures. Do not alter the numerical
    TPS pipeline or its `gm_utils` calls.
  </action>
  <acceptance_criteria>
    - Source assertion: `rtk grep -n "gmw_downsample_session" integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r` shows the new entry.
    - Source assertion: the browser entry does NOT call the C `add("downsample"` — `rtk grep -n "add(\"downsample\"" integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r` shows only the legacy `downSample` site (line ~554), not `.gmw_downsample_session`.
    - Both blocks of `test-surface-flatten.R` pass, including the `.gmw_downsample_session` row-major order block.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --no-init-file -e 'testthat::test_file("tests/testthat/test-surface-flatten.R")'</automated>
  </verify>
  <done>Headless downsample entry reuses the validated TPS warp, stores surfaces in the session, and returns a correctly-transposed cloud; regression test green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-04)

New functions:
- `.gmw_downsample_session(token)` — internal `@noRd` headless surface builder; reuses the `downSample` TPS warp, stores `surfaces` in the session record, returns `.gmw_flat` row-major cloud (`3dDigitize.surface.r`)

Session record slot populated: `specimens[[current]]$surfaces` (s x 3 numeric)

New files:
- `tests/testthat/test-surface-flatten.R`

No new routes (the `/downsample` branch is registered by 05-02); no new template suffixes.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /downsample route → R geometry | The route body is empty/parameter-bounded; the compute reads only server-owned session state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-11 | Tampering | surface point order (dropped transpose) | medium | mitigate | `as.vector(t(...))` enforced and pinned by `test-surface-flatten.R`; a column-major regression fails the build. |
| T-5-12 | Tampering | C-engine re-coupling via add() | medium | mitigate | headless entry never calls `add("downsample", ...)`; reads session state only (keeps Phase-6 retirement clean). |
| T-5-SC | Tampering | package installs | low | accept | No installs this phase; nothing to gate. |
</threat_model>

<verification>
- `test-surface-flatten.R` fully green (row-major order pinned; column-major rejected).
- `.gmw_downsample_session` reuses `downSample`'s TPS warp; no browser-path `add("downsample")`.
</verification>

<success_criteria>
The browser can trigger surface semilandmark computation; R runs the validated TPS warp headlessly,
owns the result in the session, and returns a correctly-transposed point cloud for display — with the
transpose guaranteed by a regression test.
</success_criteria>

<output>
Create `.planning/phases/05-full-digitizing-and-data-parity/05-04-SUMMARY.md` when done.
</output>
