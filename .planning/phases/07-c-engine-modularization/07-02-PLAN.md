---
phase: 07-c-engine-modularization
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
  - BUILD.md
  - .planning/smoke-test-findings.md
autonomous: false
requirements:
  - CENG-01
user_setup: []

must_haves:
  truths:
    - "HWND/WGL setup and setWindow handler live in tcl_window.c"
    - "dc, width, height defined in exactly one translation unit"
    - "setWindow handler still calls onDisplay() at exit; viewer renders after window resize"
    - "CMake builds with tcl_dispatch.c + tcl_window.c + remaining god file"
    - "Per-plan smoke: PLY load and double-click landmark still work"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c"
      provides: "setWindowId WGL setup, setWindow Tcl handler"
      min_lines: 80
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h"
      provides: "Window module API — setWindowId, setWindow, dc/width/height extern"
    - path: "BUILD.md"
      provides: "Documents tcl_window.c in Phase 7 layout"
      contains: "tcl_window"
  key_links:
    - from: "tcl_window.c setWindow handler"
      to: "tcl_dispatch.c onDisplay"
      via: "#include tcl_dispatch.h + onDisplay() call at handler exit"
      pattern: "onDisplay\\(\\)"
    - from: "setWindowId"
      to: "ogl_init"
      via: "wglMakeCurrent then ogl_init()"
      pattern: "ogl_init\\(\\)"
    - from: "def_ZARF_9.h"
      to: "extern HDC dc"
      via: "existing declaration — avoid duplicate if kept in def_ZARF_9.h"
      pattern: "extern HDC dc"
---

<objective>
Extract HWND/WGL window setup (`setWindowId`) and the `setWindow` Tcl handler from the god file into `tcl_window.c`/`tcl_window.h` per D-09 step 2. Move `dc`, `width`, `height` definitions to the window module. Incremental CMake (D-11), BUILD.md update (D-16), per-plan smoke (D-05).

Purpose: CENG-01 — isolate WGL context lifecycle from dispatch handlers; reduces god file before state/log/init extraction in 07-03.
Output: `tcl_window.c`/`tcl_window.h`, further trimmed god file, CMake + BUILD.md updates, smoke findings entry.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-c-engine-modularization/07-CONTEXT.md
@.planning/phases/07-c-engine-modularization/07-RESEARCH.md
@.planning/phases/07-c-engine-modularization/07-PATTERNS.md
@.planning/phases/07-c-engine-modularization/07-01-SUMMARY.md
@integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
@integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c
@integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
@integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
@BUILD.md

<interfaces>
Symbols to MOVE from god file to tcl_window.c:
- setWindowId (~582–610) — WGL pixel format, wglCreateContext, ogl_init()
- setWindow handler (~2998–3154) — branches: attr "id" (HWND) and attr "size" (width/height)
- Definitions: HDC dc, int width, int height (currently ~355–385 region)

setWindow handler dependencies (extern, not moved):
- Wrapper_Get*FromObj — in tcl_dispatch.c; include tcl_dispatch.h
- models, context, model_index, model_amount — still in god file until 07-03; extern via def_ZARF_9.h or tcl_state.h stub
- set_dot_slice_amount, set_anchors_slice_amount — dot module functions
- simpleLog_* — still in god file until 07-03; extern declarations
- onDisplay() — tcl_dispatch.c; include tcl_dispatch.h
- TCL_RESULT_* macros — in tcl_dispatch.c; setWindow size branch uses TCL_RESULT2 under CODE_FOR_LIBRARY

Note: def_ZARF_9.h line 116 already has `extern HDC dc`. Define dc in tcl_window.c; do NOT duplicate extern in tcl_window.h if def_ZARF_9.h remains canonical (D-14 minimal extension).

CMake after 07-02:
add_library(tkogl2 SHARED ${SRC}/tcl_dispatch.c ${SRC}/tcl_window.c ${SRC}/tcl_if_ZARF_9.c ...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract window module from god file</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-PATTERNS.md (tcl_window.c, tcl_window.h sections)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c (setWindowId ~582, setWindow ~2998)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h
    - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h (extern HDC dc)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_ZARF9.c (ogl_init pattern)
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c</files>
  <action>
    Per D-09 step 2, D-12, D-13, D-14, D-15 — move window code verbatim:

    1. Create `tcl_window.h` with guard, `#include "def_ZARF_9.h"`, declarations for `setWindowId(HWND)`, `setWindow(...)` handler signature, and `extern int width; extern int height;` (omit duplicate `extern HDC dc` if def_ZARF_9.h already declares it).

    2. Create `tcl_window.c` with standard pragmas and includes: `def_ZARF_9.h`, `RunTime_Defines_ZARF_9.h`, `tcl_window.h`, `tcl_dispatch.h` (for onDisplay + Wrapper_Get* + TCL_RESULT macros used in size branch). Move setWindowId and setWindow handler bodies exactly — preserve `#ifdef CODE_FOR_LIBRARY` / `#ifdef STAND_ALONE_TOOL` guards.

    3. Move definitions of `dc`, `width`, `height` from god file to tcl_window.c. Ensure no second definition remains in god file or tcl_dispatch.c.

    4. Remove moved code from `tcl_if_ZARF_9.c`. Add `#include "tcl_window.h"` if god file or Tkogl2_Init still references setWindow symbol (init registers setWindow — registration stays in god file until 07-03 but handler body is in tcl_window.c).

    5. Verify onDisplay still calls `SwapBuffers(dc)` — dc must link from tcl_window.c definition visible to dispatch via def_ZARF_9.h extern.

    6. Do NOT modify dot/curve/ogl module logic (D-12). Do NOT change WGL pixel format values or glOrtho parameters.

    7. Grep for duplicate symbols before build: `grep -E '^(HDC dc|int width|int height|int setWindowId)' integrated-guimorph-development_EOC/Project/tkogl2/src/*.c` — each symbol defined once.
  </action>
  <acceptance_criteria>
    - setWindowId and setWindow exist only in tcl_window.c
    - dc, width, height each have exactly one definition (in tcl_window.c)
    - God file no longer contains setWindowId or setWindow handler bodies
    - tcl_window.h declares setWindow for Tkogl2_Init registration in 07-03
  </acceptance_criteria>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c && test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h && echo WINDOW_OK</automated>
    <automated>grep -l 'setWindowId' integrated-guimorph-development_EOC/Project/tkogl2/src/*.c | wc -l | awk '{exit ($1 == 1) ? 0 : 1}'</automated>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c | grep -c 'wglCreateContext' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c | grep -c 'setWindowId' | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>Window module source files created; god file trimmed of WGL/window handler code</done>
</task>

<task type="auto">
  <name>Task 2: Wire CMake, build, deploy, and update BUILD.md</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
    - BUILD.md
    - .planning/phases/07-c-engine-modularization/07-PATTERNS.md (CMake incremental 07-02)
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt, BUILD.md</files>
  <action>
    Per D-11, D-16:

    1. Add `${SRC}/tcl_window.c` to `add_library(tkogl2 SHARED ...)` after `tcl_dispatch.c`, before `tcl_if_ZARF_9.c`.

    2. Run `cmake --build build -j` from tkogl2 project directory. Resolve any undefined reference to setWindow, dc, or onDisplay by fixing includes/extern — not by duplicating code.

    3. Verify export gate: `objdump -p build/tkogl2.dll | grep Tkogl2_Init`.

    4. Deploy DLL to `GUImorphDevelopment/inst/libs/x64/tkogl2.dll` via `scripts/deploy-dll.ps1` or BUILD.md manual copy (creates `.bak` of previous deploy).

    5. Update BUILD.md Phase 7 layout table: add row for `tcl_window.c` — "setWindowId, HWND/WGL, window dimensions, setWindow handler". Note god file still holds state/log/init.
  </action>
  <acceptance_criteria>
    - CMakeLists.txt lists tcl_window.c
    - Build completes with zero errors
    - Tkogl2_Init export present
    - BUILD.md documents tcl_window.c
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'tcl_window.c' integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && cmake --build build -j 2>&1 | tail -5</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && objdump -p build/tkogl2.dll 2>/dev/null | grep -c Tkogl2_Init | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -c 'tcl_window' BUILD.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Incremental build green; DLL deployed; BUILD.md updated for window module</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Per-plan smoke UAT — window extraction (D-05, D-07)</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-CONTEXT.md (D-05)
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md (double-click UX)
    - .planning/smoke-test-findings.md
  </read_first>
  <files>.planning/smoke-test-findings.md</files>
  <what-built>07-02 window extraction — `tcl_window.c`/`tcl_window.h`, deployed DLL with WGL in separate module</what-built>
  <how-to-verify>
    1. Windows R: `devtools::load_all(".")` — tkogl2 loads without error
    2. `GUImorph()` — window opens (exercises setWindow id branch → setWindowId → WGL init)
    3. Load `zips/Folsom 3D models/C13.1.ply` — mesh renders (confirms WGL context + onDisplay path)
    4. Resize GUImorph window if possible — viewer should redraw without crash (setWindow size branch)
    5. **Double-click** place ≥1 landmark on Digitize tab
    6. Compare against 07-01 smoke baseline if any visual regression
  </how-to-verify>
  <action>
    Append "## Phase 7 — Window Extraction (07-02)" section to `.planning/smoke-test-findings.md` per D-07: date, steps table with pass/fail, notes on WGL/resize behavior. Record in 07-02-SUMMARY.md.
  </action>
  <acceptance_criteria>
    - GUI opens with working 3D viewer after WGL module split
    - C13.1.ply loads and landmark double-click placement works
    - smoke-test-findings.md contains 07-02 section
  </acceptance_criteria>
  <verify>
    <automated>grep -c '07-02\|Window Extraction' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <resume-signal>Type "approved" with pass/fail, or describe WGL/viewer regression</resume-signal>
  <done>07-02 per-plan smoke documented; window module validated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tcl setWindow → HWND | R passes widget window ID; cast to HWND for WGL binding |
| WGL context → OpenGL draw | dc handle shared across dispatch onDisplay and window module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | setWindow HWND argument | accept | Existing Wrapper_GetIntFromObj cast preserved verbatim — no new validation in Phase 7 |
| T-07-06 | Denial of Service | WGL context creation failure | accept | Legacy behavior unchanged — failure modes same as pre-split |
| T-07-07 | Tampering | Shared dc handle | mitigate | Single definition in tcl_window.c; extern via def_ZARF_9.h — prevent duplicate dc definitions |
| T-07-08 | Spoofing | DLL swap during deploy | mitigate | deploy-dll.ps1 backup or manual .bak before copy per BUILD.md |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A |

</threat_model>

<verification>
- tcl_window.c compiles alongside tcl_dispatch.c and god file
- No duplicate dc/width/height definitions
- Tkogl2_Init export unchanged
- Manual smoke: GUImorph + PLY + double-click landmark + optional resize
</verification>

<success_criteria>
- Window/WGL code isolated in tcl_window.c per architectural map
- Incremental CMake and BUILD.md updated (D-11, D-16)
- Per-plan smoke passes (D-05, D-07)
- No dot/curve/ogl logic changes (D-12)
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Window module | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c` | setWindowId, setWindow handler, dc/width/height |
| Window header | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h` | Window module public API |
| Trimmed god file | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c` | State, log, init only |
| CMake update | `integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt` | Adds tcl_window.c |
| Build docs | `BUILD.md` | Phase 7 layout includes tcl_window.c |
| UAT log | `.planning/smoke-test-findings.md` | Phase 7 / 07-02 smoke section |

<output>
Create `.planning/phases/07-c-engine-modularization/07-02-SUMMARY.md` when done
</output>
