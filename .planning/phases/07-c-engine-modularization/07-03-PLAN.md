---
phase: 07-c-engine-modularization
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.h
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
  - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
  - BUILD.md
  - .planning/smoke-test-findings.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - CENG-01
user_setup: []

must_haves:
  truths:
    - "All GBL_* and runtime globals live in tcl_state.c with extern declarations for cross-TU consumers"
    - "Logging helpers live in tcl_log.c separate from state per D-02"
    - "tcl_init.c contains only Tkogl2_Init and Tcl_CreateObjCommand registration per D-04"
    - "tcl_if_ZARF_9.c removed from CMake; god file deleted or emptied"
    - "Full digitize round-trip works: landmarks + curve + save .dgt + openDgt reload"
    - "CENG-01 marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c"
      provides: "Tkogl2_Init DLLEXPORT + 8 command registrations"
      contains: "Tkogl2_Init"
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.c"
      provides: "GBL_* globals, initialize_state, resetContext, buffer"
      min_lines: 150
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c"
      provides: "simpleLog_* and TclIf_LogCommands"
      min_lines: 100
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt"
      provides: "Final five-module source list without tcl_if_ZARF_9.c"
      must_not_contain: "tcl_if_ZARF_9.c"
    - path: "BUILD.md"
      provides: "Complete Phase 7 C source layout table"
  key_links:
    - from: "tcl_init.c"
      to: "tcl_dispatch.h + tcl_window.h handlers"
      via: "Tcl_CreateObjCommand registrations"
      pattern: "Tcl_CreateObjCommand"
    - from: "ogl_model_ply_ZARF_9.c"
      to: "tcl_state.c globals"
      via: "extern pointerTO_GBL_LANDMARK_SET unchanged"
      pattern: "pointerTO_GBL_LANDMARK_SET"
    - from: "tcl_dispatch.c"
      to: "tcl_log.c"
      via: "simpleLog / TclIf_LogCommands calls"
      pattern: "simpleLog\\("
---

<objective>
Complete CENG-01: extract state/globals to `tcl_state.c`, logging to `tcl_log.c`, thin entry to `tcl_init.c` (D-02, D-03, D-04, D-10); remove `tcl_if_ZARF_9.c` from CMake. Run final per-plan smoke plus full digitize round-trip (D-06) and document in smoke-test-findings.md (D-07).

Purpose: Finish god-file elimination with stable DLL export surface and validated digitize workflow — no behavior change vs Phase 4 baseline.
Output: Five-module layout, deleted god file from build, complete BUILD.md layout, CENG-01 closed, final UAT log.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-c-engine-modularization/07-CONTEXT.md
@.planning/phases/07-c-engine-modularization/07-RESEARCH.md
@.planning/phases/07-c-engine-modularization/07-PATTERNS.md
@.planning/phases/07-c-engine-modularization/07-02-SUMMARY.md
@.planning/phases/04-digitize-workflow/04-CONTEXT.md
@.planning/phases/05-analysis-round-trip/05-CONTEXT.md
@.planning/smoke-test-findings.md
@integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
@integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c
@integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
@BUILD.md

<interfaces>
Symbols to MOVE to tcl_state.c (~31–106, 355–385, 390–580, clear_model/resetModel if in god file):
- GBL_PTR_MODEL_1..5, GBL_PTR_CONTEXT_1..5, GBL_PTR_CURVE_1..6
- GBL_LANDMARK_SET, GBL_CURVE_SET, pointerTO_GBL_* , scale factors
- models, context, deltas, model_index, model_amount, showModel
- defaultDotColor, defaultAnchorColor, radii, downSampleOffset*
- buffer[1024], messageBuffer[128]
- ALLOCATE_WRAPPER, FREE_WRAPPER
- initialize_state, resetContext, clear_deltas, clear_GBL_* helpers

Symbols to MOVE to tcl_log.c (~4176–5223):
- simpleLog_Open/Close/BlankLine, simpleLog, simpleLog_Obj
- simpleLogWrite*, simpleLog*Information
- TclIf_LogCommands, commandStream_*
- Module-private: static FILE* fp, cmmdFileP

Symbols to MOVE to tcl_init.c (~4146–4172):
- Tkogl2_Init with DLLEXPORT under CODE_FOR_LIBRARY
- Register exactly 8 commands: add, show, setWindow, setSpecimen, setDownSample, setDot, del, loadDgt
- Do NOT register set or directive

Final CMake source list (07-PATTERNS.md):
tcl_init.c, tcl_dispatch.c, tcl_window.c, tcl_state.c, tcl_log.c, ogl_*.c, dot, curve, Statistics, tcl_stub_bootstrap.c
NO tcl_if_ZARF_9.c

Cross-TU consumers to verify still link (extern unchanged):
- ogl_model_ply_ZARF_9.c: pointerTO_GBL_LANDMARK_SET, model_index, GBL_*_SET_*
- curve_ZARF_9.c: GBL_INWORK_CURVE_SCALEFACTOR_*
- ogl_model_ZARF_9.c: GBL_INWORK_LABEL_SCALEFACTOR_*

Final smoke digitize workflow (D-06, Phase 4 baseline):
- Specimens: C13.1.ply + second Folsom PLY (e.g. C8.1.ply) OR reload test_fresh.dgt
- 3 landmarks per specimen (double-click), 1 curve on specimen 1 (3-landmark bind)
- Save multi-specimen .dgt, same-session openDgt reload
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract state, logging, and init modules; remove god file from build</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-PATTERNS.md (tcl_state, tcl_log, tcl_init sections)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
    - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c (extern consumer pattern)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/curve_ZARF_9.c
    - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.c, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.h, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c, integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt, integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h</files>
  <action>
    Per D-02, D-03, D-04, D-10, D-11, D-12, D-14, D-15:

    1. Create `tcl_state.h` / `tcl_state.c` — move all global definitions listed in context interfaces. Single definition per symbol. Export extern declarations for symbols consumed by ogl_model_ply_ZARF_9.c, curve_ZARF_9.c, ogl_model_ZARF_9.c (follow 07-PATTERNS.md tcl_state.h sketch). Move ALLOCATE_WRAPPER/FREE_WRAPPER with Tcl_Alloc/Tcl_Free under CODE_FOR_LIBRARY.

    2. Create `tcl_log.h` / `tcl_log.c` — move all simpleLog_* and commandStream_* functions. Keep static FILE* handles module-private in tcl_log.c. Include tcl_state.h only if logging reads globals for debug dumps — preserve existing access pattern.

    3. Create `tcl_init.c` — move Tkogl2_Init verbatim with DLLEXPORT. Includes: def_ZARF_9.h, RunTime_Defines_ZARF_9.h, tcl_dispatch.h, tcl_window.h. Register 8 commands only — names and order unchanged from original.

    4. Update tcl_dispatch.c and tcl_window.c includes: add `#include "tcl_state.h"` and `#include "tcl_log.h"` where they call simpleLog or touch globals — remove redundant extern blocks now covered by headers.

    5. Delete remaining content from `tcl_if_ZARF_9.c` OR delete the file entirely. Remove `${SRC}/tcl_if_ZARF_9.c` from CMakeLists.txt. Add tcl_init.c, tcl_state.c, tcl_log.c to add_library in order: tcl_init, tcl_dispatch, tcl_window, tcl_state, tcl_log, then unchanged ogl/dot/curve sources.

    6. Extend def_ZARF_9.h minimally if needed so unchanged modules (dot, curve, ogl) still compile without editing their logic — prefer adding extern to tcl_state.h and ensuring def_ZARF_9.h still exposes symbols those files include today.

    7. Grep gate before build: no remaining references requiring god file — `grep -r 'tcl_if_ZARF_9' integrated-guimorph-development_EOC/Project/tkogl2/` should only hit docs or git history, not CMake/sources.

    8. Build: `cmake --build build -j`. Fix linker errors for undefined GBL_* or simpleLog symbols by adjusting headers — never duplicate definitions.

    9. Export verify: `objdump -p build/tkogl2.dll | grep Tkogl2_Init`.

    10. Deploy DLL to inst/libs/x64/.

    Do NOT: remove debug printf/if(0)/MAKE_INERT (Phase 9), unify dot/anchor (Phase 8), static-ify cross-module helpers (D-15).
  </action>
  <acceptance_criteria>
    - tcl_init.c, tcl_state.c, tcl_log.c and matching headers exist
    - CMakeLists.txt has five tcl_*.c modules and NO tcl_if_ZARF_9.c
    - cmake --build succeeds; Tkogl2_Init exported
    - ogl_model_ply_ZARF_9.c extern consumers still resolve at link time
  </acceptance_criteria>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c && test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.c && test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c && echo FINAL_MODULES_OK</automated>
    <automated>grep -c 'tcl_if_ZARF_9.c' integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
    <automated>grep -c 'tcl_init.c' integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && cmake --build build -j 2>&1 | tail -5</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && objdump -p build/tkogl2.dll 2>/dev/null | grep -c Tkogl2_Init | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c | grep -c 'Tcl_CreateObjCommand' | awk '{exit ($1 >= 8) ? 0 : 1}'</automated>
  </verify>
  <done>God file eliminated from build; five-module layout compiles and exports Tkogl2_Init</done>
</task>

<task type="auto">
  <name>Task 2: Finalize BUILD.md layout and mark CENG-01 complete</name>
  <read_first>
    - BUILD.md
    - .planning/REQUIREMENTS.md (CENG-01 row)
    - .planning/phases/07-c-engine-modularization/07-PATTERNS.md (BUILD.md pattern)
  </read_first>
  <files>BUILD.md, .planning/REQUIREMENTS.md</files>
  <action>
    Per D-16:

    1. Complete BUILD.md "C source layout (Phase 7)" table with all five modules:

    | File | Responsibility |
    | tcl_init.c | Tkogl2_Init, Tcl command registration |
    | tcl_dispatch.c | Tcl handlers + draw pass + onDisplay |
    | tcl_window.c | setWindowId, HWND/WGL, window dimensions |
    | tcl_state.c | Globals, initialize_state, resetContext |
    | tcl_log.c | simpleLog_*, command stream logging |
    | dot_ZARF_9.c, curve_ZARF_9.c, ogl_*.c | Unchanged logic |

    2. Note god file `tcl_if_ZARF_9.c` removed from build (reference only if file kept in repo as empty stub — prefer delete or move note in BUILD.md).

    3. Update `.planning/REQUIREMENTS.md`: mark CENG-01 `[x]` with validation date 2026-06-19 (or execution date) and reference Phase 7 final smoke.

    4. Update traceability table row for CENG-01 to Complete.
  </action>
  <acceptance_criteria>
    - BUILD.md lists all five tcl_* modules with responsibilities
    - REQUIREMENTS.md shows CENG-01 complete
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'tcl_init.c' BUILD.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -c 'tcl_state.c' BUILD.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -c 'tcl_log.c' BUILD.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep '\[x\].*CENG-01\|CENG-01.*\[x\]' .planning/REQUIREMENTS.md | wc -l | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Documentation complete; CENG-01 requirement closed pending final UAT</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final smoke — per-plan UAT + full digitize round-trip (D-05, D-06, D-07)</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-CONTEXT.md (D-05, D-06, D-07)
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md (digitize workflow, test_fresh.dgt)
    - .planning/smoke-test-findings.md (Phase 4 section for round-trip baseline)
  </read_first>
  <files>.planning/smoke-test-findings.md</files>
  <what-built>07-03 complete modularization — five tcl_* modules, god file removed, deployed DLL</what-built>
  <how-to-verify>
    **Part A — Per-plan smoke (D-05):**
    1. Windows R: `devtools::load_all(".")` — success
    2. `GUImorph()` — window opens
    3. Load `zips/Folsom 3D models/C13.1.ply` — mesh visible
    4. Double-click ≥1 landmark

    **Part B — Full digitize round-trip (D-06)** — fresh session OR reload `test_fresh.dgt` then re-save:
    5. Load two specimens (C13.1.ply + C8.1.ply) OR Load DGT File → `test_fresh.dgt`
    6. Place 3 landmarks per specimen (double-click Digitize tab)
    7. Curve tab: bind 1 curve (3 landmark IDs) on specimen 1 — no R error
    8. Fit smoke: click Fit once — no crash
    9. Save to new local `.dgt` (do not commit)
    10. Same session: File → Load DGT File → reopen saved file — landmarks + curve restored

    **Rollback check (if failure):** swap `tkogl2.dll.pre-phase7.bak` via deploy script to confirm refactor vs baseline.
  </how-to-verify>
  <action>
    Append comprehensive "## Phase 7 — Modularization Complete (07-03)" section to `.planning/smoke-test-findings.md` per D-07:
    - Part A and Part B step tables with pass/fail
    - Module layout summary (five tcl_* files)
    - Any linker/symbol notes from extraction
    - Explicit CENG-01 validation statement
    Record in 07-03-SUMMARY.md.
  </action>
  <acceptance_criteria>
    - Part A per-plan smoke passes
    - Part B digitize round-trip passes (landmarks + curve + save + openDgt reload)
    - smoke-test-findings.md documents final Phase 7 UAT
    - No regression vs Phase 4 baseline behavior
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 7.*07-03\|Modularization Complete' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <resume-signal>Type "approved" with Part A/B pass/fail, or paste errors from save/reload path</resume-signal>
  <done>CENG-01 validated end-to-end; Phase 7 modularization complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loadDgt Tcl handler → specimen state | DGT file path from R; C restores landmarks/curves in-process |
| Global state → ogl render | GBL_* pointers read by ogl_model_ply during draw |
| DLL export surface | Only Tkogl2_Init visible to R Tcl loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | loadDgt / add shape routing | mitigate | Preserve handler bodies verbatim in tcl_dispatch.c — no input validation changes |
| T-07-10 | Denial of Service | buffer[1024] shared across modules | accept | Move buffer with state module; do not resize — Phase 9 hardening |
| T-07-11 | Tampering | Global state initialization order | mitigate | Preserve initialize_state selector semantics and call order when moving to tcl_state.c |
| T-07-12 | Spoofing | DLL rollback comparison | mitigate | Use tkogl2.dll.pre-phase7.bak for side-by-side regression isolation (D-08) |
| T-07-13 | Information Disclosure | simpleLog file writes to ./DATA_LOG_FILES/ | accept | Existing debug logging behavior unchanged — not expanded in Phase 7 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A |

</threat_model>

<verification>
- Five-module CMake build without god file
- Tkogl2_Init export verified
- Cross-TU extern consumers (ogl_model_ply, curve) link
- Per-plan smoke + full digitize round-trip manual UAT
- CENG-01 marked complete in REQUIREMENTS.md
- BUILD.md complete layout table
</verification>

<success_criteria>
- CENG-01 satisfied: god file split into dispatch, window/WGL, state, log, init modules
- CMake build succeeds; Tkogl2_Init export unchanged
- GUI opens, loads PLY, digitize round-trip matches Phase 4 baseline
- All Phase 7 UAT documented in smoke-test-findings.md
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Init module | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_init.c` | Tkogl2_Init + 8 Tcl_CreateObjCommand registrations |
| State module | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.c` | GBL_* globals, initialize_state, resetContext, buffers |
| State header | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_state.h` | extern declarations for cross-TU consumers |
| Log module | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c` | simpleLog_*, TclIf_LogCommands |
| Log header | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h` | Logging API for dispatch/window modules |
| Final CMake | `integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt` | Five tcl_* sources; no tcl_if_ZARF_9.c |
| Removed god file | `tcl_if_ZARF_9.c` deleted or empty | No longer in build |
| Build docs | `BUILD.md` | Complete Phase 7 C source layout |
| Requirements | `.planning/REQUIREMENTS.md` | CENG-01 marked complete |
| UAT log | `.planning/smoke-test-findings.md` | Phase 7 final smoke + digitize round-trip |

<output>
Create `.planning/phases/07-c-engine-modularization/07-03-SUMMARY.md` when done
</output>
