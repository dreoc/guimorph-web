---
phase: 07-c-engine-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
  - BUILD.md
  - .planning/smoke-test-findings.md
autonomous: false
requirements:
  - CENG-01
user_setup: []

must_haves:
  truths:
    - "Pre-Phase-7 working DLL is backed up before any extraction (D-08)"
    - "Tcl command handlers and draw pass live in tcl_dispatch.c with no duplicate symbols at link time"
    - "CMake builds tkogl2.dll with tcl_dispatch.c alongside remaining god-file code"
    - "Tkogl2_Init export unchanged; GUImorph opens and places landmark via double-click after deploy"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c"
      provides: "Tcl handlers, draw pass, onDisplay, Wrapper_Get* helpers"
      min_lines: 200
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h"
      provides: "Handler and draw-pass declarations for tcl_init.c (07-03)"
      exports: ["add", "show", "onDisplay", "drawDots"]
    - path: "GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak"
      provides: "Rollback artifact for side-by-side comparison"
    - path: "BUILD.md"
      provides: "Phase 7 C layout documents tcl_dispatch.c"
      contains: "tcl_dispatch"
  key_links:
    - from: "tcl_dispatch.c handlers"
      to: "tcl_if_ZARF_9.c remaining symbols"
      via: "extern globals and setWindow/setWindowId until 07-02"
      pattern: "extern.*models|setWindowId"
    - from: "CMakeLists.txt"
      to: "tcl_dispatch.c + tcl_if_ZARF_9.c"
      via: "add_library source list"
      pattern: "tcl_dispatch\\.c"
    - from: "build/tkogl2.dll"
      to: "Tkogl2_Init"
      via: "still defined in god file until 07-03"
      pattern: "Tkogl2_Init"
---

<objective>
Extract Tcl command dispatch, draw-pass functions, and Tcl_Obj wrapper helpers from `tcl_if_ZARF_9.c` into `tcl_dispatch.c`/`tcl_dispatch.h` per D-01 and D-09 step 1. Backup the working DLL (D-08), add incremental CMake source (D-11), update BUILD.md (D-16), and run per-plan smoke UAT (D-05).

Purpose: CENG-01 — first structural split with zero behavior change; largest handler block moved first so subsequent extractions shrink the god file safely.
Output: `tcl_dispatch.c`/`tcl_dispatch.h`, trimmed god file, CMake + BUILD.md updates, DLL backup, smoke findings entry.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-c-engine-modularization/07-CONTEXT.md
@.planning/phases/07-c-engine-modularization/07-RESEARCH.md
@.planning/phases/07-c-engine-modularization/07-PATTERNS.md
@.planning/phases/07-c-engine-modularization/07-VALIDATION.md
@.planning/smoke-test-findings.md
@integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
@integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
@integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
@BUILD.md

<interfaces>
God-file regions to MOVE to tcl_dispatch.c (07-PATTERNS.md, 07-RESEARCH.md line map):
- Wrapper_Get*FromObj helpers (~114–206)
- TCL_CMD macro, TCL_RESULT_* macros (~215–337)
- getRealZ, drawDots/drawAnchors/drawCurves/drawGrid (~619–905)
- validateDot, specimen_del, getSpecimenCoordinate (~902–1066)
- Handlers: add (~1068–2730), show (~2731–2997), setSpecimen, setDownSample, setDot, del, loadDgt (~3155–4138)
- onDisplay (~5063–5133), addDot_NO_TCL, addAnchor_NO_TCL (~5275–5496)
- changeLandmarkDotColor, changeDotColorToSlider (~5503–5579)
- directive handler (unregistered — move but do NOT register in Tkogl2_Init)

Symbols to LEAVE in god file until later plans:
- Tkogl2_Init (~4146–4172) — stays until 07-03 (D-04, D-10)
- setWindowId, setWindow handler (~582–610, 2998–3154) — 07-02
- GBL_* globals, initialize_state, resetContext, buffer — 07-03
- simpleLog_* block — 07-03 (D-02)

CMake after 07-01 (incremental — both files compile):
add_library(tkogl2 SHARED ${SRC}/tcl_dispatch.c ${SRC}/tcl_if_ZARF_9.c ...)

Registered Tcl commands (must remain stable): add, show, setWindow, setSpecimen, setDownSample, setDot, del, loadDgt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — backup pre-Phase-7 DLL and verify baseline build</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-CONTEXT.md (D-08, Phase 6 baseline note)
    - BUILD.md §3 DLL deploy
    - integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt
  </read_first>
  <files>GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak</files>
  <action>
    Per D-08, preserve the current working deployed DLL before any C extraction:

    1. Confirm `GUImorphDevelopment/inst/libs/x64/tkogl2.dll` exists and is the MinGW build from Phase 1–5 baseline (not empty, size &gt; 100 KB).

    2. Copy it to `GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak` (do not overwrite if backup already exists from a prior attempt — verify checksum/size match instead).

    3. Run baseline compile gate from tkogl2 project dir: `cmake -B build -S . -G "MinGW Makefiles"` (if build dir stale) then `cmake --build build -j`.

    4. Verify export: `objdump -p build/tkogl2.dll | grep Tkogl2_Init` — must show export entry.

    5. Grep god file still sole init host: `grep -c 'Tkogl2_Init' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c` equals 1.

    Do NOT modify C sources in this task. If build fails, stop and report — do not proceed to extraction on broken baseline.
  </action>
  <acceptance_criteria>
    - `tkogl2.dll.pre-phase7.bak` exists beside deployed DLL
    - `cmake --build build` succeeds with zero errors
    - `objdump -p build/tkogl2.dll | grep Tkogl2_Init` returns at least one match
  </acceptance_criteria>
  <verify>
    <automated>test -f GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak && echo BACKUP_OK</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && cmake --build build -j 2>&1 | tail -3</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && objdump -p build/tkogl2.dll 2>/dev/null | grep -c Tkogl2_Init | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Rollback artifact saved; baseline DLL builds and exports Tkogl2_Init</done>
</task>

<task type="auto">
  <name>Task 2: Extract dispatch module and wire incremental CMake</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-PATTERNS.md (tcl_dispatch.c, tcl_dispatch.h sections)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c
    - integrated-guimorph-development_EOC/Project/tkogl2/src/dot_ZARF_9.c (include order pattern)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h, integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c, integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt, BUILD.md</files>
  <action>
    Per D-01, D-11, D-12, D-13, D-14, D-15 — move dispatch code verbatim (no logic edits, no debug removal):

    1. Create `tcl_dispatch.h` with `#pragma once` guard, `#include "def_ZARF_9.h"`, and declarations for all eight registered handler functions plus draw pass (`drawDots`, `drawAnchors`, `drawCurves`, `drawGrid`, `onDisplay`) and `Wrapper_GetIntFromObj`/`Wrapper_GetDoubleFromObj`/`Wrapper_GetStringFromObj`. Follow 07-PATTERNS.md header sketch.

    2. Create `tcl_dispatch.c` with standard `#pragma warning` lines, includes: `def_ZARF_9.h`, `RunTime_Defines_ZARF_9.h`, `tcl_dispatch.h`. Move from god file: Wrapper helpers, TCL_CMD/TCL_RESULT macros, draw functions, onDisplay, all handlers listed in context interfaces EXCEPT setWindow. Move `directive` handler but do not register it.

    3. Remove moved code from `tcl_if_ZARF_9.c` — leave only: version strings, globals cluster, state init, setWindowId/setWindow, logging, Tkogl2_Init, and any symbols not yet assigned to 07-02/07-03. Add `#include "tcl_dispatch.h"` where handlers were referenced.

    4. For symbols dispatch still references but definitions remain in god file (models, context, buffer, simpleLog_*, setWindowId, dc, width, height): use `extern` declarations via existing `def_ZARF_9.h` entries or add temporary extern in dispatch file — do NOT duplicate definitions (pitfall: multiple definition linker errors).

    5. Update `CMakeLists.txt`: add `${SRC}/tcl_dispatch.c` to `add_library` BEFORE `${SRC}/tcl_if_ZARF_9.c`. Keep god file in list. Do NOT remove `tcl_if_ZARF_9.c` yet.

    6. Do NOT modify `dot_ZARF_9.c`, `curve_ZARF_9.c`, or `ogl_*.c` except allowed `#include` additions if link requires (prefer zero changes per D-12).

    7. Update BUILD.md per D-16: add or extend "C source layout (Phase 7)" section documenting `tcl_dispatch.c` responsibility; note god file still holds window/state/log/init until plans 07-02/07-03.

    8. Build: `cmake --build build -j`. Fix duplicate-symbol or undefined-reference errors by ensuring each moved function exists in exactly one `.c` file.

    9. Deploy built DLL: `powershell -ExecutionPolicy Bypass -File scripts/deploy-dll.ps1` from repo root, or manual copy per BUILD.md §3 if script unavailable.
  </action>
  <acceptance_criteria>
    - `tcl_dispatch.c` and `tcl_dispatch.h` exist with handler + draw-pass symbols
    - God file line count reduced substantially; no duplicate function definitions between files
    - `cmake --build build -j` completes with zero errors
    - `objdump -p build/tkogl2.dll | grep Tkogl2_Init` still passes
    - BUILD.md documents `tcl_dispatch.c` in Phase 7 layout table
  </acceptance_criteria>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c && test -f integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h && echo DISPATCH_OK</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && cmake --build build -j 2>&1 | tail -5</automated>
    <automated>grep -c 'tcl_dispatch.c' integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c | grep -c '^int add\|^TCL_CMD(add)' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2 && objdump -p build/tkogl2.dll 2>/dev/null | grep -c Tkogl2_Init | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Dispatch module extracted; incremental CMake build green; BUILD.md updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Per-plan smoke UAT — dispatch extraction (D-05, D-07)</name>
  <read_first>
    - .planning/phases/07-c-engine-modularization/07-CONTEXT.md (D-05, D-07)
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md (double-click landmark UX)
    - .planning/smoke-test-findings.md
  </read_first>
  <files>.planning/smoke-test-findings.md</files>
  <what-built>07-01 dispatch extraction — `tcl_dispatch.c`/`tcl_dispatch.h`, deployed `tkogl2.dll`</what-built>
  <how-to-verify>
    1. Windows R: `setwd("integrated-guimorph-development_EOC/Project/GUImorphDevelopment")`; `devtools::load_all(".")` — expect tkogl2 load confirmation, no fatal error
    2. `GUImorph()` — "3D GUImorph" window opens and stays responsive
    3. Load specimen: `zips/Folsom 3D models/C13.1.ply` — mesh visible in 3D viewer (not blank/black)
    4. Digitize tab: **double-click** canvas to place ≥1 landmark — dot visible after placement (single-click is pick/select only)
    5. Optional: rotate/zoom if exposed — no crash during navigation
    6. If regression suspected: compare behavior against `tkogl2.dll.pre-phase7.bak` (swap via deploy script backup path)
  </how-to-verify>
  <action>
    After human confirms pass/fail, append a "## Phase 7 — Dispatch Extraction (07-01)" section to `.planning/smoke-test-findings.md` per D-07 with: date, DLL path, build commit hash if available, table of steps (load_all, GUImorph, PLY load, double-click landmark) with pass/fail, and any technical notes (link errors fixed, symbol moves). Record same notes in plan SUMMARY when created.
  </action>
  <acceptance_criteria>
    - All four smoke steps pass (load_all, GUImorph, C13.1.ply render, double-click landmark)
    - smoke-test-findings.md contains new Phase 7 / 07-01 section with dated results
    - No blocker regression vs pre-Phase-7 backup behavior
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 7.*07-01\|07-01.*dispatch' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <resume-signal>Type "approved" with pass/fail per step, or describe regression (crash, blank viewer, landmark placement failure)</resume-signal>
  <done>07-01 per-plan smoke documented; dispatch extraction validated for CENG-01 step 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Tcl → C handlers | Untrusted shape strings and Tcl_Obj args cross into dispatch handlers |
| Build artifact → inst/libs/x64 | Deployed DLL must come from trusted local build, not third-party binary |
| File I/O (PLY via handlers) | Existing load paths unchanged — out of scope for hardening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Tcl handler input (`add`, `show`, …) | mitigate | Preserve existing `Wrapper_GetIntFromObj`/`Wrapper_GetDoubleFromObj`/`validateDot` verbatim when moving — no relaxation of parsing (ASVS V5) |
| T-07-02 | Spoofing | DLL deploy path | mitigate | Backup before swap (D-08); deploy only to `inst/libs/x64/` via BUILD.md script or documented manual copy |
| T-07-03 | Denial of Service | Shared `buffer[1024]` / `TCL_RESULT_*` macros | accept | Do not change buffer sizes or `sprintf` format strings in Phase 7 — defer hardening to Phase 9 |
| T-07-04 | Tampering | CMake source list | mitigate | Incremental add only; verify single definition per symbol before merge |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package manager installs in this phase |

</threat_model>

<verification>
- Wave 0 backup exists before extraction commits
- Link succeeds with `tcl_dispatch.c` + trimmed god file
- `Tkogl2_Init` export unchanged
- Manual Windows R smoke: PLY load + double-click landmark
- BUILD.md reflects new dispatch module
</verification>

<success_criteria>
- `tcl_dispatch.c`/`tcl_dispatch.h` contain all dispatch-region code per D-01
- No logic changes in dot/curve/ogl modules (D-12)
- Per-plan smoke UAT passes and is documented (D-05, D-07)
- Pre-Phase-7 DLL backup preserved (D-08)
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Dispatch module | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.c` | Tcl handlers, draw pass, onDisplay, Wrapper helpers |
| Dispatch header | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_dispatch.h` | Cross-module handler/draw declarations |
| Trimmed god file | `integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_if_ZARF_9.c` | Window, state, log, init only (until 07-03) |
| CMake update | `integrated-guimorph-development_EOC/Project/tkogl2/CMakeLists.txt` | Adds `tcl_dispatch.c` alongside god file |
| DLL backup | `GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase7.bak` | Rollback baseline per D-08 |
| Build docs | `BUILD.md` | Phase 7 layout section starts with `tcl_dispatch.c` |
| UAT log | `.planning/smoke-test-findings.md` | Phase 7 / 07-01 smoke section |

<output>
Create `.planning/phases/07-c-engine-modularization/07-01-SUMMARY.md` when done
</output>
