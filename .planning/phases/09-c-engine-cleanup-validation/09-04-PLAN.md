---
phase: 09-c-engine-cleanup-validation
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - .planning/smoke-test-findings.md
  - BUILD.md
autonomous: false
requirements: [CENG-05]
user_setup: []

must_haves:
  truths:
    - "Fixture A (test_fresh.dgt) full round-trip — load PLY -> landmarks -> curve -> save -> reload -> GPA Compute -> save CSV — matches the Phase 4-5 baseline (D-09)"
    - "Fixture B (test_dgt_anchors_curves.dgt) round-trip incl. anchor place/select/move/delete matches the Phase 8 baseline (D-10)"
    - "Surface=0 does not abort openDgt reload on either fixture (D-05)"
    - "Both fixture result tables are recorded in smoke-test-findings.md with date, freshness banner, and rollback DLL name (D-11/D-12)"
    - "BUILD.md reflects the final module-layout table and prunes obsolete tcl_if_ZARF_9.c/.vcxproj references; MSVC-only (D-14)"
  artifacts:
    - path: ".planning/smoke-test-findings.md"
      provides: "Phase 9 regression UAT results (both fixtures)"
      contains: "Phase 9"
    - path: "BUILD.md"
      provides: "Final module-layout table (D-14)"
      contains: "tcl_dispatch"
  key_links:
    - from: "deployed inst/libs/x64/tkogl2.dll"
      to: "rollback inst/libs/x64/tkogl2.dll.pre-phase9.bak"
      via: "documented Copy-Item rollback on UAT failure"
      pattern: "pre-phase9\\.bak"
---

<objective>
Satisfy CENG-05: prove the fully-cleaned engine has no regression versus the Phase 4-5 and
Phase 8 baselines via manual Windows MSVC GUI UAT (no automated Tk/OpenGL harness exists —
D-11/D-12). Run the full `load PLY -> digitize landmarks + curves + anchors -> save .dgt ->
reload -> GPA Compute -> save CSV` round-trip across BOTH fixtures, record both result tables
in `.planning/smoke-test-findings.md`, and update `BUILD.md` with the final module-layout
table (D-14), pruning obsolete `tcl_if_ZARF_9.c`/`.vcxproj` references and reaffirming
MSVC-only.

Purpose: This is the phase's acceptance gate — it exercises the engine after CENG-03 +
CENG-04 to confirm digitize, the D-05 reload path, marker dedup, and GPA analysis all behave
identically to baseline, then locks the documentation to the cleaned-up layout.
Output: Phase 9 UAT tables in `smoke-test-findings.md`; updated `BUILD.md`.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-c-engine-cleanup-validation/09-CONTEXT.md
@.planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md
@.planning/phases/09-c-engine-cleanup-validation/09-VALIDATION.md
@.planning/smoke-test-findings.md
@BUILD.md
</context>

<artifacts_this_phase_produces>
- New "Phase 9 — Cleanup & Validation" section appended to `.planning/smoke-test-findings.md`
  (two fixture result tables with date, DLL freshness banner, rollback DLL name).
- Updated module-layout table in `BUILD.md`.
Phase-wide code symbols (capacity `#define`s, `GBL_PTR_*` arrays, `.pre-phase9.bak`) are
listed in 09-01-PLAN.md. This plan adds no C symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Fixture A full round-trip UAT (test_fresh.dgt) + record results</name>
  <files>.planning/smoke-test-findings.md</files>
  <read_first>
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (Validation Architecture: Fixture A table, full step list and assertions)
    - .planning/phases/09-c-engine-cleanup-validation/09-VALIDATION.md (Manual-Only Verifications Fixture A row; pass criterion D-12)
    - .planning/smoke-test-findings.md (Phase 7/8 table format to mirror)
  </read_first>
  <what-built>
    Claude confirms the deployed `inst/libs/x64/tkogl2.dll` is the fresh Phase-9 build (from
    09-03), states the freshness banner, and notes the rollback file
    `inst/libs/x64/tkogl2.dll.pre-phase9.bak` for the UAT header. No new build needed unless
    09-03 was not deployed.
  </what-built>
  <action>
    Drive the user through the Fixture A round-trip, then write the result table into a new
    "## Phase 9 — Cleanup & Validation" section of `.planning/smoke-test-findings.md`,
    mirroring the Phase 7/8 tables, including the run date, the DLL freshness banner string,
    and the rollback DLL name. If any assertion fails, mark it a regression and STOP (the
    rollback is `Copy-Item inst/libs/x64/tkogl2.dll.pre-phase9.bak inst/libs/x64/tkogl2.dll`);
    surface for a gap-closure plan rather than recording a pass.
  </action>
  <how-to-verify>
    Using `zips/Folsom 3D models/test_fresh.dgt` (2 specimens; 3 landmarks; curve on specimen 1):
    1. `devtools::load_all(".")` + `GUImorph()` — window opens; no doubled nav buttons.
    2. Load PLY `zips/Folsom 3D models/C13.1.ply` — shaded mesh visible (not blank/black).
    3. Double-click to place landmarks — placement works; dots visible.
    4. Bind a 3-landmark curve on specimen 1 (`add("curve",1,2,3)`) — chord segments draw; no R error.
    5. Save `.dgt` — file written; `Curve=1`, `LM3=3`, `Surface=0` sections present.
    6. Same-session `openDgt` reload — both specimens + landmarks + curve restore; **`Surface=0` does not abort reload** (D-05 critical).
    7. GPA tab -> Compute (sliding OFF) — `gpagen` converges (3 LM x 2 specimens).
    8. Save Result CSV — non-empty (Csize + aligned coords).
    Expected: every step matches the Phase 4-5 baseline by visual + functional confirmation (D-12). Report "approved" or describe deviations.
  </how-to-verify>
  <verify>
    <human-check>All 8 Fixture A steps match the Phase 4-5 baseline; results recorded in smoke-test-findings.md with date, freshness banner, and rollback DLL name.</human-check>
  </verify>
  <acceptance_criteria>
    - All 8 Fixture A assertions pass by manual confirmation; `Surface=0` reload does not abort.
    - A "Phase 9 — Cleanup & Validation" Fixture A table exists in `smoke-test-findings.md` with date, DLL freshness banner, and `tkogl2.dll.pre-phase9.bak` rollback note.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the regression.</resume-signal>
  <done>Fixture A full round-trip passes with no regression and is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fixture B round-trip UAT (anchors + curves) + record results</name>
  <files>.planning/smoke-test-findings.md</files>
  <read_first>
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (Validation Architecture: Fixture B table)
    - .planning/phases/09-c-engine-cleanup-validation/09-VALIDATION.md (Manual-Only Verifications Fixture B row)
    - .planning/smoke-test-findings.md (Phase 8 anchor parity baseline)
  </read_first>
  <action>
    Drive the user through the Fixture B round-trip (exercises the Phase 8 dedup'd marker
    path), then append the Fixture B result table to the same "Phase 9 — Cleanup & Validation"
    section. Same regression/rollback handling as Task 1.
  </action>
  <how-to-verify>
    Using `zips/Folsom 3D models/test_dgt_anchors_curves.dgt` (landmarks + curve + anchor):
    1. Load `.dgt` — specimen renders; landmarks + curve + anchor restore (dedup'd marker path).
    2. Anchor place / select / move / delete — parity with Phase 8 (selected anchor moves/deletes; landmark unaffected).
    3. Landmark place / select / move / delete — parity with Phase 8.
    4. Re-save -> reload — anchor + curve survive the round-trip.
    Expected: parity with the Phase 8 baseline by visual + functional confirmation (D-12). Report "approved" or describe deviations.
  </how-to-verify>
  <verify>
    <human-check>All Fixture B steps match the Phase 8 baseline; anchor+curve survive re-save/reload; results recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - All Fixture B assertions pass by manual confirmation; anchor + curve survive re-save/reload.
    - A Fixture B table is appended to the "Phase 9 — Cleanup & Validation" section of `smoke-test-findings.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the regression.</resume-signal>
  <done>Fixture B round-trip passes with no regression and is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Update BUILD.md with final module-layout table (D-14)</name>
  <files>BUILD.md</files>
  <read_first>
    - BUILD.md (current "C source layout (Phase 7)" section to replace; MSVC toolchain note)
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (BUILD.md update (D-14): final module-layout table; prune list)
    - .planning/phases/09-c-engine-cleanup-validation/09-PATTERNS.md (Build & docs 09-03 section)
  </read_first>
  <action>
    Replace the current C-source-layout section in `BUILD.md` with the final module-layout
    table from RESEARCH (D-14): `tcl_init.c` (Tkogl2_Init + 8 commands), `tcl_dispatch.c`,
    `tcl_window.c`, `tcl_state.c` (note the new capacity-`#define`d arrays), `tcl_log.c`
    (the surviving diagnostic channel), `marker.c` (unified marker core, Phase 8),
    `curve_ZARF_9.c`, `ogl_ZARF9.c`/`ogl_model_ZARF_9.c`/`ogl_model_ply_ZARF_9.c`,
    `StatisticsFunction_ZARF_9.c`. Prune obsolete references to the deleted god file
    `tcl_if_ZARF_9.c` and the reference-only `.vcxproj`, and note the orphan
    `tcl_if_ZARF_9.c.bak` in `src/` (D-14). Keep build instructions MSVC-only (D-16) — do not
    reintroduce MinGW as a supported path. Do not change the `.dgt` format docs or any other
    section.
  </action>
  <verify>
    <automated>powershell -Command "$f='\\wsl$\Ubuntu\home\akagi\home\GUImorph\BUILD.md'; (Select-String -Path $f -Pattern 'tcl_dispatch','tcl_state','tcl_log','marker').Count -ge 4 -and (Select-String -Path $f -Pattern 'tcl_if_ZARF_9\.c(?!\.bak)').Count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - `BUILD.md` module-layout table lists the final modules (`tcl_init`/`tcl_dispatch`/`tcl_window`/`tcl_state`/`tcl_log`/`marker`/`curve`/`ogl_*`/`StatisticsFunction`).
    - No remaining reference to `tcl_if_ZARF_9.c` as a build source (the `.bak` orphan may be noted as removable); `.vcxproj` is reference-only.
    - Build instructions remain MSVC-only.
  </acceptance_criteria>
  <done>BUILD.md reflects the cleaned-up module layout (D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.dgt`/`.ply` file → loaders | Exercised (not modified) by this validation plan. UAT confirms the parse guards preserved in 09-02 still reject/handle malformed sections (e.g. `Surface=0`) correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | (validation only) | Whole engine via GUI UAT | mitigate | Full round-trip UAT on both fixtures is the regression gate; rollback to `tkogl2.dll.pre-phase9.bak` on any failure. No code changes except BUILD.md docs. |
| T-09-04-V5 | Tampering / DoS | `.dgt`/`.ply` parse guards | accept | Confirmed (not changed) here — the `Surface=0` reload assertion validates the guards survived 09-02. Pre-existing fixed-buffer hardening remains out of scope (cleanup-only). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- Fixture A: all 8 round-trip steps pass; GPA converges; CSV non-empty.
- Fixture B: anchor/landmark place/select/move/delete parity; anchor+curve survive re-save/reload.
- `Surface=0` reload does not abort on either fixture (D-05).
- Both result tables recorded in `smoke-test-findings.md` (date, freshness banner, rollback DLL).
- `BUILD.md` module table updated; `tcl_if_ZARF_9.c` build refs pruned; MSVC-only.
</verification>

<success_criteria>
- CENG-05 satisfied: full digitize + analysis workflow matches the Phase 4-5 / Phase 8 baselines by manual confirmation (D-09/D-10/D-12), with no regression.
- Documentation (BUILD.md) reflects the final cleaned-up engine layout (D-14).
- Phase 9 complete and rollback-able.
</success_criteria>

<output>
Create `.planning/phases/09-c-engine-cleanup-validation/09-04-SUMMARY.md` when done.
</output>
