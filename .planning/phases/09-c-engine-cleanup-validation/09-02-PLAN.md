---
phase: 09-c-engine-cleanup-validation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/curve_ZARF_9.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/marker.c
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll
autonomous: false
requirements: [CENG-04]
user_setup: []

must_haves:
  truths:
    - "Bare trace printf in the PLY/.dgt loaders is removed; load-bearing TAG_* diagnostics are routed through simpleLog (D-04)"
    - "The .dgt reload path still works — Surface=0 does not abort openDgt reload (D-05 critical)"
    - "All input-validation guards (sscanf/strstr/strncmp/FLAG_*) in the parsers are intact (V5)"
    - "if(0) debug toggles in the loader/geometry modules are deleted (D-07)"
    - "MSVC build green; Tkogl2_Init still exported"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c"
      provides: "Loader with trace printf removed and TAG_* errors on simpleLog"
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ZARF_9.c"
      provides: "if(0) toggle at line 488 deleted; D-macro call sites removed"
  key_links:
    - from: "ogl_loadDgtModel"
      to: "simpleLog()"
      via: "ported TAG_* error diagnostics"
      pattern: "simpleLog\\("
    - from: "ogl_loadDgtModel SURF parse"
      to: "FLAG_READ_SURFACES / sscanf"
      via: "preserved input-validation guards"
      pattern: "FLAG_READ_SURFACES"
---

<objective>
First half of CENG-04: remove debug cruft from the loader/geometry modules, concentrating
the highest-risk work — the D-05 `.dgt`/PLY reload path in `ogl_model_ply_ZARF_9.c` (1,616
lines, 74 live bare `printf`). Delete pure stdout trace, port the load-bearing
`ERROR : why did we not find TAG_*` diagnostics to `simpleLog`, and delete the `if(0)`
toggles in `ogl_model_ply_ZARF_9.c`, `ogl_model_ZARF_9.c`, `curve_ZARF_9.c`, and `marker.c`.
Also strip the `D/D1/D2/D3` debug-macro call sites in the two `ogl_*` files (the macro
definitions are removed later in 09-03).

Purpose: Finish the noisy-trace removal where it is riskiest, behind the unchanged `.dgt`
format, while preserving every parse guard (D-05 / V5). The mandatory smoke re-runs BOTH
fixtures' reload to confirm the documented `Surface=0` reload fix still holds.
Output: Cleaned loader/geometry sources, fresh MSVC DLL deployed, D-05 reload confirmed.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-c-engine-cleanup-validation/09-CONTEXT.md
@.planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md
@.planning/phases/09-c-engine-cleanup-validation/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates no new persistent symbols — it removes trace `printf`/`if(0)` and ports a
small number of error strings to `simpleLog`. Phase-wide new symbols (capacity `#define`s,
`GBL_PTR_*` arrays, `.pre-phase9.bak`, the `smoke-test-findings.md` Phase 9 section) are
listed in 09-01-PLAN.md / 09-04-PLAN.md. Routed diagnostic strings (e.g.
`"ERROR : ogl_loadDgtModel : TAG_SURF present but no value parsed"`) are new `simpleLog`
message literals, not exported symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove trace printf in the D-05 loader zone; port TAG_* errors to simpleLog</name>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c (ogl_loadModel 594+; ogl_loadLandMark 579+; ogl_loadDgtModel 1054+ incl. SURF/Surface parse and TAG_* error sites ~1181-1182/1207-1208/1240-1241; if(0) blocks at 324 and 379; D/D1/D2/D3 call sites x2)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.c (lines 130-155 simpleLog facility; confirm fprintf is the file writer — KEEP)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h (lines 7-23 simpleLog / simpleLog_Obj API)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/marker.c (lines 33, 51-52 simpleLog literal vs sprintf-then-simpleLog idiom)
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (D-05 zone; Pitfalls 1/2/4; Code Examples "Port a load-bearing diagnostic")
    - .planning/phases/09-c-engine-cleanup-validation/09-PATTERNS.md (Port target form; diagnostic destination API)
  </read_first>
  <action>
    In `ogl_model_ply_ZARF_9.c`, remove all ~74 live bare `printf(` trace calls (per-vertex
    dumps, "found SURF", "processing surfaces Na", per-landmark/curve/anchor dumps). Match only
    word-boundary bare `printf(` — NEVER touch `sprintf`/`fprintf`/`snprintf`/`vprintf`
    (Pitfall 1: those build log strings / write the `tcl_log.c` file facility). For the
    load-bearing diagnostics in `ogl_loadDgtModel` — the `ERROR : why did we not find TAG_*`
    pairs (~lines 1181-1182, 1207-1208, 1240-1241) — replace each pair with a single descriptive
    `simpleLog("ERROR : ogl_loadDgtModel : TAG_<x> present but no value parsed")` call (D-04),
    using the literal-string `simpleLog(...)` idiom from `marker.c:33`. Delete the two `if(0)`
    debug blocks at ~line 324 (hardcoded `C:/home/...` flat-file dump) and ~line 379
    ("turned off 10 July 2020" per-vertex dump) (D-07). Remove the two `D/D1/D2/D3` debug-macro
    call sites in this file (the definitions are removed in 09-03).

    CRITICAL D-05 / V5: do NOT remove or alter any `sscanf`, `strstr`, `strncmp`, or `FLAG_*`
    (e.g. `FLAG_READ_SURFACES`) line. The `Surface=0` reload handling depends on
    `sscanf(++surfDigits, "%d", &nSurfaces)` + `FLAG_READ_SURFACES = 0/1`; these are
    load-bearing, not trace. Where a bare `printf` is the sole body of a brace-less `if`/`for`
    (Pitfall 2), remove the entire statement (and the now-empty `if`) — never leave `if (cond);`.
    Leave the pre-existing logging bug at ~1227-1228 (`sprintf(ogl_buffer,...)` then
    `simpleLog(buffer)`) as-is unless separately decided (D-02). Do not change the `.dgt` read
    or write semantics in any way.
  </action>
  <verify>
    <automated>powershell -Command "$f='\\wsl$\Ubuntu\home\akagi\home\GUImorph\integrated-guimorph-development_EOC\Project\tkogl2\src\ogl_model_ply_ZARF_9.c'; (Get-Content $f | Where-Object { $_ -notmatch '^\s*//' -and $_ -notmatch '^\s*\*' } | Select-String -Pattern '(?<![A-Za-z_])printf\(').Count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - Zero live (non-comment) bare `printf(` remain in `ogl_model_ply_ZARF_9.c` (word-boundary match; `sprintf`/`fprintf` excluded).
    - The `ERROR : ... TAG_*` diagnostics now emit via `simpleLog(...)` (grep finds the new simpleLog calls; the old bare-`printf` TAG_* pairs are gone).
    - All `sscanf`/`strstr`/`strncmp`/`FLAG_READ_SURFACES` lines in `ogl_loadDgtModel` are unchanged (diff shows no edits to those lines).
    - The two `if(0)` blocks (324, 379) and the two `D/D1/D2/D3` call sites are removed.
    - No `if (cond);` empty-controlled-statement introduced (MSVC build has no new "empty controlled statement"/empty-body warning).
    - MSVC build green (exit 0), no new warnings vs Phase 8.
  </acceptance_criteria>
  <done>Loader trace removed, TAG_* errors on simpleLog, parse guards intact, build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete if(0) toggles and D-macro call sites in geometry modules</name>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ZARF_9.c, integrated-guimorph-development_EOC/Project/tkogl2/src/curve_ZARF_9.c, integrated-guimorph-development_EOC/Project/tkogl2/src/marker.c</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ZARF_9.c (if(0) at line 488 named in CONTEXT D-07; D/D1/D2/D3 call sites x2; per RESEARCH 0 live bare printf)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/curve_ZARF_9.c (if(0) at lines 572, 585; per RESEARCH 0 live bare printf)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/marker.c (if(0) at line 469 "SHOULD NOT BE HERE" dead error path — shown in 09-PATTERNS.md)
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (if(0) site table; per-file printf table confirming 0 live bare printf in these three files)
    - .planning/phases/09-c-engine-cleanup-validation/09-PATTERNS.md (marker.c:469-479 if(0) delete precedent)
  </read_first>
  <action>
    Delete the entire `if(0) { ... }` debug blocks at: `ogl_model_ZARF_9.c:488`,
    `curve_ZARF_9.c:572` and `:585`, and `marker.c:469` (the dead "SHOULD NOT BE HERE"
    anchors-slice path). Remove the whole block including the `if (0)` header and its braces
    (D-07), matching the precedent in 09-PATTERNS.md. In `ogl_model_ZARF_9.c` also remove the
    two `D/D1/D2/D3` debug-macro call sites (definitions removed in 09-03). Per RESEARCH,
    these three files have 0 live bare `printf(` — do NOT hunt for printf here; only the
    `if(0)` blocks and the two `ogl_model_ZARF_9.c` macro call sites are in scope. Do not alter
    any live geometry/marker logic, the `marker_*` core, or `marker.h`.
  </action>
  <verify>
    <automated>powershell -Command "$fs='ogl_model_ZARF_9.c','curve_ZARF_9.c','marker.c'; $b='\\wsl$\Ubuntu\home\akagi\home\GUImorph\integrated-guimorph-development_EOC\Project\tkogl2\src\'; ($fs | ForEach-Object { Select-String -Path ($b+$_) -Pattern 'if\s*\(\s*0\s*\)' }).Count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - No `if (0)` / `if(0)` blocks remain in `ogl_model_ZARF_9.c`, `curve_ZARF_9.c`, or `marker.c`.
    - The two `D/D1/D2/D3` call sites in `ogl_model_ZARF_9.c` are removed.
    - `marker.h` and the `marker_*` core functions are unchanged (diff limited to the deleted `if(0)` block).
    - MSVC build green (exit 0), no new warnings vs Phase 8.
  </acceptance_criteria>
  <done>if(0) toggles and geometry-module D-macro call sites removed; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy and re-run BOTH fixtures' reload (D-05 critical smoke)</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll</files>
  <read_first>
    - .planning/phases/09-c-engine-cleanup-validation/09-VALIDATION.md (Sampling Rate: smoke after debug-removal; Manual-Only Verifications D-05 row)
    - .planning/phases/09-c-engine-cleanup-validation/09-RESEARCH.md (Pitfall 4 D-05; Validation Architecture Fixture A/B reload rows)
    - .planning/smoke-test-findings.md (prior reload findings; Surface=0 fix history)
  </read_first>
  <what-built>
    Claude automates: build MSVC Release, confirm `Tkogl2_Init` export, deploy
    `build-msvc\Release\tkogl2.dll` to `inst/libs/x64/tkogl2.dll`, confirm the freshness banner.
  </what-built>
  <action>
    After the automated build/export/deploy, ask the user to verify the D-05 reload path for
    BOTH fixtures (the precise reason this plan's loader edits are high-risk). This is a focused
    reload smoke, not the full round-trip (that is 09-04).
  </action>
  <how-to-verify>
    1. `devtools::load_all(".")` + `GUImorph()`; load `zips/Folsom 3D models/C13.1.ply` — mesh renders.
    2. Fixture A — `openDgt` on `zips/Folsom 3D models/test_fresh.dgt`: both specimens + landmarks + curve restore; **`Surface=0` does NOT abort the reload** (D-05 critical). No R error in console.
    3. Fixture B — `openDgt` on `zips/Folsom 3D models/test_dgt_anchors_curves.dgt`: specimen + landmarks + curve + anchor restore.
    4. Confirm the log file still receives `ERROR : ... TAG_*` style diagnostics via `simpleLog` if a malformed section is hit (best-effort; the key check is that reload succeeds).
    Expected: reload parity with Phase 4 (Fixture A) and Phase 8 (Fixture B) baselines. Report "approved" or describe deviations.
  </how-to-verify>
  <verify>
    <automated>powershell -Command "dumpbin /exports '\\wsl$\Ubuntu\home\akagi\home\GUImorph\integrated-guimorph-development_EOC\Project\tkogl2\build-msvc\Release\tkogl2.dll' | Select-String 'Tkogl2_Init'"</automated>
    <human-check>Both fixtures reload via openDgt with Surface=0 not aborting (D-05); landmarks/curve/anchor restore; no R error.</human-check>
  </verify>
  <acceptance_criteria>
    - `Tkogl2_Init` present in `dumpbin /exports`; fresh DLL deployed (freshness banner today's date).
    - User confirms both `.dgt` fixtures reload with `Surface=0` not aborting and full marker/curve restore — parity with baselines.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
  <done>Loader cleanup verified safe; D-05 reload holds for both fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.dgt`/`.ply` file → `ogl_loadDgtModel` / `ogl_loadModel` | The only external untrusted input. This plan edits trace `printf` AROUND the parser; the parse + validation logic must remain byte-for-byte behavior-equivalent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-V5 | Tampering / DoS | `.dgt`/`.ply` parse guards (`sscanf`, `strncmp`, `strstr`, `FLAG_*` in `ogl_model_ply_ZARF_9.c`) | mitigate | Delete only trace `printf`; preserve ALL existing input-validation guards (V5). Task 1 acceptance asserts those lines are unchanged; Task 3 re-runs both fixtures' reload. Pre-existing fixed-buffer/`sscanf` width hardening is out of scope (cleanup-only) — flag, do not fix. |
| T-09-02 | Information Disclosure | stdout trace volume | accept | Removing stdout trace reduces, not increases, disclosure. Load-bearing diagnostics move to the existing `simpleLog` file facility (no new sink). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- Zero live bare `printf(` in `ogl_model_ply_ZARF_9.c`; TAG_* errors on `simpleLog`.
- Zero `if(0)` blocks in the four edited modules' targeted sites.
- All parser guards intact (diff inspection); MSVC build green, no new warnings.
- `Tkogl2_Init` exported; both fixtures reload with `Surface=0` not aborting (D-05).
</verification>

<success_criteria>
- Loader/geometry trace cruft removed without weakening any parse guard (D-04/D-05/V5/D-07).
- `.dgt` round-trip reload parity preserved for both fixtures.
- DLL interface byte-compatible; smoke approved.
</success_criteria>

<output>
Create `.planning/phases/09-c-engine-cleanup-validation/09-02-SUMMARY.md` when done.
</output>
