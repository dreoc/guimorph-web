---
phase: 06-reproducible-dev-environment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - BUILD.md
  - scripts/deploy-dll.ps1
  - integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
  - .planning/smoke-test-findings.md
autonomous: false
requirements:
  - DEV-01
  - DEV-02
  - DEV-03
user_setup:
  - service: MSYS2
    why: "Windows-native MinGW build is primary C path per D-11"
    dashboard_config:
      - task: "Install MSYS2 if missing"
        location: "PowerShell: winget install -e --id MSYS2.MSYS2"
      - task: "Install UCRT64 toolchain in MSYS2 UCRT64 shell"
        location: "pacman -S --needed mingw-w64-ucrt-x86_64-toolchain cmake make"

must_haves:
  truths:
    - "Root BUILD.md documents full contributor cycle: prerequisites → build → deploy → renv restore → smoke test"
    - "Windows-native MSYS2 MinGW is the primary build path; WSL cross-compile is optional appendix only"
    - "deploy-dll.ps1 copies build/tkogl2.dll to inst/libs/x64/ with auto-backup to tkogl2.dll.bak"
    - "Manual Copy-Item fallback documented alongside script"
    - "Post-deploy verification requires load_all + GUImorph() + sample PLY load"
    - "Fresh clone warned that tkogl2.dll is gitignored and must be built or deployed"
  artifacts:
    - path: BUILD.md
      provides: "Integrated contributor setup doc"
      min_lines: 80
    - path: scripts/deploy-dll.ps1
      provides: "DLL deploy with backup"
      exports: ["RepoRoot resolution", "Src validation", "Backup before swap"]
    - path: integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
      provides: "Compile deep-dive with Windows-native primary"
  key_links:
    - from: scripts/deploy-dll.ps1
      to: integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll
      via: "Copy-Item from tkogl2/build/tkogl2.dll"
      pattern: "tkogl2/build/tkogl2\\.dll"
    - from: BUILD.md
      to: integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
      via: "Advanced / deep-dive link"
      pattern: "tkogl2/BUILD\\.md"
    - from: BUILD.md
      to: renv.lock
      via: "R Environment section: renv::restore()"
      pattern: "renv::restore"
---

<objective>
Create root BUILD.md with validated build-deploy-test workflow, implement scripts/deploy-dll.ps1 with auto-backup, and restructure tkogl2/BUILD.md for Windows-native MinGW primary.

Purpose: DEV-02 and DEV-03 — contributor-facing build cycle and DLL swap workflow. Implements D-05 through D-16, D-09 through D-12.
Output: BUILD.md, deploy-dll.ps1, updated tkogl2/BUILD.md, optional UAT note in smoke-test-findings.md.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-reproducible-dev-environment/06-CONTEXT.md
@.planning/phases/06-reproducible-dev-environment/06-RESEARCH.md
@.planning/phases/06-reproducible-dev-environment/06-PATTERNS.md
@.planning/phases/06-reproducible-dev-environment/06-01-SUMMARY.md
@integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
@integrated-guimorph-development_EOC/Project/tkogl2/cmake/mingw-w64-x86_64.cmake
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/deploy-dll.ps1 with validation and auto-backup</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (Pattern 3, Security Domain)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (deploy-dll.ps1 section)
    - .gitignore (tkogl2.dll gitignore policy)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R (.onLoad path)
  </read_first>
  <files>scripts/deploy-dll.ps1</files>
  <action>
    Create scripts/deploy-dll.ps1 per D-13, D-14, D-15 and security requirements:

    1. **Path resolution from repo root (T-6-04):** $RepoRoot = Split-Path -Parent $PSScriptRoot. All paths via Join-Path — no user-supplied destination paths.

    2. **Source validation (T-6-04, V5):** $Src = Join-Path $RepoRoot "integrated-guimorph-development_EOC/Project/tkogl2/build/tkogl2.dll". If (-not (Test-Path $Src)) { throw "Build output not found: $Src" } — fail closed.

    3. **Deploy target:** $DestDir = .../GUImorphDevelopment/inst/libs/x64; $Dest = tkogl2.dll. Do NOT copy or overwrite glut64.dll (D-15).

    4. **Auto-backup (D-14):** If $Dest exists, Copy-Item to tkogl2.dll.bak in same directory before overwrite.

    5. **Deploy:** Copy-Item -Force $Src to $Dest. Write-Host confirming source, dest, and backup path (or "none" if first deploy).

    6. **Error handling:** $ErrorActionPreference = "Stop" at top.

    Do NOT use WSL UNC paths. Do NOT accept -Dest or -Src parameters from caller without validation.
  </action>
  <acceptance_criteria>
    - Script resolves RepoRoot from scripts/ parent directory
    - Missing build/tkogl2.dll throws explicit error (fail closed)
    - Existing inst/libs/x64/tkogl2.dll backed up to tkogl2.dll.bak before overwrite
    - glut64.dll is never referenced or modified
    - Script runs from repo root via: powershell -File scripts/deploy-dll.ps1
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/deploy-dll.ps1 && grep -q 'Split-Path -Parent \$PSScriptRoot' scripts/deploy-dll.ps1</automated>
    <automated>grep -q 'Test-Path \$Src' scripts/deploy-dll.ps1 && grep -q 'tkogl2\.dll\.bak' scripts/deploy-dll.ps1</automated>
    <automated>grep -v '^#' scripts/deploy-dll.ps1 | grep -c 'glut64' | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>deploy-dll.ps1 created with validated paths, source check, and backup-before-swap</done>
</task>

<task type="auto">
  <name>Task 2: Write root BUILD.md and restructure tkogl2/BUILD.md</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-05, D-07, D-09, D-10, D-11, D-12)
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (BUILD.md Section Outline, Pitfalls 1–5)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (BUILD.md + tkogl2/BUILD.md sections)
    - integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
    - integrated-guimorph-development_EOC/Project/tkogl2/cmake/mingw-w64-x86_64.cmake
  </read_first>
  <files>BUILD.md, integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md</files>
  <action>
    **Create repo-root BUILD.md (D-05, D-07)** — single integrated doc, numbered sections matching tkogl2/BUILD.md tone:

    1. **Prerequisites:** Windows 10+, R 4.6+ (winget install -e --id RProject.R per D-10), MSYS2 (winget install -e --id MSYS2.MSYS2), Git clone to normal Windows path. Explicit note: tkogl2.dll is **gitignored** — clone + renv restore alone does not provide DLL (RESEARCH Pitfall 1). WSL not required (D-09).

    2. **Native Build (Windows — primary, D-11):** MSYS2 UCRT64 shell commands:
       pacman -S --needed mingw-w64-ucrt-x86_64-toolchain cmake make
       cd to tkogl2/; cmake -B build -S . -G "MinGW Makefiles"; cmake --build build -j
       Do NOT use cmake/mingw-w64-x86_64.cmake on Windows (Linux sysroot paths).

    3. **DLL Deploy (DEV-03):** Document scripts/deploy-dll.ps1 usage and manual Copy-Item fallback (RESEARCH Pattern 3). State deploy touches tkogl2.dll only (D-15).

    4. **R Environment (DEV-01 cross-ref):** renv::restore() from GUImorphDevelopment/ on Windows R only (D-04). Minimum R 4.6+ (D-03). Link to renv.lock from Plan 06-01.

    5. **Smoke Test (D-16, D-12):** load_all → GUImorph() → load sample PLY. Targeted verification: R-only edits → load_all + affected GUI path; C/tkogl2 edits → rebuild → deploy → full GUI smoke.

    6. **Troubleshooting:** winget R PATH issue, missing rgl after restore, tkogl2 load failure, warning inventory pointer to smoke-test-findings.md (D-18).

    7. **Advanced: WSL Cross-Compile:** Brief pointer to tkogl2/BUILD.md appendix — not contributor default.

    **Update tkogl2/BUILD.md (D-11):** Restructure — Windows-native MSYS2 UCRT64 as primary "## 2. Configure &amp; build". Move existing WSL apt/mingw-w64 block to "## Advanced: WSL Cross-Compile" with explicit -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-w64-x86_64.cmake. Keep dependency table, Tcl stub notes, build/ throwaway notes. Update title/intro to reflect Windows-native primary (not WSL-only).

    Do NOT create DEVELOPMENT.md split. Do NOT document scripts/guimorph-startup.R WSL UNC paths as contributor template (D-08).
  </action>
  <acceptance_criteria>
    - BUILD.md exists at repo root with all six main sections plus Advanced appendix link
    - BUILD.md states tkogl2.dll gitignored and Windows R restore-only path
    - BUILD.md documents winget R and MSYS2 install one-liners
    - BUILD.md includes deploy-dll.ps1 and manual Copy-Item fallback
    - tkogl2/BUILD.md primary build section uses MSYS2 MinGW Makefiles without toolchain file
    - WSL cross-compile retained as appendix with toolchain file warning
  </acceptance_criteria>
  <verify>
    <automated>test -f BUILD.md && grep -q 'renv::restore' BUILD.md && grep -q 'deploy-dll.ps1' BUILD.md</automated>
    <automated>grep -q 'gitignored\|not committed\|not tracked' BUILD.md</automated>
    <automated>grep -q 'MinGW Makefiles' integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md</automated>
    <automated>grep -q 'WSL\|Cross-Compile' integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md</automated>
  </verify>
  <done>Integrated BUILD.md and restructured tkogl2/BUILD.md published with Windows-native primary path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT deploy script and BUILD.md smoke path (DEV-02, DEV-03, D-16)</name>
  <read_first>
    - BUILD.md (just written)
    - scripts/deploy-dll.ps1
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (Assumption A1 — native build UAT)
  </read_first>
  <files>BUILD.md, scripts/deploy-dll.ps1, .planning/smoke-test-findings.md</files>
  <what-built>BUILD.md contributor guide, deploy-dll.ps1, updated tkogl2/BUILD.md</what-built>
  <how-to-verify>
    **Option A — C rebuild path (preferred if MSYS2 available):**
    1. Follow BUILD.md Native Build section in MSYS2 UCRT64; confirm build/tkogl2.dll exists.
    2. Run: powershell -ExecutionPolicy Bypass -File scripts/deploy-dll.ps1 from repo root.
    3. Confirm inst/libs/x64/tkogl2.dll.bak created if prior DLL existed.
    4. Windows R: renv::restore(); devtools::load_all("."); GUImorph(); load sample PLY (e.g. C13.1.ply).

    **Option B — existing DLL (if native build not yet UAT'd):**
    1. Use existing inst/libs/x64/tkogl2.dll; touch build/ copy or skip rebuild.
    2. Run deploy-dll.ps1 dry-run path: verify script error when build/tkogl2.dll missing; copy build artifact manually once to test backup behavior.
    3. Complete steps 4 above for GUI smoke.

    Append brief UAT result to smoke-test-findings.md (## Phase 6 — BUILD.md UAT) noting whether MSYS2 native build was validated (A1).
  </how-to-verify>
  <acceptance_criteria>
    - deploy-dll.ps1 runs without error when build/tkogl2.dll exists
    - tkogl2.dll.bak created when prior deployed DLL existed
    - load_all + GUImorph() + PLY load succeeds after deploy
    - BUILD.md walkthrough feasible on Windows without WSL
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe BUILD/deploy/smoke failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/tkogl2.dll → inst/libs/x64/ | Native binary swap affects runtime behavior |
| PowerShell script → filesystem | deploy-dll.ps1 writes to package inst/ tree |
| Contributor → BUILD.md instructions | Untrusted reader follows documented commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering | deploy-dll.ps1 wrong/missing source | mitigate | Test-Path $Src with throw; resolve paths from $PSScriptRoot only |
| T-6-05 | Tampering | Path traversal in deploy script | mitigate | Join-Path from fixed RepoRoot; no user-supplied dest without validation |
| T-6-06 | Tampering | Executing wrong DLL | mitigate | Auto-backup to tkogl2.dll.bak before swap (D-14); document rollback Copy-Item |
| T-6-07 | Denial of Service | Missing build output | mitigate | Fail closed with explicit error message; BUILD.md states build prerequisite |
| T-6-SC | Tampering | MSYS2/pacman packages | accept | Standard MSYS2 UCRT64 toolchain; document winget/pacman sources in BUILD.md |
</threat_model>

<verification>
- deploy-dll.ps1 passes automated grep gates (RepoRoot, Src validation, backup, no glut64)
- BUILD.md and tkogl2/BUILD.md pass section/content grep gates
- Human UAT confirms deploy + GUI smoke per D-16
</verification>

<success_criteria>
- DEV-02 satisfied: BUILD.md documents full build-deploy-test cycle with Windows-native primary path
- DEV-03 satisfied: DLL swap via script + manual fallback with backup
- D-11 UAT status recorded in smoke-test-findings.md
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Plan | Description |
|---------------|------|-------------|
| `BUILD.md` (repo root) | 06-02 | Integrated contributor setup: prerequisites through troubleshooting |
| `scripts/deploy-dll.ps1` | 06-02 | PowerShell deploy with source validation and tkogl2.dll.bak backup |
| `integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md` | 06-02 | Restructured compile doc; Windows-native primary, WSL appendix |
| `.planning/smoke-test-findings.md` (BUILD UAT section) | 06-02 | Optional MSYS2 native build UAT note |

<output>
Create `.planning/phases/06-reproducible-dev-environment/06-02-SUMMARY.md` when done
</output>
