---
phase: 06-reproducible-dev-environment
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DEV-02
user_setup: []

must_haves:
  truths:
    - "README provides 5-step quick-start linking to BUILD.md without duplicating full instructions"
    - "README documents inherent GUImorph UX quirks (double-click landmarks, etc.)"
    - "README does not promote WSL/UNC paths or maintainer-local startup scripts as defaults"
    - "New contributor can find BUILD.md from README and understand Windows-first setup"
  artifacts:
    - path: README.md
      provides: "GitHub-facing quick-start and known behavior"
      min_lines: 25
  key_links:
    - from: README.md
      to: BUILD.md
      via: "Quick start step 2 link"
      pattern: "\\[BUILD\\.md\\]\\(BUILD\\.md\\)"
    - from: README.md
      to: GUImorphDevelopment/
      via: "renv restore cwd instruction"
      pattern: "GUImorphDevelopment"
---

<objective>
Add brief GitHub-facing quick-start and inherent GUImorph behavior section to README.md, pointing contributors to BUILD.md for full setup.

Purpose: DEV-02 contributor onboarding — README is the entry point; BUILD.md holds detail. Implements D-06 and D-08.
Output: Updated README.md with quick-start + Known behavior sections.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-reproducible-dev-environment/06-CONTEXT.md
@.planning/phases/06-reproducible-dev-environment/06-RESEARCH.md
@.planning/phases/06-reproducible-dev-environment/06-PATTERNS.md
@.planning/phases/06-reproducible-dev-environment/06-02-SUMMARY.md
@BUILD.md
@.planning/smoke-test-findings.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quick start section with BUILD.md pointer</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-06, D-09)
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (README Quick-Start outline)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (README section)
    - BUILD.md (verify section names for accurate links)
    - README.md (preserve existing title and description paragraph)
  </read_first>
  <files>README.md</files>
  <action>
    Update README.md per D-06 — brief pointer, not duplicate BUILD.md content:

    1. **Preserve** existing title (line 1) and description paragraph (line 2) unchanged.

    2. **Add ## Quick start** with exactly 5 numbered steps (D-06 discretion):
       1. Install R 4.6+ and clone this repo to a Windows path.
       2. Follow [BUILD.md](BUILD.md) to build and deploy tkogl2.dll.
       3. Open R with working directory = integrated-guimorph-development_EOC/Project/GUImorphDevelopment/ and run renv::restore().
       4. Run devtools::load_all(".") then GUImorph().
       5. Load a .ply specimen; see **Known behavior** below for UI quirks.

    3. **Platform note (D-09):** One sentence: Windows R required for GUI; WSL not required for contributors.

    4. **Do NOT** inline full cmake, deploy, or renv instructions — link to BUILD.md only.

    5. **Do NOT** reference scripts/guimorph-startup.R or WSL UNC setwd paths (D-08).
  </action>
  <acceptance_criteria>
    - README contains ## Quick start with 5 numbered steps
    - Step 2 links to BUILD.md with relative path BUILD.md
    - Step 3 names GUImorphDevelopment/ as renv cwd
    - No WSL/UNC path examples in quick-start
    - Original title and description paragraph preserved
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Quick start' README.md && grep -q '\\[BUILD\\.md\\](BUILD\\.md)' README.md</automated>
    <automated>grep -c '^[0-9]\\.' README.md | awk '{exit ($1 >= 5) ? 0 : 1}'</automated>
    <automated>grep -v '^#' README.md | grep -ci 'wsl\\|unc\\|guimorph-startup' | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>README quick-start section added with BUILD.md link and Windows-first path</done>
</task>

<task type="auto">
  <name>Task 2: Add Known behavior quirks section</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-08, D-18)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (Known behavior table)
    - .planning/smoke-test-findings.md (landmark double-click, Fit button, curves, GPA plot)
    - BUILD.md (troubleshooting cross-reference only — do not duplicate warning inventory)
  </read_first>
  <files>README.md</files>
  <action>
    Add ## Known behavior section per D-08 — inherent GUImorph UX that looks like bugs; document only, no code fixes:

    Include bullet points sourced from smoke-test-findings.md:
    - **Landmarks:** double-click canvas to place; single-click is pick/select only.
    - **Fit button:** may show no visible change if view already at default rotation/zoom.
    - **Curves:** legacy chord segments between 3 landmark IDs, not surface-following splines.
    - **GPA Plot:** may need rgl; Alt+Tab if plot window opens behind Tk GUI.

    Add one line: For build troubleshooting and load_all warning details, see [BUILD.md](BUILD.md) and .planning/smoke-test-findings.md (maintainer audit log).

    Do NOT list WSL setup quirks. Do NOT document backlog items (999.x openDgt specimen order) as expected behavior unless confirmed inherent — skip openDgt second-specimen issue (backlog, not inherent quirk).
  </action>
  <acceptance_criteria>
    - README contains ## Known behavior with at least 4 quirk bullets
    - Double-click landmark placement documented
    - No WSL/UNC setup documented as contributor default
    - Technical warning triage deferred to BUILD.md / smoke-test-findings (not full inventory in README)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Known behavior' README.md && grep -qi 'double-click' README.md</automated>
    <automated>grep -qi 'Fit button\|Curves:\|GPA Plot' README.md</automated>
  </verify>
  <done>README documents inherent GUImorph quirks for contributors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → external readers | Public-facing doc; no secrets expected |
| README → BUILD.md | Links must remain valid relative paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-08 | Information Disclosure | README content | accept | No credentials or local paths; generic Windows path guidance only |
| T-6-09 | Tampering | Stale BUILD.md link | mitigate | Relative link BUILD.md; verify link exists in automated grep gate |
| T-6-SC | Spoofing | Misleading contributor path | mitigate | D-09 Windows-first; no WSL/UNC defaults in README |
</threat_model>

<verification>
- README quick-start and Known behavior sections pass automated grep gates
- BUILD.md link resolves from repo root
- No WSL/UNC/guimorph-startup references in README body
</verification>

<success_criteria>
- DEV-02 contributor onboarding: new reader can follow README → BUILD.md → working GUI path
- D-06 and D-08 satisfied: brief quick-start + inherent quirks only
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Plan | Description |
|---------------|------|-------------|
| `README.md` (updated) | 06-03 | ## Quick start (5 steps) + ## Known behavior sections |

<output>
Create `.planning/phases/06-reproducible-dev-environment/06-03-SUMMARY.md` when done
</output>
