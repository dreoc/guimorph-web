---
phase: 06-reproducible-dev-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rprofile
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv/activate.R
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rbuildignore
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv/settings.dcf
  - .planning/smoke-test-findings.md
autonomous: false
requirements:
  - DEV-01
user_setup:
  - service: Windows R
    why: "renv restore and lockfile snapshot target Windows R 4.6+ GUI runtime per D-04"
    env_vars: []
    dashboard_config:
      - task: "Install R 4.6+ if missing"
        location: "PowerShell: winget install -e --id RProject.R"

must_haves:
  truths:
    - "renv.lock restores package versions on Windows R 4.6+ from GUImorphDevelopment/"
    - "Lockfile pins DESCRIPTION Imports plus workflow extras (geomorph, rgl, RRPP, devtools)"
    - "Restore instructions document Windows R-only path with minimum R 4.6+"
    - "Post-restore load_all warning baseline captured and classified HOT vs DEFERRED"
    - "HOT warnings blocking load_all, tkogl2 load, or GPA are fixed; DEFERRED documented only"
  artifacts:
    - path: integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock
      provides: "Pinned R package versions for reproducible restore"
      contains: "geomorph"
    - path: integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rprofile
      provides: "renv activation on R startup"
      pattern: "renv/activate.R"
    - path: integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv/activate.R
      provides: "renv library bootstrap"
    - path: integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rbuildignore
      provides: "Exclude renv dirs from package tarball"
      contains: "^renv"
  key_links:
    - from: ".Rprofile"
      to: "renv/activate.R"
      via: "source() on R startup"
      pattern: "source\\(\"renv/activate\\.R\"\\)"
    - from: "renv.lock"
      to: "DESCRIPTION Imports"
      via: "snapshot after explicit install of Imports + extras"
      pattern: "geomorph|rgl|RRPP"
---

<objective>
Initialize `renv` in `GUImorphDevelopment/`, pin working R dependencies after Phases 1–5 smoke, capture the first post-lockfile warning baseline, and fix HOT warnings only.

Purpose: DEV-01 — reproducible R environment locked before C engine refactor. Implements D-01 through D-04, D-17, D-19, D-20.
Output: `renv.lock`, renv scaffolding, `.Rbuildignore` updates, Phase 6 warning triage section in smoke-test-findings.md.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-reproducible-dev-environment/06-CONTEXT.md
@.planning/phases/06-reproducible-dev-environment/06-RESEARCH.md
@.planning/phases/06-reproducible-dev-environment/06-PATTERNS.md
@.planning/phases/06-reproducible-dev-environment/06-VALIDATION.md
@.planning/smoke-test-findings.md
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/DESCRIPTION
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rbuildignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize renv and snapshot working dependencies</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (Pattern 1, Package Legitimacy Audit)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (renv.lock + .Rprofile section)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/DESCRIPTION
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock, integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rprofile, integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv/activate.R, integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rbuildignore, integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv/settings.dcf</files>
  <action>
    Run all renv steps on **Windows R 4.6+** with working directory = `GUImorphDevelopment/` on a normal Windows path (D-04). Do not create WSL R lockfiles.

    1. **Pre-smoke package install (D-02):** Before `renv::init()`, install DESCRIPTION Imports plus workflow extras explicitly:
       geomorph, Morpho, Rvcg, vegan, tcltk2, devtools, rgl, RRPP, renv.
       Include RRPP explicitly (not only transitively via geomorph) for lockfile reproducibility.

    2. **Smoke before snapshot (RESEARCH Pattern 1):** With deployed `inst/libs/x64/tkogl2.dll` present, run:
       devtools::load_all("."); GUImorph(); load a sample PLY if available.
       Do not snapshot until load_all completes without fatal error.

    3. **Initialize renv (D-01):** From package root:
       renv::init(settings = list(ignored.packages = "devtools"))
       renv::snapshot()
       Commit scaffold output: renv.lock, .Rprofile (sources renv/activate.R), renv/activate.R, renv/settings.dcf if created.

    4. **Verify .Rbuildignore (D-01):** Ensure scaffold added ^renv$ and ^renv\.lock$ entries; preserve existing ^.*\.Rproj$ and ^\.Rproj\.user$ lines.

    5. **Document minimum R (D-03):** Add a comment block at top of renv.lock or note in smoke-test appendix: minimum R 4.6+; minor R patch mismatch acceptable — do not configure renv to fail restore on patch drift.

    Do NOT hand-author renv.lock JSON; use renv::snapshot() only. Do NOT commit renv/library/.
  </action>
  <acceptance_criteria>
    - renv.lock exists and lists geomorph, rgl, and RRPP (or RRPP as transitive with explicit install documented)
    - .Rprofile contains source("renv/activate.R")
    - renv/activate.R exists and is generated by renv::init()
    - .Rbuildignore contains ^renv$ and ^renv\.lock$ in addition to existing entries
    - Snapshot taken only after successful load_all smoke on Windows R
  </acceptance_criteria>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock && grep -q 'geomorph' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock</automated>
    <automated>grep -q 'rgl' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock</automated>
    <automated>grep -q 'RRPP' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock</automated>
    <automated>grep -q 'renv/activate.R' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rprofile</automated>
    <automated>grep -E '^\^renv\$|^\^renv\\\.lock\$' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/.Rbuildignore | wc -l | awk '{exit ($1 >= 2) ? 0 : 1}'</automated>
  </verify>
  <done>renv scaffolding committed; lockfile pins Imports + workflow extras from working Windows R baseline</done>
</task>

<task type="auto">
  <name>Task 2: Capture warning baseline and triage HOT warnings</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-CONTEXT.md (D-17, D-18, D-19, D-20)
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (Pattern 4, Pitfall 6)
    - .planning/phases/06-reproducible-dev-environment/06-PATTERNS.md (smoke-test-findings append section)
    - .planning/smoke-test-findings.md
  </read_first>
  <files>.planning/smoke-test-findings.md, integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/*.R</files>
  <action>
    After Task 1 lockfile exists, on Windows R in a **fresh session** (D-20):

    1. setwd to GUImorphDevelopment/; run renv::restore(); devtools::load_all(".").
    2. Capture w &lt;- warnings() output — full text, not just count. Append new section to smoke-test-findings.md:

       ## Phase 6 — renv Baseline + Warning Triage (06-01)
       Table: renv::restore result, load_all result, warning count, HOT/DEFERRED classification.

    3. **Classify each warning (D-19):**
       HOT = blocks load_all, missing symbols, tkogl2 load failure, gpagen/GPA breakage.
       DEFERRED = roxygen, deprecated noise, documentation gaps, cosmetic NAMESPACE notes.

    4. **Fix HOT only (D-17, D-19):** Apply minimal source fixes in R/ if any HOT warning causes workflow failure. Re-run load_all to confirm fix. Do NOT bulk-fix all 26 warnings.

    5. If no HOT warnings, state "none — all DEFERRED" in smoke-test section. DEFERRED list goes to smoke-test-findings.md (D-18 technical inventory); user-facing quirks belong in README (Plan 06-03).

    Append only — do not rewrite Phase 1–5 sections.
  </action>
  <acceptance_criteria>
    - smoke-test-findings.md contains Phase 6 section with restore + load_all results
    - Full warnings() output or structured summary with HOT/DEFERRED labels
    - Any HOT warning has corresponding fix or documented blocker with next step
    - No bulk cleanup of DEFERRED warnings attempted
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 6 — renv Baseline' .planning/smoke-test-findings.md</automated>
    <automated>grep -q 'HOT' .planning/smoke-test-findings.md</automated>
  </verify>
  <done>Post-lockfile warning inventory captured; HOT warnings fixed or explicitly none</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify renv restore on clean Windows R session (DEV-01)</name>
  <read_first>
    - .planning/phases/06-reproducible-dev-environment/06-RESEARCH.md (renv Restore Instructions)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/renv.lock</files>
  <what-built>renv lockfile + scaffolding; warning baseline captured in smoke-test-findings.md</what-built>
  <how-to-verify>
    1. Open **new** Windows R 4.6+ session (not WSL R).
    2. setwd to GUImorphDevelopment/ on a normal Windows path (e.g. C:/dev/GUImorph/.../GUImorphDevelopment).
    3. Run renv::restore() — expect packages to install without fatal error.
    4. Run devtools::load_all(".") — expect success (warnings OK if classified DEFERRED).
    5. Confirm inst/libs/x64/tkogl2.dll exists; console shows tkogl2 path on load_all.
  </how-to-verify>
  <acceptance_criteria>
    - renv::restore() completes on clean Windows R session
    - devtools::load_all(".") succeeds after restore
    - Package count sane (nrow(installed.packages()) &gt; 10)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe restore/load failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRAN → renv library | External packages installed via renv::restore() |
| renv.lock → git | Lockfile committed; tampering affects all contributors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | renv.lock supply chain | mitigate | Use CRAN repos only; renv records package hashes; review lockfile diff on commit |
| T-6-02 | Tampering | Malicious R package postinstall | mitigate | Pin after smoke on known-good baseline; Package Legitimacy Audit approved CRAN packages only |
| T-6-03 | Information Disclosure | smoke-test-findings.md | accept | Local dev audit log; no secrets expected in warnings output |
| T-6-SC | Tampering | install.packages / renv restore | mitigate | RESEARCH Package Legitimacy Audit: renv, geomorph, rgl, RRPP all [OK] verdict |
</threat_model>

<verification>
- renv.lock + scaffold files exist and pass automated grep gates
- Phase 6 section appended to smoke-test-findings.md with warning classification
- Human confirms renv::restore() + load_all on clean Windows R session
</verification>

<success_criteria>
- DEV-01 satisfied: lockfile pins versions with documented Windows R restore path
- D-20 satisfied: first post-restore warning baseline captured
- D-19 satisfied: HOT warnings fixed only; DEFERRED documented
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Plan | Description |
|---------------|------|-------------|
| `GUImorphDevelopment/renv.lock` | 06-01 | Committed lockfile pinning Imports + workflow extras |
| `GUImorphDevelopment/.Rprofile` | 06-01 | Sources renv/activate.R on R startup |
| `GUImorphDevelopment/renv/activate.R` | 06-01 | renv library bootstrap (generated) |
| `GUImorphDevelopment/renv/settings.dcf` | 06-01 | renv settings (ignored.packages = devtools) |
| `GUImorphDevelopment/.Rbuildignore` | 06-01 | Updated with ^renv$ and ^renv\.lock$ |
| `.planning/smoke-test-findings.md` (Phase 6 section) | 06-01 | Warning baseline + HOT/DEFERRED triage |

<output>
Create `.planning/phases/06-reproducible-dev-environment/06-01-SUMMARY.md` when done
</output>
