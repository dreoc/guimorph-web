---
phase: 06-shell-and-native-retirement
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-digitize-curve-stripped.R
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "3dDigitize.digitize.r has no Tk widget builders (ui.digitize/ui.anchor), no tkgetOpenFile, no tk_chooseColor, and no engine verbs"
    - "3dDigitize.curve.r has no Tk widget builder (ui.curve) and no engine verbs; curve logic remains server-driven via the /curve route"
    - "Color selection is a browser <input type=color> stored over the Plan-01 /color route; the chosen hex still reaches the curve/anchor color state R-side"
    - "The Set Landmark Number / Set Anchor Number toplevels are removed; those counts are set via the browser shell"
  artifacts:
    - "Stripped digitize.r and curve.r with kept landmark/anchor/curve logic"
    - "Color-state wiring reading the /color route instead of tk_chooseColor"
    - "A source-scan test asserting both files are Tk/engine-free"
  key_links:
    - "tk_chooseColor (digitize.r:481,500) → /color route (Plan 01) → session color state"
    - "GUImorphWeb() no longer calls ui()/init() (Plan 03), so removing ui.digitize/ui.anchor/ui.curve is safe"
---

<objective>
Strip the Tk tab builders and dialogs from `R/3dDigitize.digitize.r` (~104 `tk*` calls) and
`R/3dDigitize.curve.r` (~12 `tk*` calls), keeping the landmark/anchor/curve DATA logic. Delete
`ui.digitize`, `ui.anchor`, `ui.curve`, the Set-Landmark-Number and Set-Anchor-Number toplevels,
the `tkgetOpenFile` sites, and the two `tcl('tk_chooseColor')` sites — replacing color selection
with the browser `<input type="color">` + Plan-01 `/color` route. Remove any `add`/`set`/`del`/`shows`
engine verbs in these files.

Purpose: UI-01 (Tk dialogs/tabs gone from these files) + UI-02 (no engine verbs). Output: two
stripped files, color-state wiring via `/color`, and a source-scan guard test.
</objective>

<wave3_coordination>
## Wave 3 parallel-execution contract (Plans 06-04, 06-05, 06-06)

Runs in parallel with 06-04 and 06-06; `files_modified` are disjoint by design. **This plan
exclusively owns `3dDigitize.digitize.r` and `3dDigitize.curve.r`** and edits no other source
file. Full ownership table + S3 rationale live in Plan 06-04's `<wave3_coordination>` block.

**S3 rule:** This plan removes the `ui.digitize`/`ui.anchor`/`ui.curve` tab builders and any
`*.main`/tab S3 methods that physically live **in these two files**. It does NOT touch the S3
**generic definitions** (`ui`/`init`/`bind`/`updateWidgets`) — those live in `3dDigitize.main.r`
and are owned by Plan 06-04. Because Plan 06-03 already removed the entry's `UseMethod` dispatch,
removing a method here while 06-04 concurrently removes the generic is safe and order-independent
(the orphaned method is inert). Do not read or edit `main.r`, `surface.r`, or `geomorph.r`.
</wave3_coordination>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shell-and-native-retirement/06-CONTEXT.md
@.planning/phases/06-shell-and-native-retirement/06-RESEARCH.md
@.planning/phases/06-shell-and-native-retirement/06-PATTERNS.md
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Tk builders + dialogs from 3dDigitize.digitize.r and wire color to /color</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r (read `ui.digitize` ~168, `ui.anchor` ~312, the `tkgetOpenFile` at ~902, the two `tcl('tk_chooseColor')` at ~481 and ~500, and the Set-Landmark-Number ~779 / Set-Anchor-Number ~840 toplevels)
    - .planning/phases/06-shell-and-native-retirement/06-PATTERNS.md (section "R/3dDigitize.{digitize,surface,curve,geomorph}.r — strip Tk tab builders"; the `/color` mapping)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Don't-Hand-Roll: color picker → `<input type=color>`; Tk Removal Surface (a)/(c))
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R (the `/color` route from Plan 01)
  </read_first>
  <action>
    Delete `ui.digitize`, `ui.anchor`, and the Set-Landmark-Number / Set-Anchor-Number Tk toplevels
    (the count values are now set from the browser shell). Delete the `tkgetOpenFile` site (opening is
    the Plan-01 `/files`+`/open` picker). Delete both `tcl('tk_chooseColor')` sites; the anchor/curve
    color is now the `#rrggbb` value the browser posts to `/color` (Plan 01) and R stores on the
    session — read the color from that session slot wherever the deleted `tk_chooseColor` result used
    to feed. Remove any `add`/`set`/`del`/`shows` engine-verb calls in this file. KEEP all landmark/
    anchor placement/data logic that is not Tk (it is already server-driven over `/pick`/`/anchor`).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' 3dDigitize.digitize.r | grep -c -E 'tkgetOpenFile|tk_chooseColor|tktoplevel|tkgrid|tkpack'` is 0.
    - Source assertion: no `add(`/`set(`/`del(`/`shows(` engine-verb call remains (comment-filtered).
    - Behavior: the color used for anchors/curves is read from the session color slot (fed by `/color`), not from a Tk dialog.
    - `devtools::load_all()` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::load_all(); invisible(TRUE)'</automated>
  </verify>
  <done>digitize.r is Tk/engine-free; color comes from `/color`; landmark/anchor logic intact; package loads.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Tk builder from 3dDigitize.curve.r, keep curve logic</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r (read `ui.curve` ~29 and the surrounding curve logic; identify any `*.main` S3 method and any engine verb)
    - .planning/phases/06-shell-and-native-retirement/06-PATTERNS.md (curve logic already server-side via `/curve`)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Tk Removal Surface (c))
  </read_first>
  <action>
    Delete `ui.curve` (its Tk widgets) and any Tk layout/`tclVar` scaffolding in this file. Remove any
    `add`/`set`/`del`/`shows` engine-verb calls. KEEP the curve definition/index logic — the browser
    already drives curves through the existing `/curve` route (Phase 5), so no new curve transport is
    needed. If any `ui.curve`-era `*.main` S3 method is defined in THIS file, remove it here (this plan
    owns `curve.r`); the generic DEFINITIONS in `main.r` are Plan 06-04's — do not edit `main.r`. Per
    `<wave3_coordination>`, removal is order-independent (the entry no longer `UseMethod`-dispatches).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' 3dDigitize.curve.r | grep -c -E 'ui\.curve|tktoplevel|tkgrid|tkpack|tk2'` is 0.
    - Source assertion: no engine-verb call remains (comment-filtered).
    - Behavior: curve placement still works over the existing `/curve` route (covered by curve tests).
    - `devtools::load_all()` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="curve")'</automated>
  </verify>
  <done>curve.r has no Tk builder or engine verbs; curve logic and `/curve` route behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add source-scan test that digitize.r and curve.r are Tk/engine-free</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-digitize-curve-stripped.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R (the `readLines`+`grepl` source-scan idiom + `skip_if_no_pkg_source()`)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/helper-pkg-source.R
  </read_first>
  <behavior>
    - Test (source-scan): `3dDigitize.digitize.r` has no `tkgetOpenFile`, no `tk_chooseColor`, no `tktoplevel`, and no engine-verb call in non-comment lines.
    - Test (source-scan): `3dDigitize.curve.r` has no `ui.curve`/`tktoplevel`/`tk2*` and no engine-verb call in non-comment lines.
  </behavior>
  <action>
    Create `test-digitize-curve-stripped.R` reusing the `readLines`+`grepl` idiom. Filter comment
    lines before counting so header prose cannot self-invalidate the negative gate (grep the source
    with `^\\s*#` removed). Guard with `skip_if_no_pkg_source()`.
  </action>
  <acceptance_criteria>
    - `devtools::test(filter="digitize-curve-stripped")` passes.
    - The negative gates reference the tokens by pattern, not as literal head-comment snippets, so they cannot self-trip.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="digitize-curve-stripped")'</automated>
  </verify>
  <done>The Tk/engine-free source-scan test for both files exists and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser `<input type=color>` → `/color` | Untrusted hex value crosses into R session state (validated server-side in Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-14 | Tampering | malformed color hex from `/color` | low | mitigate | Server-side `#rrggbb` validation (Plan 01 T-6-05); R stores only a validated hex |
| T-6-15 | Denial of service | over-strip removes kept curve/anchor logic | medium | mitigate | Keep placement/index logic; `curve` tests + `load_all()` gate each task |
| T-6-16 | Tampering | lingering engine verb | high | mitigate | Negative source-scan gate = 0 for `add`/`set`/`del`/`shows` (Pitfall 2) |
</threat_model>

<verification>
- `devtools::load_all()` clean; `devtools::test(filter="curve")` and `filter="digitize-curve-stripped"` green.
- Source-scan: zero Tk dialogs/builders + zero engine verbs across both files.
</verification>

<success_criteria>
`digitize.r` and `curve.r` carry only non-Tk landmark/anchor/curve logic; color is a browser
input persisted via `/color`; no engine verbs remain; source-scan test passes.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Deleted symbols:** `ui.digitize`, `ui.anchor` (digitize.r), `ui.curve` (curve.r), the
Set-Landmark-Number / Set-Anchor-Number toplevels, both `tk_chooseColor` sites, the `tkgetOpenFile`
site, and all `add`/`set`/`del`/`shows` engine-verb calls in both files.

**Rewired behavior:** anchor/curve color now read from the session color slot fed by the Plan-01 `/color` route.

**Kept (unchanged):** landmark/anchor placement + curve index/definition data logic (server-driven via `/pick`/`/anchor`/`/curve`).

**New file:** `tests/testthat/test-digitize-curve-stripped.R`.
</artifacts_produced>

<output>
Create `.planning/phases/06-shell-and-native-retirement/06-05-SUMMARY.md` when done.
</output>
