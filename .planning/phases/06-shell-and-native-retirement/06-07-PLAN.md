---
phase: 06-shell-and-native-retirement
plan: 07
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05", "06-06"]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/tkogl2.dylib
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/glut64.dll
  - integrated-guimorph-development_EOC/Project/tkogl2/
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-engine-absent.R
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "R/rtkogl.R is deleted; the native engine binaries and the sibling tkogl2/ build tree are gone from the repo"
    - "No .gmw_engine / .gmw_require_engine / .onLoad tcl-load / engine-verb definition survives anywhere in R/"
    - "The test-transport.R engine-PRESENCE assertions (:121–133) are rewritten to assert engine ABSENCE in the SAME change, so the suite never goes red"
    - "The package loads and the digitize→GPA→save workflow runs with inst/libs absent (UI-02)"
  artifacts:
    - "Deleted rtkogl.R, inst/libs/tkogl2.dylib, inst/libs/x64/tkogl2.dll, inst/libs/x64/glut64.dll, and the integrated-guimorph-development_EOC/Project/tkogl2/ tree"
    - "Rewritten test-transport.R :121–133 asserting absence"
    - "New test-engine-absent.R source-scan + workflow test"
  key_links:
    - "Survivors already relocated (Plan 03) + engine verbs already removed (Plans 04–06) → deletion is now safe (Landmine 1 / Pitfall 1/2)"
    - "Deletion + presence→absence test inversion happen in one plan so the suite is never left failing (Pitfall 6)"
---

<objective>
Physically remove the native engine now that the shell boots (Plan 03) and every engine-verb call
site is gone (Plans 04–06): delete `R/rtkogl.R` (its remaining engine content — `.gmw_engine`,
`.gmw_require_engine`, `.onLoad` tcl-load, `add`/`set`/`del`/`shows`, `get_rtkogl_date`, `loadDgt`),
the compiled binaries in `inst/libs/`, and the entire sibling `integrated-guimorph-development_EOC/
Project/tkogl2/` build tree (CMake/MSVC, C/ObjC backends, bundled Tcl/Tk headers, CMP-01 gate). In
the SAME change, rewrite the `test-transport.R` engine-PRESENCE assertions to assert ABSENCE so the
suite is never left red (Pitfall 6), and add a source-scan + workflow test proving engine-absence.

Purpose: UI-02 — the complete workflow runs with the native engine uninstalled and absent from the
library path. Output: the deletions, the inverted transport test, and `test-engine-absent.R`.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shell-and-native-retirement/06-CONTEXT.md
@.planning/phases/06-shell-and-native-retirement/06-RESEARCH.md
@.planning/phases/06-shell-and-native-retirement/06-PATTERNS.md
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete rtkogl.R, the engine binaries, and the sibling tkogl2/ build tree</name>
  <files>
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/tkogl2.dylib
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/glut64.dll
    integrated-guimorph-development_EOC/Project/tkogl2/
  </files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R (confirm it now contains ONLY engine content — `.gmw_engine`/`.gmw_require_engine`/`.onLoad`/`add`/`set`/`del`/`shows`/`get_rtkogl_date`/`loadDgt`/`bindPlatform*` — and that Plan 03 already moved `GUImorphWeb`/`dbg`/`.plot_show`/`.onAttach`/`.isMacOS` out)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Native Engine Removal Surface; tkogl2 Build-Tree Removal; Runtime State Inventory)
    - .planning/phases/06-shell-and-native-retirement/06-CONTEXT.md (D-02 engine deletion boundary — nothing archived in-tree)
  </read_first>
  <action>
    VERIFY first that `rtkogl.R` no longer defines any survivor (Plan 03 relocated them) — if any
    survivor is still here, STOP and relocate it before deleting. Then delete `R/rtkogl.R` entirely.
    Delete the shipped binaries `inst/libs/tkogl2.dylib`, `inst/libs/x64/tkogl2.dll`,
    `inst/libs/x64/glut64.dll` (and the now-empty `inst/libs/x64` and `inst/libs` dirs if empty).
    Delete the entire sibling directory `integrated-guimorph-development_EOC/Project/tkogl2/` (CMakeLists,
    cmake/, .sln/.vcxproj, src/, include/, third_party/, test/gate/ CMP-01 machinery, its stale sibling
    R/ copies, BUILD.md/docs/lib/deltas.txt). Nothing is archived in-tree (D-02). Use the file-delete
    tool for `rtkogl.R`; use a shell `rm`/`git rm` for the binaries and the tree.
  </action>
  <acceptance_criteria>
    - `test -e integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/rtkogl.R` is false.
    - No file matches `inst/libs/**/tkogl2*` or `inst/libs/**/glut64*`.
    - `test -d integrated-guimorph-development_EOC/Project/tkogl2` is false.
    - Behavior: `devtools::load_all()` succeeds with `rtkogl.R` gone (survivors resolve from `shell.R`).
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::load_all(); stopifnot(!file.exists(file.path("R","rtkogl.R")))'</automated>
  </verify>
  <done>rtkogl.R, the engine binaries, and the tkogl2/ tree are gone; the package still loads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite test-transport.R :121–133 to assert engine ABSENCE (same change as deletion)</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R (the block at :121–133 that currently asserts `rtkogl.R` still has `.onLoad`/`Tkogl2`)
    - .planning/phases/06-shell-and-native-retirement/06-PATTERNS.md (excerpt 121:133 and the "rewrite the second half to assert the engine is GONE" note)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Pitfall 6)
  </read_first>
  <behavior>
    - Test: `transport.R` still never references `.gmw_engine` (kept assertions).
    - Test: `R/rtkogl.R` does NOT exist (`file.exists(...)` is FALSE).
    - Test: no file under `inst/libs/` matches `tkogl2` or `glut64`.
    - Test: no file in `R/` contains `.onLoad` tcl-load of the engine, `.gmw_engine`, or `.gmw_require_engine` (non-comment lines).
  </behavior>
  <action>
    In the SAME plan/wave as the deletion (Task 1), rewrite the `test_that` block at :121–133: keep the
    first-half `transport.R` never-touches-`.gmw_engine` assertions; replace the second-half
    presence assertions (`expect_true(.onLoad)`, `expect_true(Tkogl2)`) with absence assertions —
    `expect_false(file.exists(file.path(pkg_root,"R","rtkogl.R")))`, no `inst/libs/tkogl2*`/`glut64*`,
    and no `.gmw_engine`/`.gmw_require_engine`/engine `.onLoad` anywhere in `R/`. Rename the test
    description to reflect the retirement. Reference forbidden tokens by pattern (not as literal
    head-comment snippets) so the negative gate cannot self-trip.
  </action>
  <acceptance_criteria>
    - `devtools::test(filter="transport")` passes with the rewritten block (no red left from the inversion).
    - The rewritten block would FAIL if `rtkogl.R` still existed (real absence gate).
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="transport")'</automated>
  </verify>
  <done>The :121–133 block asserts engine absence and passes; the suite is never left failing by the deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add test-engine-absent.R — no engine verbs anywhere + workflow runs engine-absent</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-engine-absent.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R (source-scan idiom) and helper-pkg-source.R / helper-transport.R
    - .planning/phases/06-shell-and-native-retirement/06-VALIDATION.md (Wave 0 rows for UI-02 source-scan + workflow integration)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Validation Architecture → Test Map, UI-02 rows)
  </read_first>
  <behavior>
    - Test (source-scan): no file in `R/` contains an `add(`/`set(`/`del(`/`shows(` engine-verb call, and none defines `.gmw_engine`/`.gmw_require_engine` (non-comment lines).
    - Test (integration): booting the shell in a temp dir and driving PLY-load → landmark → GPA → save over routes completes with no engine present (inst/libs already deleted).
  </behavior>
  <action>
    Create `test-engine-absent.R`. Add the R/-wide source-scan (comment-filtered) for engine verbs and
    engine env symbols, guarded by `skip_if_no_pkg_source()`. Add an integration block that boots the
    shell (`GUImorphWeb(dir=<tempdir>, open=FALSE)`) and exercises the digitize→gpa→save routes,
    asserting success — this is the UI-02 workflow-engine-absent proof. Reference forbidden tokens by
    pattern, not literal head-comment snippets.
  </action>
  <acceptance_criteria>
    - `devtools::test(filter="engine-absent")` passes.
    - The source-scan would FAIL if any `add`/`set`/`del`/`shows` engine call remained in R/.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="engine-absent")'</automated>
  </verify>
  <done>The engine-absence source-scan + engine-absent workflow test exists and passes (UI-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → installed package | A previously installed 0.10.0 in a user library still contains the engine until reinstall — addressed by the D-06/D-07 pin note (Plan 08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-20 | Denial of service | deleting rtkogl.R decapitates the package | high | mitigate | Task 1 verifies survivors already moved to `shell.R` (Plan 03) before deleting; `load_all()` gate |
| T-6-21 | Tampering | stale binary lingers in a hand-copied libs/ | medium | mitigate | Delete all `inst/libs/tkogl2*`/`glut64*`; source-scan + reinstall guidance (Plan 08 pin note) |
| T-6-22 | Repudiation | suite left red by the presence→absence inversion | medium | mitigate | Rewrite `test-transport.R` :121–133 in the SAME plan as deletion (Pitfall 6) |
| T-6-SC | Tampering | dependency installs | n/a | accept | Removal-only phase; no installs; no legitimacy gate required |
</threat_model>

<verification>
- `devtools::load_all()` clean; `devtools::test(filter="transport")` and `filter="engine-absent")` green.
- Filesystem: `rtkogl.R`, `inst/libs/tkogl2*`, `inst/libs/x64/*.dll`, and `tkogl2/` tree are gone.
- Per-wave: full `devtools::test()` after this wave (expect some CMP-01/parity tests still to be reconciled in Plan 08).
</verification>

<success_criteria>
The native engine (in-package binding, binaries, and build tree) is physically gone; the package
loads and the workflow runs engine-absent; the transport suite asserts absence and stays green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Deleted files:** `R/rtkogl.R`; `inst/libs/tkogl2.dylib`, `inst/libs/x64/tkogl2.dll`,
`inst/libs/x64/glut64.dll`; the entire `integrated-guimorph-development_EOC/Project/tkogl2/` tree
(incl. `test/gate/` CMP-01 machinery and stale sibling R/ copies).

**Deleted symbols:** `.gmw_engine`, `.gmw_require_engine`, `.onLoad` (engine tcl-load), `add`, `set`,
`del`, `shows`, `get_rtkogl_date`, `loadDgt`, `bindPlatformAccelerator`, `bindDeleteGesture`.

**New/rewritten files:** `tests/testthat/test-transport.R` (:121–133 inverted to absence);
`tests/testthat/test-engine-absent.R` (source-scan + engine-absent workflow).

**Note:** `export(loadDgt)` is dropped from NAMESPACE in Plan 08 (regen) since `loadDgt` is deleted here.
</artifacts_produced>

<output>
Create `.planning/phases/06-shell-and-native-retirement/06-07-SUMMARY.md` when done.
</output>
