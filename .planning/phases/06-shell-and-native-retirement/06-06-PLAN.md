---
phase: 06-shell-and-native-retirement
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-geomorph-stripped.R
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "3dDigitize.surface.r has no ui.surface Tk builder, no tkmessageBox, no Set-Number-of-Surface-Sliders toplevel, and no engine verbs; downsample/template logic intact"
    - "3dDigitize.geomorph.r has no ui.geomorph tk2* widgets, no tkmessageBox, no tkgetSaveFile, and no engine verbs; GPA/PCA/export logic intact"
    - "GPA options (previously tclVar-backed tk2checkbutton) are carried to R as bare-CSV boolean flags over a route so .build_geomorph_data/compute stay untouched"
    - "Save-name for GPA export uses the browser save-name field + Plan-01 /savepath; R still owns the directory (/save carries no path)"
  artifacts:
    - "Stripped surface.r and geomorph.r"
    - "GPA-options transport branch on /gpa (or /gpaopts) carrying boolean flags"
    - "A source-scan test asserting both files are Tk/engine-free"
  key_links:
    - "GPA option flags → route body → gpagen option tclVars via .gmw_session_to_geomorph_env (Phase 5), compute() forwarding untouched"
    - "tkgetSaveFile (geomorph.r:314,368) → /savepath (Plan 01), R owns the path"
---

<objective>
Strip the Tk tab builders and dialogs from `R/3dDigitize.surface.r` (~79 `tk*` calls) and
`R/3dDigitize.geomorph.r` (~131 `tk*`/`tk2*` calls) while keeping the downsample/template logic
and the GPA/PCA/export logic. Delete `ui.surface` (+ the Set-Number-of-Surface-Sliders toplevel)
and its `tkmessageBox` sites; delete `ui.geomorph` (the `tk2label`/`tk2entry`/`tk2spinbox`/
`tk2checkbutton` widgets), its `tkmessageBox` and `tkgetSaveFile` sites. Carry the GPA option flags
to R as a bare-CSV body over the `/gpa` route (extend it, or add `/gpaopts`) so
`.build_geomorph_data`/`compute` remain untouched (Phase 5). Route the GPA save-name through the
Plan-01 `/savepath` (R owns the directory). Remove any `add`/`set`/`del`/`shows` engine verbs.

Purpose: UI-01 (Tk dialogs/tabs gone) + UI-02 (no engine verbs) + preserve DGT-03 GPA/export
behavior. Output: two stripped files, a GPA-options route branch, and a source-scan guard test.

**Resolves RESEARCH Open Question 2 (GPA-options transport):** DECIDED here — the boolean GPA
option flags travel as a bare CSV over the `/gpa` (or new `/gpaopts`) route into the gpagen
option tclVars via the Phase-5 `.gmw_session_to_geomorph_env` path, leaving
`.build_geomorph_data`/`compute` byte-unchanged. This is no longer an open question.
</objective>

<wave3_coordination>
## Wave 3 parallel-execution contract (Plans 06-04, 06-05, 06-06)

Runs in parallel with 06-04 and 06-05; `files_modified` are disjoint by design. **This plan
exclusively owns `3dDigitize.surface.r`, `3dDigitize.geomorph.r`, and the Wave-3 edit to
`transport.R`** (the GPA-options route branch). No other Wave-3 plan edits `transport.R` in this
wave (06-01's edit was Wave 1, a prior wave — no concurrency). Full ownership table + S3
rationale live in Plan 06-04's `<wave3_coordination>` block.

**S3 rule:** This plan removes `ui.surface`/`ui.geomorph` and any `*.main`/tab S3 methods that
physically live **in these two digitizing files**. It does NOT touch the S3 **generic
definitions** (`ui`/`init`/`bind`/`updateWidgets`) in `3dDigitize.main.r` (owned by Plan 06-04).
Because Plan 06-03 already removed the entry's `UseMethod` dispatch, removing a method here while
06-04 concurrently removes the generic is safe and order-independent. Do not read or edit
`main.r`, `digitize.r`, or `curve.r`.
</wave3_coordination>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shell-and-native-retirement/06-CONTEXT.md
@.planning/phases/06-shell-and-native-retirement/06-RESEARCH.md
@.planning/phases/06-shell-and-native-retirement/06-PATTERNS.md
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
@integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Tk builder + dialogs from 3dDigitize.surface.r, keep downsample/template logic</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r (read `ui.surface` ~35, the Set-Number-of-Surface-Sliders toplevel ~160, and the `tkmessageBox` sites ~327, ~507, ~562)
    - .planning/phases/06-shell-and-native-retirement/06-PATTERNS.md (message box → reusable DOM modal; Tk Removal Surface (c))
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (surface flattening note — do NOT touch the `as.vector(t(...))` flatten logic)
  </read_first>
  <action>
    Delete `ui.surface` and its Tk widgets, the Set-Number-of-Surface-Sliders toplevel (slider count
    now set from the browser shell), and the three `tkmessageBox` sites (conditions now surface through
    the browser message-box modal / `/msgack`, or the function returns the message text for the shell to
    render). Remove any `add`/`set`/`del`/`shows` engine verbs. KEEP the downsample/template logic —
    especially the surface-semilandmark `as.vector(t(...))` flatten (covered by `test-surface-flatten.R`);
    do not alter it.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' 3dDigitize.surface.r | grep -c -E 'ui\.surface|tkmessageBox|tktoplevel|tkgrid|tkpack'` is 0.
    - Source assertion: no engine-verb call remains (comment-filtered).
    - Behavior: `devtools::test(filter="surface-flatten")` still passes (flatten logic untouched).
    - `devtools::load_all()` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="surface-flatten")'</automated>
  </verify>
  <done>surface.r has no Tk builder/dialogs/engine verbs; downsample/template + flatten logic intact.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Tk builder + dialogs from 3dDigitize.geomorph.r and carry GPA options over a route</name>
  <files>
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
    integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R
  </files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (read `ui.geomorph` ~25 and the `tk2label`/`tk2entry`/`tk2spinbox`/`tk2checkbutton` GPA-option widgets ~52–110, the `tkmessageBox` sites ~199/226/254/303/408/414/429/615/627, and the `tkgetSaveFile` sites ~314/368)
    - .planning/phases/06-shell-and-native-retirement/06-PATTERNS.md (GPA options → `/gpa`/`/gpaopts`; `tkgetSaveFile` → `/savepath`)
    - .planning/phases/06-shell-and-native-retirement/06-RESEARCH.md (Open Q2: GPA options transport; Phase-5 `.gmw_session_to_geomorph_env` + gpagen option tclVars)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/transport.R (existing `/gpa` and `/export`/`/save` branches; Phase-5 `.gmw_session_to_geomorph_env`)
  </read_first>
  <action>
    Delete `ui.geomorph` (all `tk2*` widgets), the `tkmessageBox` sites (→ browser modal / `/msgack`
    or returned text), and the `tkgetSaveFile` sites (→ Plan-01 `/savepath`; R owns the directory).
    Replace the `tclVar`-backed GPA option checkbuttons with a route contract: extend the existing
    `/gpa` branch (or add a `/gpaopts` branch) in `transport.R` to accept the boolean GPA flags as a
    bare CSV, and feed them into the gpagen option tclVars via the Phase-5 `.gmw_session_to_geomorph_env`
    path so `.build_geomorph_data`/`compute` forwarding stays UNTOUCHED (DGT-03 parity preserved).
    Remove any `add`/`set`/`del`/`shows` engine verbs. KEEP all GPA/PCA/export computation logic.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' 3dDigitize.geomorph.r | grep -c -E 'ui\.geomorph|tk2label|tk2entry|tk2spinbox|tk2checkbutton|tkmessageBox|tkgetSaveFile'` is 0.
    - Source assertion: no engine-verb call remains (comment-filtered).
    - Source assertion: `.build_geomorph_data`/`compute` forwarding is byte-unchanged (GPA option flags reach it via the session env, not new arguments).
    - Behavior: `devtools::test(filter="gpa-parity")` and `filter="export-parity"` still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="gpa-parity"); devtools::test(filter="export-parity")'</automated>
  </verify>
  <done>geomorph.r is Tk/engine-free; GPA options travel over a route into the unchanged compute path; GPA/export parity holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add source-scan test that surface.r and geomorph.r are Tk/engine-free</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-surface-geomorph-stripped.R</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/test-transport.R (the `readLines`+`grepl` source-scan idiom + `skip_if_no_pkg_source()`)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/tests/testthat/helper-pkg-source.R
  </read_first>
  <behavior>
    - Test (source-scan): `3dDigitize.surface.r` has no `ui.surface`/`tkmessageBox`/`tktoplevel` and no engine verb (non-comment lines).
    - Test (source-scan): `3dDigitize.geomorph.r` has no `ui.geomorph`/`tk2*`/`tkmessageBox`/`tkgetSaveFile` and no engine verb (non-comment lines).
    - Test: `transport.R` has a `/gpa`-family branch that accepts GPA option flags (a `grepl("/gpa"` route exists).
  </behavior>
  <action>
    Create `test-surface-geomorph-stripped.R` reusing the `readLines`+`grepl` idiom, comment-filtered,
    guarded by `skip_if_no_pkg_source()`. Reference the forbidden tokens by pattern, not as literal
    head-comment snippets, so the negative gates cannot self-trip.
  </action>
  <acceptance_criteria>
    - `devtools::test(filter="surface-geomorph-stripped")` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript --no-init-file -e 'devtools::test(filter="surface-geomorph-stripped")'</automated>
  </verify>
  <done>The Tk/engine-free source-scan test for both files exists and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser GPA tab → `/gpa`/`/gpaopts` | Untrusted boolean-flag CSV crosses into R GPA options |
| browser save-name → `/savepath` | Untrusted save-name string; R owns the directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-17 | Tampering | GPA-option CSV injection | medium | mitigate | Base-R parse to strict booleans; fixed option set; no `eval`; flags feed existing gpagen option tclVars only |
| T-6-18 | Tampering | save-name path traversal via `/savepath` | high | mitigate | R owns the directory and joins only a sanitized basename; `/save` still carries no path (Phase-5 invariant) |
| T-6-19 | Denial of service | over-strip breaks GPA/export/flatten | high | mitigate | Keep compute/export/flatten logic byte-unchanged; `gpa-parity`/`export-parity`/`surface-flatten` gate each task |
</threat_model>

<verification>
- `devtools::load_all()` clean; `gpa-parity`, `export-parity`, `surface-flatten`, `surface-geomorph-stripped` green.
- Source-scan: zero Tk builders/dialogs + zero engine verbs across both files; `.build_geomorph_data`/`compute` unchanged.
</verification>

<success_criteria>
`surface.r` and `geomorph.r` carry only non-Tk analytical logic; GPA options travel over a route
into the unchanged compute path; save-name uses `/savepath`; no engine verbs remain; parity tests hold.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Deleted symbols:** `ui.surface` + Set-Number-of-Surface-Sliders toplevel (surface.r); `ui.geomorph`
+ its `tk2*` GPA-option widgets (geomorph.r); all `tkmessageBox`, `tkgetSaveFile`, and
`add`/`set`/`del`/`shows` sites in both files.

**New/extended behavior (`transport.R`):** GPA option flags accepted over `/gpa` (or new `/gpaopts`)
as bare CSV, fed to gpagen option tclVars via Phase-5 `.gmw_session_to_geomorph_env`.

**Kept (unchanged):** downsample/template + `as.vector(t(...))` flatten (surface.r); GPA/PCA/export
compute + `.build_geomorph_data`/`compute` forwarding (geomorph.r).

**New file:** `tests/testthat/test-surface-geomorph-stripped.R`.
</artifacts_produced>

<output>
Create `.planning/phases/06-shell-and-native-retirement/06-06-SUMMARY.md` when done.
</output>
