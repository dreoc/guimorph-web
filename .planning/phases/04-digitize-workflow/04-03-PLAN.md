---
phase: 04-digitize-workflow
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
  - .planning/smoke-test-findings.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DGT-03
  - DGT-04
user_setup: []

must_haves:
  truths:
    - "User saves a multi-specimen .dgt file containing Curve=1, LM3= sections for two distinct PLY specimens"
    - "Same-session GUI reload via File → Load DGT File restores landmarks on both specimens"
    - "Reloaded curves visible on Curves tab and persisted in activeDataList[[1]][[4]] after drawElements"
    - "Re-save after reload writes Curve=1 (non-zero) — no silent curve data loss"
    - "load_all warnings captured in smoke-test-findings.md (D-10)"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r"
      provides: "drawElements restores curves into activeDataList[[1]][[4]]"
      contains: "dgtDataList[[1]][[4]] <- curves"
    - path: ".planning/smoke-test-findings.md"
      provides: "Phase 4 save/reload section with warnings capture"
      min_lines: 30
  key_links:
    - from: "saveToDgt"
      to: ".dgt file"
      via: "write.curve then per-specimen write.digitize loop"
      pattern: "write\\.curve\\(fileName, curves\\)"
    - from: "openDgt"
      to: "drawElements"
      via: "read.curve → e$dgtcurvestuff + drawElements(..., curves, ...)"
      pattern: "drawElements\\(e, olddat, surfaceData, curves, anchors\\)"
    - from: "drawElements"
      to: "activeDataList[[1]][[4]]"
      via: "curve restore block assigns matrix after draw.curves"
      pattern: "dgtDataList\\[\\[1\\]\\]\\[\\[4\\]\\] <- curves"
---

<objective>
Fix the drawElements curve-restore blocker and validate multi-specimen `.dgt` save/reload round-trip in the same GUImorph session.

Purpose: DGT-03/DGT-04 require File → Save to DGT and File → Load DGT File to persist landmarks + curves across two specimens (D-05, D-08, D-16). Research flagged `drawElements` curve assignment wrapped in `if(0)` (~lines 1558-1586) causing reload data loss — fix per D-12 before round-trip UAT.
Output: Working save/reload, re-save preserves curves, warnings captured, DGT-03/DGT-04 validated, Phase 4 save/reload findings appended.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-digitize-workflow/04-CONTEXT.md
@.planning/phases/04-digitize-workflow/04-RESEARCH.md
@.planning/phases/04-digitize-workflow/04-PATTERNS.md
@.planning/phases/04-digitize-workflow/04-02-SUMMARY.md
@.planning/smoke-test-findings.md

<interfaces>
<!-- Save/reload contracts — executor uses directly -->

saveToDgt order (3dDigitize.main.r ~1648-1703):
1. `write.curve(fileName, e$activeDataList[[1]][[4]])` — Curve=N header + ID rows
2. `write.template(fileName, e$templOrig)`
3. Per specimen i: `write.digitize`, `write.anchors`, `write.surface` (emits Surface=0 when empty)

openDgt path (D-06 — GUI only, ~2458-2719):
1. `init.main(e)` resets session (same-session reload D-09)
2. Parse: read.digitize → read.anchors → read.surface → read.curve
3. `e$dgtcurvestuff <- curves` for switchTab id==3 transient display
4. `drawElements(e, olddat, surfaceData, curves, anchors)` — MUST assign curves to activeDataList[[1]][[4]]

drawElements FIX TARGETS (~1527-1588):
- Specimen loop slot 4: change `list()` to `matrix(nrow = 0, ncol = 3)` at ~line 1532
- Curve restore block at ~1558: change `if(0)` to active guard; when `!is.null(curves) && nrow(curves) > 0`: call `draw.curves(curves)`, `add("InfoCurves", ...)`, loop `add("curve", ...)`, `add("InfoCurves_complete", ...)`, assign `dgtDataList[[1]][[4]] <- curves` — mirror switchTab id==3 block (~340-388) and disabled if(0) block content

Multi-specimen test (D-16): C13.1.ply + second Folsom PLY (e.g. A6.1.ply or B7.1.ply from same collection). Save .dgt to directory where PLY basenames resolve on reload (same folder or valid e$dgtPath).

Local .dgt path example: save beside meshes; do NOT commit file (D-17).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-enable drawElements curve restore and fix slot [[4]] init on reload</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r lines 1527-1588 (drawElements specimen loop + if(0) curve block)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r lines 331-388 (switchTab id==3 curve C-dispatch reference)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r draw.curves function
    - .planning/phases/04-digitize-workflow/04-PATTERNS.md "drawElements — curve restore DISABLED"
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r</files>
  <action>
    Per D-12: fix silent curve data loss on openDgt reload path.

    1. In `drawElements` specimen-loop `dgtDataList[[specId]] <- list(...)` (~line 1532), replace 4th element `list()` with `matrix(nrow = 0, ncol = 3)` (same shape as loadPly fix from 04-02).

    2. Re-enable the curve restore block currently guarded by `if(0)` (~lines 1558-1586):
       - Replace `if(0)` with `if (!is.null(curves) && length(curves) > 0 && nrow(curves) > 0)` (or equivalent null-safe check matching read.curve NULL for Curve=0).
       - Inside block: keep existing logic — `add("InfoCurves", nrow(curves), ncol(curves), nSpecimens)`, `draw.curves(curves)`, `dgtDataList[[1]][[4]] <- curves`.
       - Ensure `e$activeDataList <- dgtDataList` assignment (~1588) runs AFTER curve slot is populated.

    3. Also set `e$dgtcurvestuff <- curves` when curves non-null (if not already set by openDgt) so switchTab id==3 restore remains consistent.

    Do NOT validate C-side loadDgt Tcl command separately (D-06 deferred). Do NOT remove debug print() (D-11). Do NOT change openDgt NULL surface abort unless fresh save fails reload (Pitfall 4 — fresh saveToDgt should emit Surface=0).
  </action>
  <acceptance_criteria>
    - drawElements no longer has `if(0)` wrapping curve restore
    - `dgtDataList[[1]][[4]] <- curves` executes when curves matrix non-empty
    - Specimen loop slot [[4]] init is matrix not list()
    - load_all succeeds after edit
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r | grep -c 'dgtDataList\[\[1\]\]\[\[4\]\] <- curves' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>awk '/^drawElements\s*<-|^drawElements\s*=/{f=1} f && /^[a-zA-Z]/ && !/^drawElements/{f=0} f && /if\s*\(\s*0\s*\)/{exit 1} END{exit 0}' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript -e "devtools::load_all('.'); cat('load_all OK\n')"</automated>
  </verify>
  <done>drawElements persists parsed curves into activeDataList[[1]][[4]] on reload</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Multi-specimen save to .dgt (DGT-03)</name>
  <read_first>
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md D-05, D-08, D-14, D-15, D-16, D-17
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r saveToDgt (lines 1618-1703), onNext navigation (lines 869-927)
    - .planning/phases/04-digitize-workflow/04-02-SUMMARY.md (curve workflow baseline)
  </read_first>
  <files></files>
  <what-built>drawElements curve restore fix from Task 1; 04-02 curve bind validated</what-built>
  <how-to-verify>
    1. Fresh Windows R session: `setwd(.../GUImorphDevelopment)`; `devtools::load_all(".")`; `warnings()` — capture output for Task 3 (D-10); `GUImorph()`
    2. File → Load ply File → multi-select **two distinct** Folsom PLYs: `C13.1.ply` + one other (e.g. `A6.1.ply` or `B7.1.ply`) per D-16
    3. **Specimen 1 (Digitize tab):** Set landmark count = 3; double-click 3 landmarks; define ≥1 curve on Curves tab (repeat 04-02 steps)
    4. **Digitize tab → Next** to advance to specimen 2; set landmark count = 3; double-click 3 landmarks on specimen 2 (curve on specimen 1 only is sufficient for D-05)
    5. File → **Save to DGT** → save to `zips/Folsom 3D models/` or same directory as PLYs (path resolution for reload — D-17 local only, do not commit)
    6. Inspect saved file (PowerShell or grep):
       - `grep '^Curve=' saved.dgt` → expect `Curve=1`
       - Three integer IDs on line after Curve= header
       - Two `LM3=3` sections (one per specimen)
       - Two `ID=` lines with basenames `C13.1.ply` and second specimen
       - `Surface=0` present (avoids openDgt NULL surface abort)
    7. Console should show `Writing curve data` with non-empty matrix and `Writing data for : 2 specimens`
  </how-to-verify>
  <acceptance_criteria>
    - saveToDgt completes for 2-specimen session without error dialog
    - File contains Curve=1 with 3 integer landmark IDs
    - Both specimens have LM3= and ID= sections
    - Saved file NOT committed to git (D-17)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — multi-specimen GUI save requires Windows R manual UAT</automated>
  </verify>
  <resume-signal>Type "approved" with grep output for Curve= and specimen count, or describe failure</resume-signal>
  <done>DGT-03 multi-specimen .dgt save validated with landmarks + curves serialized</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Same-session reload round-trip and re-save (DGT-04)</name>
  <read_first>
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md D-06, D-07, D-09, D-13
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r openDgt (lines 2458-2719), drawElements
    - .planning/smoke-test-findings.md (append Phase 4 save/reload section)
  </read_first>
  <files>.planning/smoke-test-findings.md, .planning/REQUIREMENTS.md</files>
  <what-built>Task 1 drawElements fix + Task 2 saved .dgt file on disk</what-built>
  <how-to-verify>
    **Same GUImorph/R session (D-09 — do NOT quit R):**
    1. File → **Load DGT File** → select the .dgt saved in Task 2
    2. Verify landmarks visible on mesh for specimen 1 (and switch specimens if UI exposes navigation)
    3. Switch to **Curves** tab — curve display restored (visual spline/line on mesh)
    4. R console: `e$activeDataList[[1]][[4]]` — expect 1×3 integer matrix matching saved IDs
    5. File → **Save to DGT** → save as new local filename (e.g. `*_resave.dgt`)
    6. `grep '^Curve=' *_resave.dgt` → must still show **Curve=1** (guards Pitfall 3 / if(0) data loss)
    7. If anchors were placed during session (optional D-07), confirm they restore on reload

    **Documentation (same task after UAT pass):**
    8. Append **Phase 4 — Save/Reload (04-03)** section to smoke-test-findings.md (D-13): specimens used, save path, grep evidence, reload/re-save results, path resolution notes
    9. Log `warnings()` capture from Task 2 step 1 (D-10) — count and summary; fix only if warning blocked save/reload
    10. Update REQUIREMENTS.md: mark **DGT-03** and **DGT-04** complete with date

    Cold restart reload is out of scope (deferred). C-side loadDgt standalone validation out of scope (D-06).
  </how-to-verify>
  <acceptance_criteria>
    - openDgt via GUI restores landmarks on loaded specimens
    - Curves visible on Curves tab AND activeDataList[[1]][[4]] populated after reload
    - Re-save writes Curve=1 (non-zero) — no silent curve loss
    - smoke-test-findings.md updated with save/reload + warnings sections
    - DGT-03 and DGT-04 marked complete in REQUIREMENTS.md
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 4.*Save' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep 'DGT-03\|DGT-04' .planning/REQUIREMENTS.md | grep -c '✓\|✅\|\[x\]' | awk '{exit ($1 >= 2) ? 0 : 1}'</automated>
  </verify>
  <resume-signal>Type "approved" with reload + re-save grep evidence, or describe failure step</resume-signal>
  <done>DGT-04 same-session round-trip validated; requirements and findings updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → openDgt | Untrusted .dgt content parsed by read.* functions; numeric row counts drive matrix dimensions |
| .dgt → specimen PLY paths | read.digitize joins e$dgtPath when ID lacks path separator |
| drawElements → C engine | Restored curve IDs must reference landmarks loaded by draw.digitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03-01 | Tampering | Malicious .dgt path injection | mitigate | Validation uses trusted local files from known Folsom directory (D-14–D-16); read.digitize existing path-join guard |
| T-4-03-02 | Denial of Service | Oversized .dgt scan() load | accept | Research-scale files only; note for future hardening |
| T-4-03-03 | Tampering | drawElements if(0) curve drop | mitigate | Task 1: re-enable curve assignment to activeDataList[[1]][[4]] |
| T-4-03-04 | Spoofing | UNC/path trickery on reload | mitigate | Save .dgt beside PLY meshes; drawElements file.exists check skips missing specimens |
| T-4-03-SC | Tampering | R source edits | mitigate | load_all verify; human round-trip UAT confirms no landmark regression |
</threat_model>

<verification>
- Task 1: grep confirms curve assignment line active; load_all passes
- Task 2: saved .dgt contains Curve=1 + 2-specimen LM3= sections
- Task 3: reload restores curves; re-save preserves Curve=1; REQUIREMENTS.md updated
- Warnings captured per D-10 (fix only if blocking)
</verification>

<success_criteria>
- Multi-specimen .dgt saves with landmarks and ≥1 curve (DGT-03)
- Same-session GUI reload restores landmarks and curves (DGT-04)
- Re-save after reload does not zero out curve section
- Phase 4 save/reload findings and warnings logged in smoke-test-findings.md
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Purpose |
|----------|------|---------|
| drawElements curve restore | `R/3dDigitize.main.r` | Persists read.curve output into activeDataList on reload |
| drawElements slot init | `R/3dDigitize.main.r` | Matrix init for slot [[4]] in reload path |
| Phase 4 save/reload smoke section | `.planning/smoke-test-findings.md` | D-13 validation + D-10 warnings capture |
| Requirement traceability | `.planning/REQUIREMENTS.md` | DGT-03, DGT-04 marked complete |
| Local test .dgt files | User machine only (D-17) | Not committed — machine-specific paths |

<output>
Create `.planning/phases/04-digitize-workflow/04-03-SUMMARY.md` when done
</output>
