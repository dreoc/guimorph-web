---
phase: 04-digitize-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r
  - .planning/smoke-test-findings.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DGT-02
user_setup: []

must_haves:
  truths:
    - "User can place 3 landmarks, unlock Curves tab, and bind 1 curve via triple double-click on existing landmark dots"
    - "activeDataList[[1]][[4]] holds a 1×3 integer matrix of landmark IDs after curve bind"
    - "Fit button on Curve tab runs without crash (view reset only — per D-04)"
    - "Curve workflow quirks (landmark count, double-click selection) documented in smoke-test-findings.md"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r"
      provides: "loadPly initializes curve slot [[4]] as empty 3-column matrix"
      contains: "matrix(nrow = 0, ncol = 3)"
    - path: ".planning/smoke-test-findings.md"
      provides: "Phase 4 curve validation section (append-only per D-13)"
      min_lines: 20
  key_links:
    - from: "bind.curve → onSelectCurve"
      to: "activeDataList[[1]][[4]]"
      via: "rbind on 3rd double-click selection"
      pattern: "rbind\\(curves, newCurve\\)"
    - from: "onSelectCurve"
      to: "C curve state"
      via: "add(\"curve\", id1, id2, id3)"
      pattern: "add\\(\"curve\""
    - from: "updateDotNum"
      to: "Curves tab unlock"
      via: "nDots == e$landmarkNum enables tabState[3]"
      pattern: "tabState\\[3\\]"
---

<objective>
Fix the curve-slot initialization blocker and validate the legacy curve definition workflow end-to-end on Windows R.

Purpose: DGT-02 requires defining at least one curve (3 landmark IDs) using the existing Curve tab workflow (D-01–D-03). DGT-01 satisfied by completed 04-01 (double-click landmark placement) — used as prerequisite only, not re-validated here. Research flagged `activeDataList[[1]][[4]] <- list()` in `loadPly` as incompatible with `onSelectCurve`'s `rbind()` — fix proactively per D-12 before UAT.
Output: Working first-curve bind, Fit smoke pass, Phase 4 curve findings appended to smoke-test-findings.md, DGT-02 marked validated in REQUIREMENTS.md.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-digitize-workflow/04-CONTEXT.md
@.planning/phases/04-digitize-workflow/04-RESEARCH.md
@.planning/phases/04-digitize-workflow/04-PATTERNS.md
@.planning/smoke-test-findings.md

<interfaces>
<!-- Curve workflow contracts — executor uses directly -->

activeDataList slot map (specimen 1 curves at [[1]][[4]]):
- `[[1]]` specimen path, `[[3]]` landmark count placed, `[[4]]` curve matrix (nrow × 3 landmark IDs)

loadPly init (3dDigitize.main.r ~1265-1266) — FIX TARGET:
- Replace 4th list element `list()` with `matrix(nrow = 0, ncol = 3)`

onSelectCurve 3rd-selection commit (3dDigitize.curve.r ~260-289):
- `curves <- e$activeDataList[[1]][[4]]`
- `newCurve <- matrix(e$curveLine, nrow = 1, ncol = 3)`
- `curves <- rbind(curves, newCurve)` then `add("curve", id1, id2, id3)`

Tab unlock (3dDigitize.digitize.r updateDotNum ~1009-1021):
- Curves tab (index 3) enables when `nDots == e$landmarkNum`
- Default `e$landmarkNum <- 5` in init.digitize — UAT must set count to 3 first (D-03)

Legacy-only UI (D-01): Fit button packed; Set curves number / Compute Curves remain commented out in ui.curve.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix curve slot [[4]] initialization for rbind compatibility</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r lines 1264-1267 (loadPly dgtDataList init)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r lines 260-289 (onSelectCurve rbind commit)
    - .planning/phases/04-digitize-workflow/04-PATTERNS.md "loadPly — multi-specimen init (BUG SITE)"
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r</files>
  <action>
    Per D-12: fix digitize-path blocker before curve UAT.

    In `loadPly`, at the `dgtDataList[[length(dgtDataList) + 1]] <- list(...)` assignment (~line 1266), replace the 4th element `list()` with `matrix(nrow = 0, ncol = 3)`. Keep all other slot positions unchanged (path, font scale, landmark count 0, template "NULL", rotation, zoom, surface "NULL", anchor count 0).

    Do NOT enable dormant curve UI buttons in ui.curve (D-01). Do NOT remove debug print() statements (D-11).

    If runtime UAT in Task 2 still hits rbind errors on a non-matrix slot, add a minimal guard at the top of `onSelectCurve`'s 3rd-selection branch in 3dDigitize.curve.r: when `is.list(curves) || length(curves) == 0 && !is.matrix(curves)`, coerce to `matrix(nrow = 0, ncol = 3)` before rbind — mirror list→matrix pattern from 3dDigitize.geomorph.r lines 136-144 only if needed.
  </action>
  <acceptance_criteria>
    - loadPly slot [[4]] is `matrix(nrow = 0, ncol = 3)`, not `list()`
    - No other loadPly slot indices changed
    - Package loads without fatal error after edit
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.main.r | grep -c 'matrix(nrow = 0, ncol = 3)' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript -e "devtools::load_all('.'); cat('load_all OK\n')"</automated>
  </verify>
  <done>loadPly initializes curve slot as empty 3-column matrix; load_all succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate legacy curve workflow on C13.1.ply (DGT-02)</name>
  <read_first>
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md decisions D-01 through D-04, D-14
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.curve.r bind.curve (lines 91-104), ui.curve Fit button (lines 51-57)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.digitize.r init.digitize (line 35), updateDotNum (lines 1009-1021)
    - .planning/smoke-test-findings.md Correction section (double-click placement baseline from 04-01 / DGT-01)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <what-built>Curve slot init fix from Task 1; ready for Windows R GUI validation of DGT-02</what-built>
  <how-to-verify>
    1. In Windows R: `setwd("integrated-guimorph-development_EOC/Project/GUImorphDevelopment")`; `devtools::load_all(".")`; `GUImorph()`
    2. File → Load ply File → select `zips/Folsom 3D models/C13.1.ply` (D-14)
    3. **3D Digitizing tab** → click **Set number of landmarks** → enter **3** (required — default is 5 per D-03)
    4. **Double-click** 3 distinct points on the mesh to place landmarks (D-02: landmarks first; same gesture as DGT-01)
    5. Confirm **Curves** tab becomes enabled; switch to Curves tab — console should show curve mode / tab switch messages
    6. On Curves tab, **double-click** each of the 3 landmarks (one at a time) — expect console `add curve` / curve bind output; no R error on 3rd click
    7. In R console run: `nrow(e$activeDataList[[1]][[4]])` — expect **1**; `ncol(...)` expect **3**; values are integer landmark IDs
    8. Click **Fit** once (D-04) — GUI must not crash or hang; view may reset rotation/zoom (no spline math validation)
    9. Confirm dormant buttons (Set curves number, Compute Curves) are NOT visible/active (D-01)
  </how-to-verify>
  <acceptance_criteria>
    - Exactly 1 curve row stored in activeDataList[[1]][[4]] with 3 landmark ID columns
    - No crash on 3rd curve-selection double-click or Fit click
    - Curves tab reached only after 3 landmarks placed with landmark count preset to 3
    - DGT-01 landmark baseline assumed from 04-01 — re-place landmarks only if session empty
  </acceptance_criteria>
  <verify>
    <automated>MISSING — GUI curve bind requires Windows R manual UAT; Nyquist Wave 0 gap per 04-VALIDATION.md</automated>
  </verify>
  <resume-signal>Type "approved" with nrow/ncol confirmation, or describe failure (error text, step number)</resume-signal>
  <done>DGT-02 curve definition validated on primary specimen; Fit smoke pass</done>
</task>

<task type="auto">
  <name>Task 3: Document curve validation and mark DGT-02 complete</name>
  <read_first>
    - .planning/smoke-test-findings.md (append-only structure from Phase 1–3 sections)
    - .planning/phases/04-digitize-workflow/04-CONTEXT.md D-12, D-13
    - .planning/REQUIREMENTS.md DGT-02 row
  </read_first>
  <files>.planning/smoke-test-findings.md, .planning/REQUIREMENTS.md</files>
  <action>
    Append a new **Phase 4 — Curve Definition (04-02)** section to smoke-test-findings.md (D-13). Include: date, specimen (`C13.1.ply`), landmark count preset (3), curve bind result (1 row × 3 IDs), Fit result, and workflow quirks documented (not code-fixed per D-12):
    - Default landmark count 5 blocks Curves tab until user sets count or places 5 landmarks
    - Curve tab requires **double-click** on existing landmarks (parallel to DGT-01 single-click pick trap)
    - Dormant curve management buttons intentionally disabled (D-01)

    Update REQUIREMENTS.md: mark **DGT-02** checked complete with date and brief note (legacy 3-click curve bind + Fit smoke).

    Do NOT commit `.dgt` test files (D-17). Do NOT fix the 26 load_all warnings unless they blocked curve workflow (D-10 deferred to 04-03).
  </action>
  <acceptance_criteria>
    - smoke-test-findings.md has dated Phase 4 curve section with pass/fail table
    - UX quirks listed as documentation, not OPEN code bugs (unless Task 2 found a blocker)
    - REQUIREMENTS.md DGT-02 status updated
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 4.*Curve' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep 'DGT-02' .planning/REQUIREMENTS.md | grep -c '✓\|✅\|\[x\]' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Phase 4 curve findings documented; DGT-02 marked validated in REQUIREMENTS.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Tk canvas | Double-click coordinates select existing landmark dots; no new geometry created on Curve tab |
| R activeDataList → C engine | Integer landmark IDs passed via add("curve", …); C must trust IDs exist |
| loadPly init → onSelectCurve | Slot [[4]] type must be matrix-compatible before rbind |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02-01 | Tampering | onSelectCurve duplicate ID check | accept | Existing tkmessageBox rejects duplicate dot in one curve — no change needed |
| T-4-02-02 | Denial of Service | rbind on list() slot | mitigate | Task 1: matrix(nrow=0,ncol=3) init in loadPly; optional list guard in onSelectCurve |
| T-4-02-03 | Information Disclosure | debug print() in curve paths | accept | Verbose logging retained per D-11 for Phase 4 debugging |
| T-4-02-SC | Tampering | R source edits | mitigate | load_all verify after edit; human UAT confirms no regression on landmark placement |
</threat_model>

<verification>
- Task 1 automated grep + load_all pass
- Task 2 human UAT confirms 1×3 curve matrix and Fit no-crash
- DGT-02 TRUE in REQUIREMENTS.md after Task 3
- No dormant curve UI enabled (D-01)
</verification>

<success_criteria>
- User defines ≥1 curve (3 landmark IDs) via legacy Curve tab workflow on C13.1.ply
- activeDataList[[1]][[4]] contains valid integer matrix after bind
- Fit button smoke test passes without crash
- Findings and UX quirks appended to smoke-test-findings.md
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Purpose |
|----------|------|---------|
| Curve slot init fix | `R/3dDigitize.main.r` (loadPly) | Prevents rbind failure on first curve bind |
| Optional onSelectCurve guard | `R/3dDigitize.curve.r` | Fallback coercion if init missed |
| Phase 4 curve smoke section | `.planning/smoke-test-findings.md` | D-13 validation record + UX quirks |
| Requirement traceability | `.planning/REQUIREMENTS.md` | DGT-02 marked complete |

<output>
Create `.planning/phases/04-digitize-workflow/04-02-SUMMARY.md` when done
</output>
