---
phase: 05-analysis-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-analysis-round-trip/05-INVENTORY.md
autonomous: true
requirements:
  - ANAL-02
user_setup: []

must_haves:
  truths:
    - "Every geomorph and Morpho call site in active R sources is listed with file:line and function name"
    - "GPA hot-path APIs (gpagen, plotAllSpecimens, two.d.array) are tagged HOT vs DEFERRED"
    - "Vendored geomorph.support.code.r procD helpers are documented as out-of-scope for Phase 5 execution"
  artifacts:
    - path: ".planning/phases/05-analysis-round-trip/05-INVENTORY.md"
      provides: "geomorph/Morpho call site inventory for ANAL-02"
      min_lines: 40
  key_links:
    - from: "3dDigitize.geomorph.r compute()"
      to: "gpagen"
      via: "landmarks-only coords.A array"
      pattern: "gpagen\\(A=coords\\.A"
    - from: "3dDigitize.geomorph.r save()"
      to: "two.d.array"
      via: "gm.results coord field"
      pattern: "two\\.d\\.array"
---

<objective>
Inventory all `geomorph` and `Morpho` function call sites in GUImorph R sources; classify GPA hot path vs deferred vendored/dead code.

Purpose: ANAL-02 requires a documented inventory before migration. D-08 locks hot-path-first scope. Output feeds 05-02 fixes and 05-03 ANAL-03 completion.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-analysis-round-trip/05-CONTEXT.md
@.planning/phases/05-analysis-round-trip/05-RESEARCH.md
@.planning/phases/05-analysis-round-trip/05-PATTERNS.md
@.planning/research/STACK.md

<interfaces>
Hot-path APIs (must tag in inventory):
- `gpagen` — 3dDigitize.geomorph.r:199
- `plotAllSpecimens` — 3dDigitize.geomorph.r:226
- `two.d.array` — 3dDigitize.geomorph.r:219
- `getLandmark` — 3dDigitize.main.r:1699 (C bridge, not geomorph)

Morpho (inventory only unless blocking):
- `Morpho::fastKmeans` — 3dDigitize.surface.r:274, 290, 361

Vendored defer bucket:
- `geomorph.support.code.r` — procD.slide, procD.fit, advanced.procD.lm, etc. (~2700 lines)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep and classify geomorph/Morpho call sites</name>
  <read_first>
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (full file)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.surface.r (Morpho lines)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/geomorph.support.code.r (scan headers/comments only)
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/DESCRIPTION (@import geomorph, Morpho)
    - .planning/research/STACK.md geomorph 4.x section
  </read_first>
  <files>.planning/phases/05-analysis-round-trip/05-INVENTORY.md</files>
  <action>
    Create `05-INVENTORY.md` with these sections:

    1. **Summary table** — columns: Function, File:Line, Tier (HOT / SURFACE / VENDORED-DEFER), Phase 5 action (fix / document / defer)
    2. **GPA hot path** — `gpagen`, `plotAllSpecimens`, `two.d.array`, `summary` on gpagen result; note unqualified vs `geomorph::` prefix
    3. **Morpho surface path** — all `Morpho::fastKmeans` sites; mark "inventory only per D-09 unless GPA UAT blocked"
    4. **Vendored geomorph.support.code.r** — list function families (procD.slide, procD.fit, advanced.procD.lm, semilandmarks.*) with note "not reachable from landmarks-only GPA per D-10"
    5. **Known API risks** from STACK.md — gpagen ProcD default, coord vs coords field names on gpagen return object, deprecated procD helpers in vendored file
    6. **No code changes** in this plan — documentation only

    Search scope: `R/3dDigitize.*.r`, `R/geomorph.support.code.r`, `R/rtkogl.R` (confirm no geomorph calls).
  </action>
  <acceptance_criteria>
    - 05-INVENTORY.md exists with ≥3 HOT-path entries
    - Morpho fastKmeans sites listed
    - Vendored procD section explicitly marked DEFERRED
    - References STACK.md geomorph version notes
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-analysis-round-trip/05-INVENTORY.md && grep -c 'HOT' .planning/phases/05-analysis-round-trip/05-INVENTORY.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep -c 'gpagen' .planning/phases/05-analysis-round-trip/05-INVENTORY.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>ANAL-02 inventory artifact written; hot path vs deferred classification complete</done>
</task>

</tasks>

<verification>
- 05-INVENTORY.md created with HOT/DEFERRED tiers
- No R source files modified
</verification>

<success_criteria>
- Documented inventory satisfies ANAL-02 traceability
- 05-02 executor knows which APIs to fix first
</success_criteria>

<output>
No SUMMARY.md required for inventory-only plan; findings consumed by 05-02
</output>
