---
phase: 05-analysis-round-trip
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r
  - .planning/smoke-test-findings.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - ANAL-01
user_setup: []

must_haves:
  truths:
    - "User loads test_fresh.dgt, switches to GPA tab, clicks Compute without R error"
    - "Plot Aligned Specimens opens geomorph plot window without crash"
    - "Save Result writes a non-empty .csv file"
    - "Landmarks-only path: all four sliding/anchor checkboxes OFF during UAT"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r"
      provides: "GPA hot-path API compatibility fixes"
    - path: ".planning/smoke-test-findings.md"
      provides: "Phase 5 GPA UAT section"
      min_lines: 15
  key_links:
    - from: "openDgt reload"
      to: "compute() getLandmark loop"
      via: "e$activeDataList + e$landmarkNum"
      pattern: "getLandmark\\(i\\)"
    - from: "compute()"
      to: "plotspecs()"
      via: "assign gm.results to global env"
      pattern: "assign\\(x = \"gm.results\""
---

<objective>
Fix GPA hot-path API blockers and validate landmarks-only GPA end-to-end on Phase 4 `test_fresh.dgt` via GUI (Compute → Plot → Save CSV).

Purpose: ANAL-01 — prove at least one analysis runs on digitized multi-specimen data. D-01–D-07 lock test protocol.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-analysis-round-trip/05-CONTEXT.md
@.planning/phases/05-analysis-round-trip/05-RESEARCH.md
@.planning/phases/05-analysis-round-trip/05-INVENTORY.md
@.planning/phases/04-digitize-workflow/04-03-SUMMARY.md
@.planning/smoke-test-findings.md

<interfaces>
compute() landmarks-only branch (sliding checkboxes OFF):
```r
coords.A <- array(coords.lmk, dim = c(as.numeric(e$landmarkNum), 3, nSpecimen))
e$gm.results <- gpagen(A=coords.A, curves=NULL, surfaces=NULL, ...)
```

plotspecs() uses global `gm.results$coords` (line 226)
save() uses `e$gm.results$coord` (line 219) — verify field name on geomorph 4.x gpagen object

Test fixture (local, do not commit): `zips/Folsom 3D models/test_fresh.dgt`
- 2 specimens: C13.1.ply, C8.1.ply
- 3 landmarks each, landmarkNum=3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GPA hot-path API blockers in 3dDigitize.geomorph.r</name>
  <read_first>
    - .planning/phases/05-analysis-round-trip/05-INVENTORY.md HOT section
    - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r (compute, save, plotspecs)
    - .planning/research/STACK.md
  </read_first>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r</files>
  <action>
    Fix only analysis-path blockers (D-12) that prevent landmarks-only GPA on geomorph 4.x:

    1. **Namespace qualification** — use `geomorph::gpagen`, `geomorph::plotAllSpecimens`, `geomorph::two.d.array` for clarity and R CMD check compliance.

    2. **gpagen return object field names** — after `gpagen`, verify whether aligned coordinates live in `$coords` or `$coord`. Align `save()` and `plotspecs()` to use the same field. Common break: `save()` line 219 uses `$coord` while `plotspecs()` uses `$coords`. Fix to match geomorph 4.x return structure (use defensive: `res_coord <- if (!is.null(e$gm.results$coords)) e$gm.results$coords else e$gm.results$coord` only if both names appear in docs — prefer single canonical field per geomorph 4.1.0).

    3. **Null gm.results guard** — in `save()` and `plotspecs()`, if `gm.results` missing, show `tkmessageBox` "Run Compute first" instead of opaque error.

    4. **plotspecs global reference** — `plotspecs()` reads `gm.results` from pos=1 environment; ensure `compute()` assign remains. Optionally use `e$gm.results` consistently if global lookup fails.

    5. **Do NOT** enable sliding checkboxes UI, add PCA tab, or edit geomorph.support.code.r (D-10, D-11).

    6. **Do NOT** remove debug print() (carried forward from Phase 4).

    Run `devtools::load_all(".")` after edits. If geomorph not installed in WSL R, document and rely on Windows R UAT for runtime verify.
  </action>
  <acceptance_criteria>
    - gpagen/plotAllSpecimens/two.d.array qualified or confirmed working via @import
    - save() and plotspecs() use consistent coordinate field from gpagen result
    - load_all succeeds without fatal error
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/GUImorphDevelopment && Rscript -e "devtools::load_all('.'); cat('load_all OK\n')" 2>&1 | tail -1 | grep -q 'load_all OK'</automated>
    <automated>grep -E 'geomorph::(gpagen|plotAllSpecimens|two\.d\.array)|gpagen\\(' integrated-guimorph-development_EOC/Project/GUImorphDevelopment/R/3dDigitize.geomorph.r | wc -l | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Hot-path source fixes applied; package loads</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GUI GPA UAT on test_fresh.dgt (ANAL-01)</name>
  <read_first>
    - .planning/phases/05-analysis-round-trip/05-CONTEXT.md D-01 through D-07
    - .planning/phases/04-digitize-workflow/04-03-SUMMARY.md openDgt handoff
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <what-built>Task 1 GPA hot-path fixes; ready for Windows R GUI validation</what-built>
  <how-to-verify>
    1. Windows R: `setwd("integrated-guimorph-development_EOC/Project/GUImorphDevelopment")`; `devtools::load_all(".")`; `GUImorph()`
    2. File → **Load DGT File** → `zips/Folsom 3D models/test_fresh.dgt` (D-06)
    3. Confirm 2 specimens loaded (C13.1.ply, C8.1.ply), 3 landmarks visible each
    4. Switch to **GPA** tab (D-04)
    5. Verify checkboxes: **Slide semilandmarks on curves** OFF, **Slide semilandmarks on surfaces** OFF, **Use anchors as surface/curve semilandmarks** OFF (D-01)
    6. Click **Compute** — expect console "before gpagen" and summary output; no R error dialog
    7. Click **Plot Aligned Specimens** — geomorph plot window opens with 2 specimens + mean; no crash
    8. Click **Save Result** — choose path, save CSV; file non-empty with Csize + coord columns (D-14: do not commit CSV)
    9. Optional console check: `dim(gm.results$coords)` or equivalent — expect 3 × 3 × 2 or geomorph 4.x equivalent
  </how-to-verify>
  <acceptance_criteria>
    - Compute completes for 3 LM × 2 specimens landmarks-only GPA
    - Plot and Save succeed in same session after Compute
    - No blocker errors; document non-blocking quirks only
  </acceptance_criteria>
  <verify>
    <automated>MISSING — GUI GPA requires Windows R manual UAT</automated>
  </verify>
  <resume-signal>Type "approved" with pass/fail per step, or paste error text from Compute/Plot/Save</resume-signal>
  <done>ANAL-01 GPA GUI path validated on test_fresh.dgt</done>
</task>

<task type="auto">
  <name>Task 3: Document GPA UAT and mark ANAL-01 complete</name>
  <read_first>
    - .planning/smoke-test-findings.md
    - .planning/REQUIREMENTS.md ANAL-01 row
  </read_first>
  <files>.planning/smoke-test-findings.md, .planning/REQUIREMENTS.md</files>
  <action>
    Append **Phase 5 — GPA Round-Trip (05-02)** section to smoke-test-findings.md (D-13):
    - Date, fixture (`test_fresh.dgt`), landmarks-only settings
    - Compute / Plot / Save pass-fail table
    - API fixes applied (brief)
    - Quirks documented not fixed (ProcD default behavior, Phase 4 curve overlay if visible on GPA tab, etc.)

    Mark **ANAL-01** complete in REQUIREMENTS.md with date and note (landmarks-only GPA, 2 specimens).
  </action>
  <acceptance_criteria>
    - smoke-test-findings.md has Phase 5 GPA section
    - REQUIREMENTS.md ANAL-01 checked
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 5.*GPA' .planning/smoke-test-findings.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
    <automated>grep 'ANAL-01' .planning/REQUIREMENTS.md | grep -cE '✓|✅|\[x\]' | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>ANAL-01 documented and marked complete</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| API field rename coord→coords | mitigate | Task 1 alignment |
| Missing Compute before Plot | mitigate | tkmessageBox guard |
| Vendored procD accidental edit | accept | D-10 scope fence |
</threat_model>

<verification>
- Task 1 load_all pass
- Task 2 human UAT approved
- ANAL-01 TRUE in REQUIREMENTS.md
</verification>

<success_criteria>
- Landmarks-only GPA runs on 2-specimen test_fresh.dgt
- Plot and CSV export work via GPA tab
- Phase 5 smoke section documents results
</success_criteria>

<output>
Create `.planning/phases/05-analysis-round-trip/05-02-SUMMARY.md` when done
</output>
