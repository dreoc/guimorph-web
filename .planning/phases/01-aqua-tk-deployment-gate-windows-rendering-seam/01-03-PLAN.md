---
phase: 01-aqua-tk-deployment-gate-windows-rendering-seam
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md
autonomous: false
requirements: [GATE-02]

must_haves:
  truths:
    - "A researcher can follow a documented, reproducible procedure to reach an R + Aqua Tk configuration on macOS"
    - "The distribution decision (D-01 bundled vs D-02 Homebrew fallback) is explicitly recorded, grounded in the Plan 02 spike outcome"
    - "The doc names which Tcl/Tk flavor ships and how to obtain it, and points to the committed gate_check.R as automated proof"
  artifacts:
    - integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md
  key_links:
    - "The setup doc references the committed gate_ext.c + gate_check.R (Plan 02) as the automated verification a researcher runs after setup"
    - "The chosen path (D-01 or D-02) is justified by the spike's package-local feasibility finding"
---

<objective>
Deliver GATE-02: a documented, reproducible "R + Aqua Tk" configuration a macOS researcher can follow, and lock the distribution decision (bundled framework D-01 vs Homebrew-Aqua fallback D-02) based on the Plan 02 spike.

Purpose: GATE-02 is a packaging + documentation deliverable, not runtime code. It turns the empirical spike result into a repeatable setup future phases and researchers rely on.
Output: `docs/AQUA-TK-SETUP.md` plus a recorded D-01/D-02 decision.
</objective>

<execution_context>
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-RESEARCH.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-CONTEXT.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-02-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 03)

New symbols/files created by this plan (source-grounding: exclude these from drift verification):
- New file: `docs/AQUA-TK-SETUP.md` (the reproducible R + Aqua Tk setup doc; Claude's discretion on standalone-vs-README chose standalone per CONTEXT)
- Decision record: locked D-01 (bundled) vs D-02 (Homebrew fallback), captured in the doc and SUMMARY
- No new code symbols, CLI flags, or struct fields

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the Aqua-Tk distribution path (D-01 bundled vs D-02 Homebrew fallback)</name>
  <read_first>
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-02-SUMMARY.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-RESEARCH.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-CONTEXT.md
  </read_first>
  <action>Present the D-01-vs-D-02 decision to the user, grounded in the Plan 02 spike outcome recorded in 01-02-SUMMARY (whether package-local Aqua binding worked on a clean machine through quarantine/universal2). Do not decide by preference — decide by the recorded feasibility finding. Record the selected option id and a one-line rationale in the SUMMARY before Task 2 writes the doc.</action>
  <decision>Which reproducible "R + Aqua Tk" configuration ships to researchers as GATE-02?</decision>
  <context>
    The Plan 02 spike (D-02) empirically tested whether the Aqua binding can be made package-local (D-01 ideal: bundle a prebuilt Aqua framework + relinked/overlaid `tcltk` so a researcher needs zero manual Tcl/Tk steps). Bundling carries downstream weight (sign/notarize + universal2, ties to Phase 4 BLD-03). If the spike showed package-local binding is infeasible or survives neither a clean machine nor quarantine/universal2, D-02 (documented Homebrew-Aqua-Tk + R reconfigure/rebuild) is the fallback. Base this decision on the spike outcome recorded in 01-02-SUMMARY, not on preference.
  </context>
  <options>
    <option id="d01-bundled">
      <name>D-01 — Bundled prebuilt Aqua framework (package-local, zero researcher setup)</name>
      <pros>Lowest researcher setup burden (the stated goal); self-contained distribution.</pros>
      <cons>Unproven (A2); requires framework relocation/relink + sign/notarize + universal2; only choose if the spike proved it works on a clean machine through quarantine.</cons>
    </option>
    <option id="d02-homebrew">
      <name>D-02 — Documented Homebrew Aqua tcl-tk + R reconfigure/rebuild (fallback)</name>
      <pros>Known-good, low technical risk, Aqua-correct; matches exactly what the spike ran.</pros>
      <cons>Pushes a one-time setup step onto the researcher.</cons>
    </option>
  </options>
  <verify>
    <human-check>Confirm the selected option matches the spike's package-local feasibility finding in 01-02-SUMMARY; state the chosen option id.</human-check>
  </verify>
  <acceptance_criteria>
    - Decision assertion: exactly one option (`d01-bundled` or `d02-homebrew`) is selected and recorded in the SUMMARY with a one-line rationale citing the spike result.
    - Traceability assertion: the rationale references D-01/D-02 and the 01-02-SUMMARY spike outcome (not preference).
  </acceptance_criteria>
  <resume-signal>Select: d01-bundled or d02-homebrew (with the spike-grounded rationale)</resume-signal>
  <done>Exactly one distribution option is locked and recorded in the SUMMARY with a spike-grounded rationale citing D-01/D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Write the reproducible R + Aqua Tk setup document</name>
  <files>
    integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md
  </files>
  <read_first>
    integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-02-SUMMARY.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-RESEARCH.md
  </read_first>
  <action>
    Create `docs/AQUA-TK-SETUP.md` (standalone doc — Claude's-discretion form per CONTEXT; match the voice/structure of `BUILD.md`). It must document, for the option chosen in Task 1, the exact reproducible steps a researcher follows to reach `tk windowingsystem == aqua` on macOS: which Tcl/Tk flavor ships and how to obtain it (Homebrew Aqua `tcl-tk` and/or bundled framework), the base-`tcltk` relink/rebuild commands captured verbatim from the Plan 02 spike (01-02-SUMMARY), the command-line-R-only caveat (Aqua Tk does not work in R.app — Pitfall 2), and the `otool -L` self-check that confirms no libX11. Include a "verify your setup" section that instructs running the committed `Rscript test/gate/gate_check.R <dylib>` (Plan 02) as the automated proof, and note the D-01/D-02 decision + rationale. If D-01 was chosen, note the sign/notarize + universal2 dependency flows into Phase 4 (BLD-03). Do NOT invent steps the spike did not actually run — the doc must reflect the recorded, reproduced procedure.
  </action>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md && grep -qi 'aqua' integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md && grep -q 'gate_check.R' integrated-guimorph-development_EOC/Project/tkogl2/docs/AQUA-TK-SETUP.md</automated>
  </verify>
  <acceptance_criteria>
    - Artifact assertion: `docs/AQUA-TK-SETUP.md` exists and names the shipping Tcl/Tk flavor and how to obtain it.
    - Content assertion: the doc contains the exact relink/rebuild commands reproduced from 01-02-SUMMARY (not invented) and the `otool -L ... | grep libX11` self-check.
    - Content assertion: the doc states the command-line-R-only caveat (not R.app).
    - Content assertion: the doc references `test/gate/gate_check.R` as the automated verification step and records the D-01/D-02 decision + rationale.
    - Reachability assertion: a reader with a clean macOS box could follow the doc to reach `aqua` (steps are complete and ordered).
  </acceptance_criteria>
  <done>`docs/AQUA-TK-SETUP.md` gives a researcher a complete, reproducible path to R + Aqua Tk, grounded in the spike, with the committed gate_check.R as its automated proof and the distribution decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc -> researcher machine | The doc instructs installs/relinks; instructions must come from first-party sources only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | setup instructions point at untrusted binaries | low | mitigate | Doc references only first-party sources (Homebrew core `tcl-tk`, CRAN R, tcl-lang.org); no third-party Tcl/Tk distro. |
| T-03-02 | Repudiation | undocumented/irreproducible setup | low | mitigate | Steps captured verbatim from the reproduced spike; gate_check.R gives an objective pass/fail. |
| T-03-SC | Tampering | package installs | low | accept | Doc-only plan; installs are the researcher's, all first-party (see T-02-SC). |
</threat_model>

<verification>
- `docs/AQUA-TK-SETUP.md` exists, names the Tcl/Tk flavor, includes the spike's relink commands and the `otool` self-check, and points to `gate_check.R`.
- The D-01/D-02 decision is recorded with a spike-grounded rationale.
</verification>

<success_criteria>
- GATE-02 met: a reproducible, documented R + Aqua Tk setup exists that a researcher can follow, with an automated self-check.
- The distribution path is locked and justified by empirical spike results.
</success_criteria>

<output>
Create `.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-03-SUMMARY.md` when done.
</output>
