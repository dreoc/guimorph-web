---
phase: 01-aqua-tk-deployment-gate-windows-rendering-seam
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply
autonomous: false
requirements: [CMP-01]
user_setup:
  - service: Windows + MSVC (VS 2022 Build Tools v143) + Windows R
    why: "The Windows-regression check (build MSVC tkogl2.dll + load + eyeball-render a PLY) CANNOT run on this macOS dev machine (no MSVC/Windows R). It must be delegated to a Windows environment/maintainer. MinGW renders black — MSVC is the only supported toolchain."
    env_vars: []
    dashboard_config:
      - task: "On a Windows box: rebuild tkogl2.dll from post-seam source per BUILD.md (MSVC/CMake), redeploy to inst/libs/x64/, launch GUImorph(), load the committed PLY fixture, and eyeball-confirm the mesh renders identically to pre-seam."
        location: "Windows machine with MSVC + Windows R"

must_haves:
  truths:
    - "A small PLY fixture is committed to the repo (none existed before) for the regression render"
    - "The MSVC tkogl2.dll, rebuilt from post-seam source, loads and renders the fixture identically to before the seam extraction (D-06 eyeball check)"
    - "The shipped inst/libs/x64/tkogl2.dll is rebuilt from source so binary and source do not drift"
  artifacts:
    - integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply
  key_links:
    - "The rebuilt DLL is produced from the Plan 01 post-seam source tree (depends_on 01-01)"
    - "The committed PLY fixture is the mesh loaded for the eyeball render"
---

<objective>
Deliver CMP-01: prove the Windows build still works after the seam extraction (Plan 01) — build the MSVC `tkogl2.dll` from post-seam source, load it, and eyeball-render a known PLY mesh identically to before (D-06). Also close the fixture gap: no `.ply` is committed anywhere in the repo, so this plan sources and commits a small regression fixture.

Purpose: The whole phase's Windows side is "nothing visibly changed." The seam is a behavior-preserving refactor; this is the guardrail that proves it. This work is off-box (no Windows/MSVC here) and is delegated to a Windows maintainer.
Output: a committed PLY fixture + a completed Windows build/render regression check.
</objective>

<execution_context>
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-RESEARCH.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-CONTEXT.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 04)

New symbols/files created by this plan (source-grounding: exclude these from drift verification):
- New file: `test/fixtures/regression.ply` (the committed regression mesh — Claude's-discretion fixture; none existed before)
- Rebuilt binary (off-box): `inst/libs/x64/tkogl2.dll` regenerated from post-seam source
- No new code symbols, CLI flags, or struct fields

<tasks>

<task type="auto">
  <name>Task 1: Source and commit a small regression PLY fixture</name>
  <files>
    integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply
  </files>
  <read_first>
    integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
    integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-RESEARCH.md
  </read_first>
  <action>
    Source a small PLY mesh and commit it at `test/fixtures/regression.ply` (Claude's discretion per CONTEXT — RESEARCH Open Q2 confirms NO `.ply` exists anywhere in the repo, so "pick an existing sample" has nothing to pick; source a fresh small mesh). Read `ogl_model_ply_ZARF_9.c` first to confirm the exact PLY variant the loader expects (ASCII vs binary, header fields, `element vertex`/`element face` layout) and pick/produce a fixture that matches that parser so it will actually load. Keep it small (a low-poly mesh — the goal is a quick eyeball render, not a stress test). Do NOT modify the PLY parser (unsafe-parser hardening is explicitly out of scope/deferred). Record the fixture's provenance (where it came from / how generated) in the SUMMARY.
  </action>
  <verify>
    <automated>test -f integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply && head -1 integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply | grep -qi '^ply' && grep -qiE 'element[[:space:]]+vertex' integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply</automated>
  </verify>
  <acceptance_criteria>
    - Artifact assertion: `test/fixtures/regression.ply` exists, begins with a `ply` magic line, and declares `element vertex` (and `element face`) headers matching the variant `ogl_model_ply_ZARF_9.c` parses.
    - Behavior assertion: the file is small (low-poly) and is a valid PLY the existing loader can read without parser modification.
    - Traceability assertion: the fixture's provenance is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>A small, loader-compatible PLY fixture is committed at `test/fixtures/regression.ply`; the PLY parser is untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Off-box Windows regression — rebuild MSVC tkogl2.dll and eyeball-render the fixture</name>
  <read_first>
    integrated-guimorph-development_EOC/Project/tkogl2/BUILD.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-01-SUMMARY.md
    integrated-guimorph-development_EOC/Project/tkogl2/test/fixtures/regression.ply
  </read_first>
  <action>Delegate the Windows-regression check to a Windows maintainer (off-box: no MSVC/Windows R on this macOS machine). Provide them the post-seam source, the committed fixture, and the steps in `<how-to-verify>`; collect their yes/no result and any diffs, and record them in the SUMMARY. This task is a blocking human-action because the build+render cannot be automated on this box (D-06 manual eyeball check).</action>
  <what-built>
    Plan 01 extracted all Win32 window/context code behind the `gfx_backend.h` seam into `gfx_backend_wgl.c` and added it to `CMakeLists.txt`. This check proves that refactor did not change Windows rendering behavior (D-05 / D-06). It cannot run on the macOS dev machine (no MSVC/Windows R) — a Windows maintainer must perform it.
  </what-built>
  <how-to-verify>
    On a Windows machine with MSVC (VS 2022 Build Tools v143) and Windows R:
    1. Check out the post-seam source (Plan 01 merged) and build `tkogl2.dll` with the MSVC toolchain per `BUILD.md §2` (CMake WIN32 branch; confirm `gfx_backend_wgl.c` compiles under `#ifdef _WIN32`). MinGW is NOT acceptable (renders black).
    2. Redeploy the freshly built DLL to `inst/libs/x64/tkogl2.dll` per `BUILD.md §3` so the shipped binary matches post-seam source.
    3. Launch `GUImorph()` in Windows R, load `test/fixtures/regression.ply`, and eyeball-confirm the mesh renders identically to before the seam extraction (same geometry, shading, orientation — no blank/black viewport).
    4. Confirm the build has no new warnings/errors introduced by the seam and both build modes still compile.
    Report: build succeeded (yes/no), render identical (yes/no), and any diffs.
  </how-to-verify>
  <verify>
    <human-check>Windows maintainer confirms: MSVC tkogl2.dll rebuilt from post-seam source, redeployed to inst/libs/x64/, and the committed PLY fixture renders identically (no blank/black) in GUImorph(). Automated verification is off-box and not runnable on this macOS machine (D-06 manual eyeball check).</human-check>
  </verify>
  <acceptance_criteria>
    - Build assertion: the MSVC `tkogl2.dll` builds from post-seam source with `gfx_backend_wgl.c` included and no new errors.
    - Behavior assertion: loading `test/fixtures/regression.ply` in `GUImorph()` renders the mesh identically to the pre-seam build (non-blank, non-black; same geometry/orientation) — the D-06 eyeball criterion.
    - Integrity assertion: `inst/libs/x64/tkogl2.dll` is rebuilt from the post-seam source (no source/binary drift).
    - Reporting assertion: the maintainer's yes/no result and any observed diffs are recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Windows maintainer types "windows-render-ok" (or reports the observed regression/diffs)</resume-signal>
  <done>MSVC tkogl2.dll rebuilt from post-seam source and redeployed; the committed PLY fixture renders identically (non-blank) in GUImorph(); result recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source -> shipped DLL | The committed `inst/libs/x64/tkogl2.dll` is a trust anchor; it must be regenerated from post-seam source. |
| PLY file -> parser | The fixture is read by the pre-existing unsafe PLY parser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | committed prebuilt tkogl2.dll | medium | mitigate | Rebuild the DLL from post-seam source and redeploy; do not ship a stale/pre-seam binary (V10/V14 code integrity). |
| T-04-02 | Tampering/DoS | fixture read by unsafe PLY parser | low | accept | Use a small, well-formed, self-authored/first-party fixture; do not modify or harden the parser this phase (explicitly deferred). |
| T-04-SC | Tampering | package installs | low | accept | No language-registry installs; MSVC/CMake are first-party toolchains (see T-02-SC). |
</threat_model>

<verification>
- `test/fixtures/regression.ply` exists, is loader-compatible (valid PLY header), and small.
- Off-box (Windows): MSVC `tkogl2.dll` rebuilds from post-seam source, redeploys, and renders the fixture identically (D-06 eyeball) — recorded by the maintainer.
</verification>

<success_criteria>
- CMP-01 met: the Windows build still works — the MSVC DLL builds and renders a known PLY identically after the seam extraction.
- A durable regression fixture is committed (closes the "no PLY committed" gap).
</success_criteria>

<output>
Create `.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-04-SUMMARY.md` when done.
</output>
