---
phase: 01-aqua-tk-deployment-gate-windows-rendering-seam
plan: 05
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c
  - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h
  - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
autonomous: true
requirements: [RND-01]

must_haves:
  truths:
    - "Core contains no platform window/context tokens outside the backend seam: a scan for HWND/HDC/HGLRC/SwapBuffers/wgl over src/ (excluding gfx_backend_wgl.c) is empty (RND-01)"
    - "The window+context lifecycle is preserved: setWindowId still calls gfx_create -> gfx_make_current -> ogl_init in that order, and the int->drawable conversion is behavior-preserving (no rendering change)"
    - "No unresolved debt marker remains in def_ZARF_9.h (phase-modified file); the model_t.count field carries a concrete definition or a formal follow-up reference"
  artifacts:
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h
    - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
  key_links:
    - "setWindowId accepts a seam-neutral void* native_drawable and forwards it to gfx_create; the void*->HWND cast lives only in gfx_backend_wgl.c"
    - "The setWindow 'id' path builds the native drawable pointer from the 32-bit Tcl int via a standard-C pointer-sized integer cast (no Win32 handle type in core)"
---

<!-- planner-discipline-allow: HWND -->
<!-- planner-discipline-allow: HDC -->
<!-- planner-discipline-allow: HGLRC -->
<!-- planner-discipline-allow: SwapBuffers -->
<!-- planner-discipline-allow: wgl -->
<!-- planner-discipline-allow: TBD -->

<objective>
Close the two BLOCKER gaps from 01-VERIFICATION.md so Phase 1 achieves strict goal-backward completeness:

1. RND-01 seam completeness: the core still types the platform window handle (`HWND`) in `tcl_window.c` and `tcl_window.h`. Move that platform typing out of core so the only place a Win32 window/context token appears is the backend translation unit (`gfx_backend_wgl.c`). The backend already accepts a seam-neutral `void *native_drawable` and casts it back to the window handle internally — core just needs to stop naming the handle type.
2. Debt marker: `def_ZARF_9.h` (~line 50) has a bare unresolved `// TBD ??` marker with no linked reference. Resolve it concretely (the field's meaning is now known from usage) or annotate it with a formal follow-up.

This is a behavior-preserving refactor (D-05 mechanical-refactor spirit): no rendering behavior changes, seam contract unchanged (5 functions, opaque `gfx_surface`, no `windows.h` in core), lifecycle ordering preserved.

Purpose: Satisfy the seam truth "Core contains no platform window/context tokens outside backend seam" and the "no unresolved debt markers" truth so Phase 1 is auditable-complete on this macOS box.
Output: `tcl_window.c` / `tcl_window.h` with a seam-neutral `setWindowId` and no `HWND` usage in core; `def_ZARF_9.h` with the debt marker resolved.
</objective>

<execution_context>
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-CONTEXT.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-VERIFICATION.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-01-PLAN.md
@.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 05 — gap closure)

Symbols touched/refined by this gap-closure plan (source-grounding: these are refinements of existing artifacts, not net-new):
- Changed signature: `int setWindowId(void *native_drawable);` (was `int setWindowId(HWND hwnd);`) in `tcl_window.h` and `tcl_window.c`
- Refined local logic: the `setWindow` "id" path builds `void *native_drawable` from the 32-bit Tcl int via a standard pointer-sized integer cast (`intptr_t` from `<stdint.h>`) instead of typing an `HWND`
- Resolved comment: `model_t.count` field in `def_ZARF_9.h` gets a concrete definition comment (or a formal follow-up reference) in place of the bare `// TBD ??` marker
- Unchanged seam: `gfx_backend.h` (opaque, 5 functions) and `gfx_backend_wgl.c` (owns the `void*`->`HWND` cast) are NOT modified by this plan

<tasks>

<task type="auto">
  <name>Task 1: Make the window/context seam boundary complete — remove HWND typing from core</name>
  <files>
    integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c
    integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h
  </files>
  <read_first>
    integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c
    integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h
    integrated-guimorph-development_EOC/Project/tkogl2/src/gfx_backend.h
    integrated-guimorph-development_EOC/Project/tkogl2/src/gfx_backend_wgl.c
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-CONTEXT.md
    .planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-01-PLAN.md
  </read_first>
  <action>
    Goal: eliminate every `HWND` occurrence from the core (`tcl_window.c`, `tcl_window.h`) so the only Win32 window/context token remaining anywhere under `src/` is inside `gfx_backend_wgl.c`. The backend's `gfx_create(void *native_drawable)` already casts the `void*` back to the window handle (`gfx_backend_wgl.c` line ~19), so core must hand the seam a `void*` and never name the handle type.

    In `tcl_window.h`: change the `setWindowId` prototype (line ~10) from the `HWND`-typed parameter to `int setWindowId(void *native_drawable);`. Do NOT add any Win32-typed declaration to this header.

    In `tcl_window.c`:
    - Add `#include <stdint.h>` to the include block (line ~4) so `intptr_t` is available in standard C (do not rely on the Win32 pointer-sized integer typedef).
    - Change the `setWindowId` definition (line ~36) to `int setWindowId(void *native_drawable)`. Its body forwards straight to the seam: `g_surface = gfx_create(native_drawable);` then `gfx_make_current(g_surface);` then the existing `ogl_init();` — preserve that exact order and the existing trailing comment/return (per D-05, no lifecycle change).
    - In the `setWindow` "id" branch (lines ~75-83, ~115): replace the `HWND`-typed local and its cast with a seam-neutral pointer. Read the 32-bit id into an `int` (keep the existing `Wrapper_GetIntFromObj(interp, objv[2], &hwndId)` call), then build `void *native_drawable = (void *)(intptr_t)hwndId;` and pass it to `setWindowId(native_drawable);` at line ~115. Using `(void *)(intptr_t)` reproduces the exact conversion the prior `(handle-type)(pointer-sized-int)hwndId` performed (signed pointer-sized widen), so behavior is preserved — this is NOT the Phase 2 pathname refactor (RND-02), which fully replaces the int cast; leave that for Phase 2.
    - Reword the explanatory comment block above that cast (lines ~75-79) so it still documents the "32-bit Tcl int widened to a pointer-sized native drawable, zero/sign-extension matters on 64-bit builds" rationale but does NOT contain the literal Win32 handle token `HWND` (the core-boundary scan greps comments too). Describe it as "the native drawable handle" instead of naming the platform type.

    Do not touch `gfx_backend.h` or `gfx_backend_wgl.c` — the seam contract and the backend's cast are already correct.
  </action>
  <verify>
    <automated>test -z "$(grep -REn 'HWND|HDC|HGLRC|SwapBuffers|wgl' integrated-guimorph-development_EOC/Project/tkogl2/src/ | grep -v 'gfx_backend_wgl.c')" && grep -q 'setWindowId(void \*native_drawable)' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c && grep -q 'setWindowId(void \*native_drawable)' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.h && grep -q 'gfx_create(native_drawable)' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c && grep -q 'stdint.h' integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_window.c</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (RND-01 boundary gate): `grep -REn 'HWND|HDC|HGLRC|SwapBuffers|wgl' integrated-guimorph-development_EOC/Project/tkogl2/src/ | grep -v 'gfx_backend_wgl.c'` produces NO output — no platform window/context token (including in comments) remains in any core file or header.
    - Source assertion: `tcl_window.h` declares `int setWindowId(void *native_drawable);` and names no Win32 type.
    - Source assertion: `tcl_window.c` defines `int setWindowId(void *native_drawable)` whose body calls `gfx_create(native_drawable)` then `gfx_make_current(g_surface)` then `ogl_init()` in that order.
    - Source assertion: the `setWindow` "id" branch builds the drawable via `(void *)(intptr_t)hwndId` and `tcl_window.c` includes `<stdint.h>`.
    - Behavior assertion: no rendering logic changed — the `glViewport`/`glOrtho` "size" path and the `onDisplay()` call are untouched; only the handle typing/forwarding changed (behavior-preserving per D-05).
  </acceptance_criteria>
  <done>The core no longer names any Win32 window/context token; `setWindowId` takes a seam-neutral `void *native_drawable` forwarded to `gfx_create`; the int->pointer conversion is preserved via standard-C `intptr_t`; the seam-completeness scan over `src/` (excluding `gfx_backend_wgl.c`) is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the unresolved debt marker on model_t.count in def_ZARF_9.h</name>
  <files>
    integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
  </files>
  <read_first>
    integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
    integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ZARF_9.c
    integrated-guimorph-development_EOC/Project/tkogl2/src/ogl_model_ply_ZARF_9.c
  </read_first>
  <action>
    In `def_ZARF_9.h`, the `model_t` struct field `int count;` (line ~50) carries a bare unresolved debt marker (`// TBD ??`) with no linked issue/PR/DEF reference — this is the BLOCKER anti-pattern flagged in 01-VERIFICATION.md.

    Confirm the field's actual purpose by reading its usages (already located): it is the vertex count handed to `glDrawArrays(GL_TRIANGLES, 0, model->count)` in `ogl_model_ZARF_9.c` (line ~67); it is initialized to 0 and incremented by 3 per emitted triangle during PLY load in `ogl_model_ply_ZARF_9.c` (line ~840), and reset in `tcl_state.c`. So `count` = the number of vertices to draw (three per triangle), distinct from `vertexCountActual` and `dsCount`.

    Resolve the marker by replacing the bare `// TBD ??` with a concrete definition comment that states this meaning (e.g. describe it as the vertex count passed to `glDrawArrays` for the triangle list, three per triangle). This is a comment-only, behavior-preserving change: do NOT rename, retype, remove, or reorder the `int count;` field, and do not alter any other struct member or the `// how is this different from dsCount?` / `dsCount` comments (out of scope for this gap). If, after reading the usages, the meaning were genuinely still ambiguous, the fallback is to annotate the marker with a formal follow-up reference instead — but here the usage is unambiguous, so prefer the concrete definition.

    Leave the `#include <windows.h>` (line ~8) untouched — the broad Windows include is a Phase 2/3 concern, explicitly out of scope for this gap plan.
  </action>
  <verify>
    <automated>! grep -n 'TBD' integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h && grep -q 'int count;' integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'TBD' integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h` produces NO output — the unresolved debt marker is gone.
    - Source assertion: the `int count;` field still exists in `model_t` (field not removed or retyped) and now has a concrete descriptive comment (or a formal follow-up reference).
    - Behavior assertion: the change is comment-only — no other struct member, include, or declaration in `def_ZARF_9.h` is modified.
  </acceptance_criteria>
  <done>`def_ZARF_9.h` contains no unresolved `TBD` debt marker; the `model_t.count` field is preserved and documented with its concrete meaning (vertex count for `glDrawArrays`), leaving the rest of the header unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source -> shipped binary | The committed MSVC `tkogl2.dll` (`inst/libs/x64/`) is a trust anchor; this refactor changes only the source-side handle typing, so the DLL must still be rebuilt from post-seam source off-box (CMP-01, deferred) before shipping. |
| Tcl int -> native drawable | The 32-bit Tk window id crossing from R/Tcl into C is untrusted-shaped input; the conversion to a pointer-sized drawable must preserve the existing widening semantics (no new truncation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | committed `tkogl2.dll` vs refactored source | medium | mitigate | Source-only change; DLL rebuild+render regression is CMP-01 (off-box MSVC, deferred to `.planning/todos/pending/phase-01-windows-validation.md`). Do not ship a DLL predating this change. |
| T-01-05-02 | Tampering | int->drawable cast in `setWindow` id path | low | accept | `(void *)(intptr_t)hwndId` reproduces the prior widening exactly (behavior-preserving); the fragile int-cast is fully replaced by pathname resolution in Phase 2 (RND-02), not here. |
| T-01-05-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this plan; nothing to gate. |
</threat_model>

<verification>
- Core-boundary gate empty: `grep -REn 'HWND|HDC|HGLRC|SwapBuffers|wgl' integrated-guimorph-development_EOC/Project/tkogl2/src/ | grep -v 'gfx_backend_wgl.c'` returns nothing.
- `setWindowId` is seam-neutral (`void *native_drawable`) in both the header and the source, and forwards to `gfx_create`.
- No `TBD` debt marker remains in `def_ZARF_9.h`; the `count` field is preserved and documented.
- Behavior-preserving: lifecycle order (`gfx_create` -> `gfx_make_current` -> `ogl_init`), the `gfx_swap` present path, and the `glViewport`/`glOrtho` size path are unchanged.
- Windows MSVC build + render parity is NOT re-run here (off-box, CMP-01 deferred) — this gap plan is macOS-runnable static/source verification only.
</verification>

<success_criteria>
- The seam-completeness truth passes strictly: the only Win32 window/context token under `src/` lives in `gfx_backend_wgl.c`.
- The "no unresolved debt markers" truth passes: `def_ZARF_9.h` has no bare `TBD` marker.
- The refactor changes no rendering behavior (handle typing + one comment only).
</success_criteria>

<output>
Create `.planning/phases/01-aqua-tk-deployment-gate-windows-rendering-seam/01-05-SUMMARY.md` when done.
</output>
