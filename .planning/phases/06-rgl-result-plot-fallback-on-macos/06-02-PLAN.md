---
phase: 06-rgl-result-plot-fallback-on-macos
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - ANL-02
  - CMP-01
user_setup: []

must_haves:
  truths:
    - "On macOS, clicking Plot Aligned Specimens opens an interactive WebGL widget in the browser (no crash, no 'no OpenGL functions' failure)."
    - "On macOS, clicking Plot Mean Shape opens an interactive WebGL widget in the browser."
    - "On macOS, clicking PCA (morphospace) opens a 2-D quartz scatter window (base graphics, unchanged)."
    - "On Windows, all three result plots render exactly as before (interactive rgl window for the two 3-D plots; base-graphics window for PCA)."
  artifacts:
    - ".planning/phases/06-rgl-result-plot-fallback-on-macos/06-02-SUMMARY.md (records live macOS render + off-box Windows regression outcomes)"
  key_links:
    - "Live macOS render is the only oracle for the WebGL widget path — no headless test can confirm the browser render/rotate."
    - "Windows regression (CMP-01) is off-box and must be confirmed by a Windows maintainer before phase closure."
---

<objective>
Confirm the ANL-02 result-plot fallback works live on macOS and that the Windows build still renders result plots unchanged (CMP-01). These are the manual-only verifications from 06-VALIDATION.md that no headless test can cover.

Purpose: The WebGL widget render/rotate in a browser and the quartz PCA window have no automated oracle; and the Windows interactive path is off-box. This plan gates phase closure on human confirmation of both.
Output: `06-02-SUMMARY.md` recording all four render confirmations.
</objective>

<execution_context>
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/Users/austinlutterback/Documents/Personal-Projects/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rgl-result-plot-fallback-on-macos/06-RESEARCH.md
@.planning/phases/06-rgl-result-plot-fallback-on-macos/06-VALIDATION.md
@.planning/phases/06-rgl-result-plot-fallback-on-macos/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Live macOS render of all three result plots (ANL-02)</name>
  <action>Human-verify only — run the live GUImorph() session on the macOS box and confirm the three result plots render as described below. No code changes in this task.</action>
  <what-built>Plan 06-01 added `.rgl_show()` (macOS: `rglwidget()` → `saveWidget(selfcontained = FALSE)` → `browseURL()` → `close3d()`) plus a `.isMacOS()`-guarded `options(rgl.useNULL = TRUE)` at `GUImorph()` startup, wired into `plotspecs` and `plotMeanShape`. `plotPCA` is unchanged base graphics.</what-built>
  <how-to-verify>
    On the macOS box, after deploying the current `tkogl2.dylib` (STATE prerequisite: pull/rebuild per Phase 5 Next-#1 if not already loaded):
    1. Launch `GUImorph()` from an R + Aqua-Tk session and confirm no startup error.
    2. Load specimens and run GPA compute (Results require `gm.results`).
    3. Click **Plot Aligned Specimens** → a browser tab opens showing the aligned point cloud + red consensus; confirm you can rotate/zoom (WebGL interactive), no "This build of rgl does not include OpenGL functions" message, no crash.
    4. Click **Plot Mean Shape** → a browser tab opens showing the ball-pivot mesh (wire or shade per the checkbox) + red mean landmarks; confirm rotate/zoom.
    5. Click **PCA (morphospace)** → a 2-D quartz scatter window opens (PC1×PC2, numbered points); confirm it appears (this is base graphics via quartz, not a browser widget).
    6. Confirm the engine viewport still renders/picks after plotting (129b42a per-frame make-current prerequisite): place a landmark or redraw the mesh — canvas must not go black.
  </how-to-verify>
  <resume-signal>Type "approved" if all five render as described (and the engine viewport survives), or describe what failed (blank widget, pandoc error, no browser, quartz missing, black canvas).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Off-box Windows result-plot regression (CMP-01)</name>
  <action>Human-verify only (off-box) — a Windows maintainer confirms result plots render unchanged. No code changes in this task.</action>
  <what-built>CMP-01 recurring checkpoint: the Windows result-plot path is unchanged in source — `.rgl_show()`'s non-macOS branch is byte-identical `rgl::rgl.bringtotop(stay = TRUE)`, and `options(rgl.useNULL = TRUE)` is set only under `.isMacOS()`. This is an off-box confirmation (no Windows/MSVC host here; delegated to a Windows maintainer, D-16 recurring pattern).</what-built>
  <how-to-verify>
    On a Windows build (maintainer / off-box):
    1. Pull this phase's changes and load GUImorph normally.
    2. Load specimens, run GPA compute.
    3. Click **Plot Aligned Specimens** and **Plot Mean Shape** → the native interactive rgl OpenGL window opens as before (NOT a browser widget) and comes to the front.
    4. Click **PCA (morphospace)** → the base-graphics window opens as before.
    5. Confirm no behavioral change vs. the pre-phase build (windows, not browser tabs; no `rgl.useNULL` side effects).
  </how-to-verify>
  <resume-signal>Type "approved" once a Windows maintainer confirms result plots render unchanged, or record the outcome as accepted-pending-off-box in the SUMMARY if the maintainer round-trip is deferred.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `.planning/phases/06-rgl-result-plot-fallback-on-macos/06-02-SUMMARY.md` — records the live macOS render confirmations (three plots + engine-viewport survival) and the off-box Windows regression outcome.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; introduces no new code paths or inputs beyond those threat-modeled in 06-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | verifying against a stale deployed `tkogl2.dylib` | low | mitigate | Confirm the loaded binary before the macOS checkpoint via `tclvalue(tcl("add", "getCompileInformation", -1, 0, 0))` (STATE process note) so the render is validated against the intended build. |
</threat_model>

<verification>
- Both checkpoints return "approved" (or Windows recorded as accepted-pending-off-box with maintainer follow-up tracked).
- `06-02-SUMMARY.md` captures which browser opened, that widgets rotate, that PCA quartz appeared, and the engine viewport survived a post-plot pick.
</verification>

<success_criteria>
- ANL-02 criterion 1 confirmed live on macOS: aligned-specimen + mean-shape widgets render in the browser; PCA renders via quartz.
- CMP-01 confirmed (or accepted-pending-off-box): Windows result plots render unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-rgl-result-plot-fallback-on-macos/06-02-SUMMARY.md` when done.
</output>
