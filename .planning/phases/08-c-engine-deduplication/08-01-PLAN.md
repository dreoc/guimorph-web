---
phase: 08-c-engine-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrated-guimorph-development_EOC/Project/tkogl2/src/marker.h
  - integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak
autonomous: false
requirements: [CENG-02]
must_haves:
  truths:
    - "A pre-Phase-8 copy of the deployed DLL exists so any regression can be rolled back and compared side-by-side (D-13)"
    - "marker.h defines the unified marker_set_t contract and the marker_* core API that 08-02 implements"
    - "The MSVC build toolchain is confirmed available on the Windows build machine (MinGW renders a black mesh and is unsupported)"
  artifacts:
    - path: "integrated-guimorph-development_EOC/Project/tkogl2/src/marker.h"
      provides: "marker_set_t struct + marker_* core prototypes (cross-module unified marker API)"
      contains: "marker_set_t"
    - path: "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak"
      provides: "Regression-rollback backup of the pre-Phase-8 deployed DLL"
  key_links:
    - from: "integrated-guimorph-development_EOC/Project/tkogl2/src/marker.h"
      to: "integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h"
      via: "#include for dot_t / point_t / color_t / show_mode_t"
      pattern: "#include \"def_ZARF_9.h\""
---

<objective>
Characterize the dot/anchor unification as an executable contract and capture the
regression-safety net before any C source is touched.

This plan produces (a) the pre-Phase-8 DLL backup (D-13), (b) `marker.h` — the new
cross-module header that declares the `marker_set_t` struct and the shared `marker_*`
core API the rest of the phase implements, and (c) a confirmation gate that the MSVC
toolchain is present (MinGW is unsupported — it links cleanly but renders a black mesh).

This is the interface-first step: 08-02 implements `marker.c` against the contract
defined here; 08-03 deploys and runs UAT.

Purpose: Lock the unified API shape and behavior decisions (D-03 anchor fixes, D-04
landmark preservation, D-05 logging trim) into a header the implementer follows, and
guarantee a rollback artifact exists before the refactor begins.
Output: `marker.h`, `tkogl2.dll.pre-phase8.bak`, confirmed MSVC availability.
</objective>

<execution_context>
@/home/akagi/home/GUImorph/.cursor/gsd-core/workflows/execute-plan.md
@/home/akagi/home/GUImorph/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-c-engine-deduplication/08-CONTEXT.md
@.planning/phases/08-c-engine-deduplication/08-RESEARCH.md
@.planning/phases/08-c-engine-deduplication/08-PATTERNS.md
@integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h
@integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back up the deployed pre-Phase-8 DLL (D-13)</name>
  <files>integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak</files>
  <read_first>
    - .planning/phases/08-c-engine-deduplication/08-RESEARCH.md (§"Runtime State Inventory" — build-artifact lifecycle; §"Environment Availability")
    - .planning/phases/07-c-engine-modularization/07-03-SUMMARY.md (precedent: `.pre-phase7.bak` backup before redeploy)
  </read_first>
  <action>
    Copy the currently deployed DLL at
    `integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll`
    to a sibling backup `…/inst/libs/x64/tkogl2.dll.pre-phase8.bak`, mirroring the Phase 7
    `tkogl2.dll.pre-phase7.bak` pattern (D-13). Do not overwrite an existing
    `.pre-phase8.bak` if one is already present (the first capture is the canonical
    pre-phase baseline). This artifact is the rollback/side-by-side comparison reference if
    a landmark regression is suspected during 08-03 UAT.
  </action>
  <acceptance_criteria>
    - `integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak` exists and is byte-identical to the source DLL at capture time (same file size).
    - The live `…/inst/libs/x64/tkogl2.dll` is unmodified by this task.
  </acceptance_criteria>
  <verify>
    <automated>test -f "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak" && cmp "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll" "integrated-guimorph-development_EOC/Project/GUImorphDevelopment/inst/libs/x64/tkogl2.dll.pre-phase8.bak"</automated>
  </verify>
  <done>Backup file exists and matches the live deployed DLL; live DLL untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create marker.h — marker_set_t struct + marker_* core contract (D-02, D-06, D-08)</name>
  <files>integrated-guimorph-development_EOC/Project/tkogl2/src/marker.h</files>
  <read_first>
    - integrated-guimorph-development_EOC/Project/tkogl2/src/tcl_log.h (header skeleton analog: `#pragma once` + `#ifndef …_H` guard + `#include "def_ZARF_9.h"` + decls + `#endif`)
    - integrated-guimorph-development_EOC/Project/tkogl2/src/def_ZARF_9.h (dot_t/point_t/color_t/show_mode_t types at ~lines 75-92; existing dot_*/anchor_* declarations ~lines 174-256)
    - .planning/phases/08-c-engine-deduplication/08-RESEARCH.md (§"Parallel Global State → marker_set_t"; §"Anchor Asymmetry Bug Specifics"; §"API Surface Inventory")
    - .planning/phases/08-c-engine-deduplication/08-PATTERNS.md (`marker.h` section — header guard + include + declaration shape)
  </read_first>
  <action>
    Create `marker.h` following the `tcl_log.h` skeleton: `#pragma once`, an `#ifndef
    MARKER_H` / `#define MARKER_H` guard, `#include "def_ZARF_9.h"` (provides `dot_t`,
    `point_t`, `color_t`, `show_mode_t`), and a closing `#endif`. Declare the unified
    contract per D-02/D-06/D-08:
      - The `marker_set_t` struct (field names are Claude's discretion per D-02; use the
        RESEARCH shape): a `dot_t**` list-of-slices pointer (was `dots`/`anchors`), an int
        active-slice id (was `dot_slice_id`/`anchor_slice_id`), an int allocated-slice
        count (was `*_slice_amount`), a `dot_t*` selected node (was `selected`/`anchor_selected`),
        an int selected index (was `selected_id`/`anchor_selected_id`), and a `show_mode_t`
        node_type so `marker_add` stamps `LANDMARK` vs `ANCHOR` from the set (per RESEARCH
        Pattern 1 / Pitfall 3).
      - Prototypes for the shared `marker_*` core that every wrapper will forward into,
        each taking a `marker_set_t*` first argument: at minimum `marker_add`,
        `marker_select`, `marker_move`, `marker_color`, `marker_del`, `marker_del_selected`,
        `marker_size`, `marker_get`, `marker_get_selected`, `marker_selected_id`,
        `marker_slice_index`, `marker_get_slice_id`, `marker_get_slice_amount`,
        `marker_set_slice_amount`, `marker_release_list`, `marker_free`,
        `marker_list_length_at_current_slice`, `marker_get_at_index_current_slice`,
        `marker_size_for_slice_index`, `marker_is_array_null` (final core-function set is
        Claude's discretion, but it must cover every behavior the ~50 public wrappers in
        08-02 need). Do NOT declare the `dot_*`/`anchor_*` wrapper prototypes here — those
        stay in `def_ZARF_9.h` (D-08 reconciliation happens in 08-02); `marker.h` carries
        only the new cross-module surface (`marker_set_t` + `marker_*`).
      - Add a top-of-file comment block recording the behavioral contract this header
        encodes: anchors use their OWN selection state (fixes D-03 BUG-1/2/3), the unified
        del path NULL-guards `s->selected` before deref (fixes D-03 BUG-4), `marker_select`
        keeps the `NULL==p` and `radius<0` guards and the 0-based `id++`-after-match
        convention (D-03 BUG-5/6), landmark observable behavior is unchanged (D-04), and
        the in-loop `IS_IN_RANGE` debug logging is trimmed on merge (D-05). Reference the
        exact `dot_ZARF_9.c` line ranges from PATTERNS so 08-02 has the source-of-truth.
    Do NOT define `marker_set_t` or any `marker_*` body here (header only). Do NOT modify
    `def_ZARF_9.h`, `dot_ZARF_9.c`, `tcl_dispatch.c`, or `CMakeLists.txt` in this plan.
  </action>
  <acceptance_criteria>
    - `marker.h` exists with a `#pragma once` line, an `#ifndef MARKER_H` include guard, and `#include "def_ZARF_9.h"`.
    - `grep -c "marker_set_t" marker.h` ≥ 1 (struct typedef present).
    - `marker.h` declares `marker_add`, `marker_select`, `marker_del_selected`, and `marker_get` (the core every wrapper forwards into).
    - `marker.h` contains no function *bodies* (no `{ … }` implementation; `grep -c "marker_select(.*{" marker.h` == 0 after filtering comments) — declarations only.
    - `def_ZARF_9.h`, `dot_ZARF_9.c`, `tcl_dispatch.c`, and `CMakeLists.txt` are byte-unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd integrated-guimorph-development_EOC/Project/tkogl2/src && test -f marker.h && grep -q "MARKER_H" marker.h && grep -q "marker_set_t" marker.h && grep -q "marker_add" marker.h && grep -q "marker_select" marker.h && grep -q 'def_ZARF_9.h' marker.h</automated>
  </verify>
  <done>marker.h declares the marker_set_t struct + marker_* core API, includes def_ZARF_9.h, and contains no implementation bodies. No other source files changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm MSVC build toolchain is available (Wave 0 gate)</name>
  <what-built>
    The pre-Phase-8 DLL backup and the `marker.h` contract are in place. Before 08-02
    swaps the build, the MSVC toolchain (the ONLY supported path — MinGW renders a black
    mesh per STATE.md 2026-06-21) must be confirmed on the Windows build machine.
  </what-built>
  <read_first>
    - .planning/phases/08-c-engine-deduplication/08-RESEARCH.md (§"Environment Availability"; §"Common Pitfalls" Pitfall 4 — MinGW black mesh)
    - BUILD.md (MSVC build/deploy cycle)
  </read_first>
  <how-to-verify>
    In a Windows **Developer PowerShell for VS 2022** at
    `integrated-guimorph-development_EOC/Project/tkogl2`:
      1. Run `cmake --version` and confirm ≥ 3.16.
      2. Run `cl` (no args) and confirm the Microsoft C/C++ compiler banner appears
         (i.e. the v143 toolset is on PATH via the developer environment).
      3. Confirm the Visual Studio 17 2022 generator is usable:
         `cmake -B build-msvc -G "Visual Studio 17 2022" -A x64` configures without a
         "generator not found" error (you may discard the build-msvc dir afterward).
    If MSVC is NOT available, STOP — do not substitute MinGW; the phase cannot be
    verified without it. Report what is missing.
  </how-to-verify>
  <acceptance_criteria>
    - `cmake --version` reports ≥ 3.16.
    - The MSVC `cl` compiler banner displays (v143 toolset reachable).
    - `cmake … -G "Visual Studio 17 2022" -A x64` configures without generator error.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — toolchain presence is environment state on the Windows build host; verified by the human-check below (no in-repo automated proxy)</automated>
    <human-check>MSVC (cmake ≥3.16 + cl v143 + VS17 2022 generator) confirmed available, per steps above.</human-check>
  </verify>
  <resume-signal>Type "approved" once MSVC is confirmed, or describe what is missing (blocks 08-02).</resume-signal>
  <done>MSVC toolchain confirmed available; 08-02 can build with the supported path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a header declaration + a file copy. No runtime code path, no new external/network/auth/input surface is introduced. The existing R↔Tcl boundary is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `marker.h` contract drift (struct/API mis-shaped vs the parallel statics it must map) | mitigate | Field-for-field map `marker_set_t` to the five `dot_ZARF_9.c:11-21` statics per RESEARCH §"Parallel Global State"; header-only, no behavior yet — caught at compile in 08-02. |
| T-08-01-02 | Denial of Service (data loss) | Pre-Phase-8 DLL backup missing/overwritten before refactor | mitigate | Task 1 creates `tkogl2.dll.pre-phase8.bak` and refuses to overwrite an existing baseline; provides rollback if 08-03 UAT regresses (D-13). |
| T-08-01-03 | Tampering | Toolchain mismatch (MinGW used) silently producing a black-mesh DLL mistaken for a code regression | mitigate | Task 3 blocking gate confirms MSVC-only build (RESEARCH Pitfall 4) before any build in 08-02/08-03. |
| T-08-01-SC | Tampering | Package-manager installs (npm/pip/cargo) | accept | N/A — phase installs no external packages; pure in-tree C edits (RESEARCH §"Standard Stack" — Package Legitimacy Audit N/A). |

No HIGH-severity threats: this plan introduces no executable code path.
</threat_model>

<verification>
- `marker.h` exists, includes `def_ZARF_9.h`, declares `marker_set_t` + the `marker_*` core, and has no bodies.
- `tkogl2.dll.pre-phase8.bak` exists and matches the live deployed DLL.
- MSVC toolchain confirmed (blocking human-verify).
- `def_ZARF_9.h`, `dot_ZARF_9.c`, `tcl_dispatch.c`, `CMakeLists.txt` unchanged this plan.
</verification>

<success_criteria>
- The unified contract (`marker_set_t` + `marker_*` core) is declared in `marker.h` for 08-02 to implement against.
- A rollback DLL backup exists (D-13).
- The supported build toolchain is confirmed (MSVC), unblocking 08-02.
</success_criteria>

## Artifacts this phase produces (introduced in this plan)

- **File:** `integrated-guimorph-development_EOC/Project/tkogl2/src/marker.h` (NEW)
- **Type:** `marker_set_t` struct — fields: list-of-slices `dot_t**`, active slice id `int`, allocated slice count `int`, selected `dot_t*`, selected index `int`, `show_mode_t` node_type (exact field names = Claude's discretion, D-02).
- **Declarations (prototypes only here; defined in 08-02):** `marker_add`, `marker_select`, `marker_move`, `marker_color`, `marker_del`, `marker_del_selected`, `marker_size`, `marker_get`, `marker_get_selected`, `marker_selected_id`, `marker_slice_index`, `marker_get_slice_id`, `marker_get_slice_amount`, `marker_set_slice_amount`, `marker_release_list`, `marker_free`, `marker_list_length_at_current_slice`, `marker_get_at_index_current_slice`, `marker_size_for_slice_index`, `marker_is_array_null`.
- **Backup artifact:** `…/inst/libs/x64/tkogl2.dll.pre-phase8.bak`.

<output>
Create `.planning/phases/08-c-engine-deduplication/08-01-SUMMARY.md` when done.
</output>
